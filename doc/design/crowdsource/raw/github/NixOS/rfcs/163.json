{
 "comments": [
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/rfc-0160-portable-service-layer/33886/1\n",
   "created_at": "2023-10-05T18:37:33Z",
   "id": 1749446474,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-10-05T18:37:33Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "PSA: RFC numbers should match the PR number, so this is RFC 163. I updated the title, but make sure to also update the file path.",
   "created_at": "2023-10-05T19:25:59Z",
   "id": 1749513698,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-10-05T19:25:59Z",
   "user": {
    "id": 20525370,
    "login": "infinisil",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> PSA: RFC numbers should match the PR number, so this is RFC 163. I updated the title, but make sure to also update the file path.\r\n\r\nAh, I wasn't aware of this. I just updated the file",
   "created_at": "2023-10-05T20:23:11Z",
   "id": 1749590087,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-10-05T20:23:11Z",
   "user": {
    "id": 1153271,
    "login": "svanderburg",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "The title of this PR should probably be \"[RFC 163] Portable Service Layer\" or something along those lines, not \"Initial commit\" :^)",
   "created_at": "2023-10-05T22:15:09Z",
   "id": 1749729409,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 5,
    "rocket": 0,
    "total_count": 5
   },
   "updated_at": "2023-10-05T22:15:09Z",
   "user": {
    "id": 3987560,
    "login": "dblsaiko",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> The title of this PR should probably be \"[RFC 163] Portable Service Layer\" or something along those lines, not \"Initial commit\" :^)\r\n\r\nIndeed. I just updated the commit and PR title",
   "created_at": "2023-10-06T10:03:14Z",
   "id": 1750328153,
   "minimized": null,
   "reactions": {
    "+1": 1,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 1
   },
   "updated_at": "2023-10-06T10:03:14Z",
   "user": {
    "id": 1153271,
    "login": "svanderburg",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "(The rendered link is still wrong.)",
   "created_at": "2023-10-06T13:44:04Z",
   "id": 1750706727,
   "minimized": {
    "reason": "outdated"
   },
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-10-11T15:08:26Z",
   "user": {
    "id": 18599032,
    "login": "Atemu",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> (The rendered link is still wrong.)\r\n\r\nAh yes. I've just fixed it!",
   "created_at": "2023-10-06T14:55:24Z",
   "id": 1750822079,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-10-06T14:55:24Z",
   "user": {
    "id": 1153271,
    "login": "svanderburg",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Does this RFC cover dynamic service orchestration or is that our of scope?",
   "created_at": "2023-10-06T15:59:42Z",
   "id": 1750988164,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 2,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 2
   },
   "updated_at": "2023-10-06T15:59:42Z",
   "user": {
    "id": 640797,
    "login": "CMCDragonkai",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> If I, as a maintainer for some NixOS module, do not care for other init systems but systemd, I must be able to write the module in such a way that it is only compatible with systemd. \r\n\r\nA mandatory non-portable module? Why?\r\n\r\n> I must still be able to use e.g. systemd's ExecStart with its exact semantics\r\n\r\nYou want access to systemd the same way the keyword `asm` provides access to the assembler?\r\n\r\nAlso, how portable is that systemd interface?",
   "created_at": "2023-10-06T17:51:10Z",
   "id": 1751187135,
   "minimized": null,
   "reactions": {
    "+1": 1,
    "-1": 1,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 2
   },
   "updated_at": "2023-10-06T17:51:10Z",
   "user": {
    "id": 5954806,
    "login": "AndersonTorres",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/duplication-home-manager-and-nixos-modules/30862/28\n",
   "created_at": "2023-10-06T17:58:19Z",
   "id": 1751195111,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-10-06T17:58:19Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> Does this RFC cover dynamic service orchestration or is that our of scope?\r\n\r\nWhat do you exactly mean with \"dynamic\"? Is it about deploying processes that are not part of a NixOS system profile?",
   "created_at": "2023-10-06T18:04:00Z",
   "id": 1751201497,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-10-06T18:04:00Z",
   "user": {
    "id": 1153271,
    "login": "svanderburg",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> > Does this RFC cover dynamic service orchestration or is that our of scope?\r\n> \r\n> What do you exactly mean with \"dynamic\"? Is it about deploying processes that are not part of a NixOS system profile?\r\n\r\nI like to think of first-class orchestrators. One can imagine this system as an orchestrator. If this system deploys an orchestrator as a service, then that results in some level of dynamic orchestration.",
   "created_at": "2023-10-06T20:25:02Z",
   "id": 1751365013,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-10-06T20:25:02Z",
   "user": {
    "id": 640797,
    "login": "CMCDragonkai",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "To add a bit of other \"prior art\", I'd like to mention[ a project I've been working on recently](https://github.com/DrRuhe/nix2dream), which extends the [drv-parts](https://github.com/DavHau/drv-parts)(Now part of [dream2nix](https://github.com/nix-community/dream2nix)) to define a generic service layer with the module system which can be rendered into configurations for multiple process managers. The project is still in development, but the core module api is pretty much finnished. It allows defining multiple services together in a straightforward way. See [this example]([source](https://github.com/DrRuhe/nix2dream/blob/main/examples/deployments/hello.nix#L9C5-L32C7)):\r\n\r\n```nix\r\ndeployments.hello.services = {\r\n      hello = {\r\n        imports = [\r\n          ../packages/hello.nix #uses the hello package defined in a drv-parts/dream2nix module\r\n        ];\r\n\r\n        service = {\r\n          args = [\r\n            \"-g\"\r\n            ''\"Hello there, $GENERAL!\"'' \r\n          ];\r\n          env.GENERAL = \"General Kenobi\";\r\n        };\r\n      };\r\n      other-service = {\r\n        imports = [\r\n          ../packages/hello.nix\r\n        ];\r\n        service.args = [\r\n          \"-g\"\r\n          ''\"Hello from the other service!\"''\r\n        ];\r\n      };\r\n    };\r\n```\r\n\r\nNote that this setup could also easily be used without drv-parts/dream2nix, by requiring services to set a \"mainProgram\" option instead.\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n",
   "created_at": "2023-10-06T21:05:38Z",
   "id": 1751402179,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-10-06T21:06:44Z",
   "user": {
    "id": 29500714,
    "login": "DrRuhe",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/rfc-0163-portable-service-layer/33886/3\n",
   "created_at": "2023-10-07T13:42:00Z",
   "id": 1751716309,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-10-07T13:42:00Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/what-is-the-equivalent-of-configuration-nix-for-just-the-nix-package-manager/34070/2\n",
   "created_at": "2023-10-11T06:26:01Z",
   "id": 1756890897,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-10-11T06:26:01Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Excellent. Immediate benefits aside this kind of abstraction would be immensely helpful for OS research.\r\n\r\nI'm wondering how far the scope of \"services\" extends above the system-level. Could graphical desktop applications be included? That's a big scope-creep but I imagine there is more incentive to write fresh desktop services than to migrate NixOS services that already work fine where they are.\r\n\r\nI run [Synit](https://synit.org/) as a second-level init system and I can't be bothered to rewrite the systemd services from NixOS, but I do write services from-scratch for my desktop environment.",
   "created_at": "2023-10-11T11:46:50Z",
   "id": 1757508412,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-10-11T11:46:50Z",
   "user": {
    "id": 10137,
    "login": "ghost",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> Could graphical desktop applications be included?\r\n\r\nIt looks very natural, since Home-Manager has support for X11 window managers.",
   "created_at": "2023-10-11T18:57:47Z",
   "id": 1758324063,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-10-11T18:57:47Z",
   "user": {
    "id": 5954806,
    "login": "AndersonTorres",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I have a plan to assimilate [Home Manager](https://github.com/nix-community/home-manager) to Nixpkgs repo.\r\nCan your RFC help this transition, since it would provide a portable service layer to HM too?",
   "created_at": "2023-10-15T02:16:23Z",
   "id": 1763238786,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 3,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 3
   },
   "updated_at": "2023-10-15T02:16:23Z",
   "user": {
    "id": 5954806,
    "login": "AndersonTorres",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": ">> If I, as a maintainer for some NixOS module, do not care for other init systems but systemd, I must be able to write the module in such a way that it is only compatible with systemd.\r\n>\r\n>A mandatory non-portable module? Why?\r\n\r\nI assume the point is «I want some weird kind of dependency that I barely managed to figure out with systemd and don't know to express in anything more generic»",
   "created_at": "2023-10-15T08:35:03Z",
   "id": 1763321451,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 1,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 1
   },
   "updated_at": "2023-10-15T08:35:03Z",
   "user": {
    "id": 1891350,
    "login": "7c6f434c",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> A mandatory non-portable module? Why?\r\n\r\nAs I elaborated in my initial comment, this is in order to preserve the ability to use every feature of systemd with its exact semantics as we currently can.\r\n\r\nI'm not saying this should be the default or even desirable but it should be an option; a valid one at that.\r\n\r\n> You want access to systemd the same way the keyword `asm` provides access to the assembler?\r\n\r\nYes. In most cases you shouldn't need it and I too would prefer to use more abstract interfaces but we must also preserve the ability to use the \"bare metal\" access.\r\n\r\nNiche process manager support cannot come at significant burden for everyone else.\r\n\r\n> Also, how portable is that systemd interface?\r\n\r\nWhat do you mean by \"portable\"? Systemd only supports Linux currently.\r\n\r\nAs a NixOS user and maintainer, I should not be forced to care about non-systemd+linux platforms. I should have the option to but not the obligation.\r\n\r\n> I want some weird kind of dependency that I barely managed to figure out with systemd and don't know to express in anything more generic\r\n\r\nThe problem isn't just knowledge, it's having to care; if I cared, I'd know.  \r\nI don't want to have to care about the specifics of 6 different other service managers that were all replaced by a (IMO) superior option that is in use today (status quo).\r\n\r\nAlso, forcing the use of generic interfaces must come at the cost of a loss in configurability as the generic interface cannot be more than the intersection of all process managers' features. If that generic interface was the only option, there'd be no reason to use systemd anymore because none of its desirable features are supported by the legacy process managers it replaced.\r\n\r\nTherefore, as a NixOS user and maintainer, I must be able to use non-generic/specific interfaces if I need to.  \r\n(That should ideally work the other way aswell; if some process manager provides an interface that systemd does not, users of said process manager should also be able to use it.)",
   "created_at": "2023-10-16T15:35:57Z",
   "id": 1764756634,
   "minimized": null,
   "reactions": {
    "+1": 4,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 4
   },
   "updated_at": "2023-10-16T15:35:57Z",
   "user": {
    "id": 18599032,
    "login": "Atemu",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> Niche process manager support cannot come at significant burden for everyone else.\r\n> \r\n\r\nYou are precisely defending the opposite. Systemd is the niche process manager here (even if the hugest one).\r\n\r\n**Generalized** process manager support cannot come at significant burden for everyone else that wants to use their niche process managers.\r\n\r\n> > Also, how portable is that systemd interface?\r\n> \r\n> What do you mean by \"portable\"? Systemd only supports Linux currently.\r\n> \r\n\r\nBesides not being useful outside Linux, I was talking about the systemd as an abstraction.\r\n\r\n-------------------------------\r\n\r\nIn this sense I believe it is a good compromise. A general opt-in interface plus the possibility of writing raw files for direct use. AFAICT Nix provides this already.",
   "created_at": "2023-10-16T17:31:00Z",
   "id": 1764948210,
   "minimized": null,
   "reactions": {
    "+1": 1,
    "-1": 0,
    "confused": 3,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 4
   },
   "updated_at": "2023-10-19T12:05:41Z",
   "user": {
    "id": 5954806,
    "login": "AndersonTorres",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> Also you completely ignored some of systemd's features like service hardening, sandboxing, tmpfiles.d, journald, networkd.\r\nReimplementing that all in shell script is probably possible but at what cost?\r\n\r\nWhy to reimplement anything? Where are these really used that are really important? Point of other service manager is to reduce what's not important.\r\n\r\nAlso, I believe having an alternating implementation added allows for future flexibility as well, maybe about another thing, before it deprecates, who knows?",
   "created_at": "2023-10-22T18:34:08Z",
   "id": 1774166640,
   "minimized": null,
   "reactions": {
    "+1": 1,
    "-1": 4,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 5
   },
   "updated_at": "2023-10-22T18:34:08Z",
   "user": {
    "id": 26714676,
    "login": "Animeshz",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> Why to reimplement anything? Where are these really used that are really important? Point of other service manager is to reduce what's not important.\r\n\r\nBecause those features are extensible used in NixOS? NixOS is for example one of the frontrunners for systemd's LoadCredential, EnvironmentFile and family. Also services are using hardening in various ways like using BindPaths to make service variations easier https://github.com/NixOS/nixpkgs/blob/master/nixos/modules/security/acme/default.nix#L359-L363 . It is also used to give various CAPs to services without giving them full root access.\r\n\r\nAlso configuring those things with plain flat config files is a perfect match for NixOS.\r\nAlso having it for networking a lot better than doing it with scripts. It is way easier to transition between two states cleanly and without complicated conditions on the configures side.\r\n\r\nAnd journald makes logging easier. You no longer need to convince every application where and how to write it's log files and when to rotate them. It automatically rotates files when the disk gets full and compresses them. Of course you can use logrotate but if the file is filled within a short timeframe, your system might just crash because you ran out of disk space.\r\n\r\n> Also, I believe having an alternating implementation added allows for future flexibility as well, maybe about another thing, before it deprecates, who knows?\r\n\r\nbut do we have actual viable alternative implementation that support the features we are using right now? If the alternatives only support the bare minimum of an init system and we need to take care of all the additional features used somehow ourselves then I am not sure if it actually is one.\r\n\r\nThat wouldn't be such a big deal if we wouldn't use those features but we are using them because they are convenient, save us time and allow us to focus on the actual service instead of worrying about shellscripts that start it.",
   "created_at": "2023-10-22T19:14:41Z",
   "id": 1774175817,
   "minimized": null,
   "reactions": {
    "+1": 6,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 6
   },
   "updated_at": "2023-10-22T19:14:41Z",
   "user": {
    "id": 7258858,
    "login": "SuperSandro2000",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> Besides not being useful outside Linux, I was talking about the systemd as an abstraction.\r\n\r\nBeing a bit more precise: \r\n1. what does Systemd provide that no other similar software stack (like, say, the native NetBSD or FreeBSD or even GNU Shepherd from Guix) can do?\r\n2. how much can we live without those extras?\r\n3. for the particular cases the extras are needed, what does it affect the general case?",
   "created_at": "2023-10-22T20:06:05Z",
   "id": 1774186716,
   "minimized": null,
   "reactions": {
    "+1": 1,
    "-1": 3,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 1,
    "rocket": 0,
    "total_count": 5
   },
   "updated_at": "2023-10-22T20:06:05Z",
   "user": {
    "id": 5954806,
    "login": "AndersonTorres",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> I appreciate and like the idea but adding extra layers of abstractions even for NixOS is not a good looking sign.\r\n\r\npersonally i agree with that statement and i think we shouldn't add a layer of abstraction on top of service managers - but i definitely think we should add a \"portable service layer\"... it just depends how you define that\r\n\r\nas @infinisil and i discussed [above](https://github.com/NixOS/rfcs/pull/163#discussion_r1348215130):\r\n\r\n> I can imagine a `service.nix` which declares a common set of options for a module and then several other files (`systemd.nix`, `supervisord.nix`, `launchd.nix`, etc...) which define different implementations of these options (maybe even declaring some implementation specific options of their own). I love how this could tie into RFC 140!\r\n\r\ni think this is the only way to add more without creating a burden on people who only care about `systemd` implementations",
   "created_at": "2023-10-22T20:29:18Z",
   "id": 1774191316,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-10-22T20:29:18Z",
   "user": {
    "id": 7755101,
    "login": "aanderse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> Also you completely ignored some of systemd's features like service hardening, sandboxing, tmpfiles.d, journald, networkd.\r\n\r\nIt's worth mentioning that some systemd-* commands work well as standalone binaries, while some need a small patchset. Artix and Gentoo have been doing this for a long time.",
   "created_at": "2023-10-23T03:01:53Z",
   "id": 1774358134,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 1,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 1
   },
   "updated_at": "2023-10-23T03:01:53Z",
   "user": {
    "id": 42209822,
    "login": "Aleksanaa",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Ah yes, that reminds me of, eudev and elogind, that has been forked out from systemd as standalone package by gentoo and its even used by void and other distributions.",
   "created_at": "2023-10-23T03:04:30Z",
   "id": 1774359752,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-10-23T03:04:30Z",
   "user": {
    "id": 26714676,
    "login": "Animeshz",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "About this extirpation from systemd:\r\n\r\nhttps://guix.gnu.org/en/blog/2016/gnome-in-guixsd/",
   "created_at": "2023-10-23T03:09:18Z",
   "id": 1774362731,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-10-23T03:10:39Z",
   "user": {
    "id": 5954806,
    "login": "AndersonTorres",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> all alternatives to systemd lack major features of it is really worrying me.\r\n\r\nI don't want to use systemd but it really does have the best features of all the init systems available. Something better than systemd is going to come along so we should be ready to pivot rather than have a messy transition with a lot of bad feelings, which happened with systemd and some of the other distros.\r\n\r\nDoing the abstraction now avoids fighting and forking later.",
   "created_at": "2023-10-23T09:29:24Z",
   "id": 1774787250,
   "minimized": null,
   "reactions": {
    "+1": 11,
    "-1": 0,
    "confused": 0,
    "eyes": 2,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 13
   },
   "updated_at": "2023-10-23T09:29:24Z",
   "user": {
    "id": 10137,
    "login": "ghost",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> Something better than systemd is going to come along\n\nSome time ago we talked about initware, a portable implementation of systemd.\n\nhttps://discourse.nixos.org/t/should-we-support-initware-is-a-fork-of-systemd-ported-to-different-bsd-systems/14400",
   "created_at": "2023-10-23T10:43:24Z",
   "id": 1774918805,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-10-23T10:43:24Z",
   "user": {
    "id": 5954806,
    "login": "AndersonTorres",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Ah, the perpetual question about abstraction.\r\nWhat's nice about the module system is that abstractions can coexist with their implementation and lower level counterparts within the same configuration system.\r\nWhether a module uses high level or low level options to achieve its goals should be up to the module's maintainers; not a decision that is preempted by others.\r\n\r\nIn this case, assuming a module-based implementation of the portable service \"layer\", the portable part could be considered the high level definitions, which can be supplemented with \"low level\" systemd-specific configuration.\r\n\r\nSo we can have all kinds of service modules.\r\n- Service modules that are simply portable.\r\n- Service modules that have systemd-specific optimizations. They configure both portable and systemd-specific options.\r\n- Serivce modules that are non-portable but use some portable options for convenience.\r\n- Non-portable modules that only define systemd-specific options. Some people prefer this, and that's ok. Could be personal preference, or the nature of the service. For instance, a portable `journald` service seems over-engineered.",
   "created_at": "2023-10-23T11:43:07Z",
   "id": 1775011732,
   "minimized": null,
   "reactions": {
    "+1": 12,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 1,
    "total_count": 13
   },
   "updated_at": "2023-10-23T11:43:16Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/os-agnostic-way-of-starting-a-new-service/34772/2\n",
   "created_at": "2023-10-29T21:00:15Z",
   "id": 1784223607,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-10-29T21:00:15Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/2023-10-31-nixpkgs-architecture-team-meeting-45/34846/1\n",
   "created_at": "2023-10-31T18:01:21Z",
   "id": 1787719998,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-10-31T18:01:21Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This RFC is now open for shepherd nominations!",
   "created_at": "2023-11-01T14:02:11Z",
   "id": 1789011908,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 5,
    "total_count": 5
   },
   "updated_at": "2023-11-01T14:02:11Z",
   "user": {
    "id": 1148549,
    "login": "edolstra",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/restructuring-nixos-to-work-without-systemd-e-g-with-sysvinit/21298/47\n",
   "created_at": "2023-11-01T18:22:08Z",
   "id": 1789441087,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-11-01T18:22:08Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> If someone wants to write glue to port services to other init systems - they are welcome, but I don't think we have the capacity to provide full-fledged support for those.\r\n\r\nIts exactly about making the current service layer co-existable with other service manager. It is by no means expecting all the packages to support every service manager.\r\n\r\nQuoting back from [roberth's comment](https://github.com/NixOS/rfcs/pull/163#issuecomment-1775011732),\r\n> Whether a module uses high level or low level options to achieve its goals should be up to the module's maintainers; not a decision that is preempted by others.",
   "created_at": "2023-11-05T20:15:25Z",
   "id": 1793834407,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-11-05T20:15:25Z",
   "user": {
    "id": 26714676,
    "login": "Animeshz",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> I don't want to use systemd but it really does have the best features of all the init systems available. Something better than systemd is going to come along so we should be ready to pivot rather than have a messy transition with a lot of bad feelings, which happened with systemd and some of the other distros.\r\n> \r\n> Doing the abstraction now avoids fighting and forking later.\r\n\r\nThe problem with that is, that we don't know how that future will look like and we could focus abstraction on an area which is later identically and we missed abstraction in an area which could be totally different.\r\n\r\nAlso if the feature sets we abstract around would be at least somewhat similar then we would have a good basis to start things off but right now it looks more like that the alternatives implement only a tiny fraction of what we are using and lots of things would need workarounds to continue to function.\r\n\r\n> Whether a module uses high level or low level options to achieve its goals should be up to the module's maintainers; not a decision that is preempted by others.\r\n\r\nUp to some extend. Obviously we shouldn't replicate higher level options with lower level ones that already exist in existing modules no matter that taste of the maintainer.\r\n\r\n> Service modules that have systemd-specific optimizations. They configure both portable and systemd-specific options.\r\n\r\nWouldn't this make modules rather complex? eg you configure most things for systemd in the systemd options but not how to start things that needs to be done in that nixos specific abstraction. I'd imagine that a major hurdle which wouldn't make contributing up to standard things a bit harder. We'd probably would need some tooling which could automatically convert those things or automatically populate those fields.\r\n\r\nAlso I am worried that the module system could slow down. It isn't the fastest right now and some of my machines already take multiple minutes to evaluate and tracking down performance problems in my own config isn't usually that easy to do. Maybe bringing some laziness back into the system and improving overhaul performance by maybe using native types alongside this could improve things to not notice the extra abstraction overhead.\r\n\r\n",
   "created_at": "2023-11-12T18:15:45Z",
   "id": 1807201333,
   "minimized": null,
   "reactions": {
    "+1": 1,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 1
   },
   "updated_at": "2023-11-12T18:15:45Z",
   "user": {
    "id": 7258858,
    "login": "SuperSandro2000",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> Also I am worried that the module system could slow down.\r\n\r\nUnfortunately NixOS is not a good example of how to use the module system in a scalable way.\r\nThis service layer is an opportunity to effectively implement #22 minimal module list, because it gets rid of those global and always-loaded `services.foo.enable` options and all the overhead parsing their files brings along.\r\n@infinisil's [gist](https://github.com/NixOS/rfcs/pull/163#discussion_r1347854623) doesn't show how to solve the performance problem, but [mine](https://github.com/NixOS/rfcs/pull/163#discussion_r1348841973) does, because of the explicit `imports`.\r\nExplicit imports could be added to his proposal too, but that may feel like unnecessary duplication.\r\n\r\n> Maybe bringing some laziness back into the system\r\n\r\nThe module system itself is already rather lazy, but it depends on how you use it. Besides the non-scalable pattern of NixOS currently importing each and every service always, there's very little to worry about.\r\n",
   "created_at": "2023-11-12T19:35:34Z",
   "id": 1807218691,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 8,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 8
   },
   "updated_at": "2023-11-12T19:35:34Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "roberth put out a PoC. Great job, thank you for your effort!\r\n\r\nOne thing immediately sticks out to me, though: we need a lot of glue code in Nix in order to bridge the module definition to a specific implementation (such as systemd). I fear this might become an issue: NixOS modules already have messy errors sometimes, and adding a lot of glue on top will exacerbate the problem when writing your own modules.\r\n\r\nBy the way, can't we translate the current `services.systemd` definitions into abstract definitions? I think it has enough information, and if we are able to pull it off, then the migration to the new system is basically free.",
   "created_at": "2023-11-13T11:06:33Z",
   "id": 1807947781,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 1,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 1
   },
   "updated_at": "2023-11-13T11:06:33Z",
   "user": {
    "id": 66781795,
    "login": "KFearsoff",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> By the way, can't we translate the current `services.systemd` definitions into abstract definitions? I think it has enough information, and if we are able to pull it off, then the migration to the new system is basically free.\r\n\r\nA few people have tried this and it's not been practical. In my experience one can sometime reuse `config.systemd.services.«…».serviceConfig.ExecStart` but not without some manipulation. Something to consider is that systemd security features are opt-in, and an init system designed for security is going to be opt-out for hardening. This means that there is little logical overlap for options and services need to be ported by hand. ",
   "created_at": "2023-11-15T07:37:01Z",
   "id": 1811946120,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-11-15T07:37:01Z",
   "user": {
    "id": 10137,
    "login": "ghost",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> can't we translate the current services.systemd definitions into abstract definitions? \r\n\r\nhttps://git.sr.ht/~guido/nixos-init-freedom/tree/master/item/systemd-to-s6.nix\r\n\r\nHowever, although it exists as a workaround, it doesn't make sense to follow the step of systemd forever.",
   "created_at": "2023-11-15T11:22:07Z",
   "id": 1812334219,
   "minimized": null,
   "reactions": {
    "+1": 1,
    "-1": 0,
    "confused": 0,
    "eyes": 2,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 3
   },
   "updated_at": "2023-11-15T11:22:07Z",
   "user": {
    "id": 42209822,
    "login": "Aleksanaa",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Makes sense. Let's add those bits of info to the RFC text, cos it's pretty important.",
   "created_at": "2023-11-15T12:53:38Z",
   "id": 1812486060,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-11-15T12:53:38Z",
   "user": {
    "id": 66781795,
    "login": "KFearsoff",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/community-calendar/18589/99\n",
   "created_at": "2023-11-22T14:01:57Z",
   "id": 1822831647,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-11-22T14:01:57Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I'd like to nominate myself as shepherd.",
   "created_at": "2023-11-23T16:40:19Z",
   "id": 1824709511,
   "minimized": null,
   "reactions": {
    "+1": 3,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 18,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 21
   },
   "updated_at": "2023-11-23T16:40:19Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I have few to no experience on this field.\r\nHowever I am interested on this because of my next RFC draft.\r\n\r\nRecruit Torres, presenting myself as shepherd.",
   "created_at": "2023-12-03T00:44:42Z",
   "id": 1837294466,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 5,
    "laugh": 0,
    "rocket": 0,
    "total_count": 5
   },
   "updated_at": "2023-12-03T00:44:42Z",
   "user": {
    "id": 5954806,
    "login": "AndersonTorres",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I have experience with unconventional init systems and I have some understanding of nix-processmgmt (https://github.com/svanderburg/nix-processmgmt/pull/5). I want to realize a portable service abstraction but not with performance regressions.\r\n\r\nI nominate myself as a shepherd.",
   "created_at": "2023-12-03T13:41:16Z",
   "id": 1837484246,
   "minimized": null,
   "reactions": {
    "+1": 10,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 10
   },
   "updated_at": "2023-12-03T16:04:31Z",
   "user": {
    "id": 10137,
    "login": "ghost",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I also have a long-standing experience in unconventional init systems (in linux desktops) such as [runit](http://smarden.org/runit/), [openrc](https://github.com/OpenRC/openrc), [s6](https://github.com/skarnet/s6), etc., having used to many systems in the past. I've actively followed projects like [notos](https://github.com/cleverca22/not-os) and [ZilchOS](https://github.com/ZilchOS/core) and [NixNG](https://github.com/nix-community/NixNG) for quite some time, and have been trying to keep the nix stack as portable as possible for my personal projects.\r\n\r\nI've seen [numtide/devshell](https://github.com/numtide/devshell) way of allowing services on a local-scope ephemeral shell, and [it might have a great benefit if this rfc happens to take effect](https://github.com/numtide/devshell/issues/170).\r\n\r\nAs for my experience with nix, even though I'm <1 yr experienced with nix, I've read the original thesis and following up nix development with NixCon and NixHour.\r\n\r\nThus, I'd like to nominate myself as shepherd if nobody have any problem :smiley: ",
   "created_at": "2023-12-04T10:35:53Z",
   "id": 1838265642,
   "minimized": null,
   "reactions": {
    "+1": 6,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 6
   },
   "updated_at": "2023-12-04T10:35:53Z",
   "user": {
    "id": 26714676,
    "login": "Animeshz",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I support this overall design direction and am interested in helping to make it happen. Therefore, I nominate myself as a shepherd.\r\n\r\nI see a few things that feel \"missing\" from the current draft, which I'd like to see investigated.\r\n\r\n1. process activation on a timer (systemd and oldschool cron both support this)\r\n2. process activation from a socket (IP or unix-domain; systemd and inetd both support this)\r\n3. designation of a process as one-shot vs. runs-forever (matters to status checks)\r\n\r\noverall this is very solid. I like the approach of pushing backend-specific stuff into an \"overrides\" option; that should allow faster migration to the new abstraction.",
   "created_at": "2023-12-07T07:16:18Z",
   "id": 1844797334,
   "minimized": null,
   "reactions": {
    "+1": 4,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 4
   },
   "updated_at": "2023-12-07T07:16:18Z",
   "user": {
    "id": 157678,
    "login": "IreneKnapp",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> As for my experience with nix, even though I'm <1 yr experienced with nix, I've read the original thesis and following up nix development with NixCon and NixHour.\r\n\r\nWhile I can't assess experience from a distance, I would say that relative inexperience could be an asset to the shepherd team, for more of a fresh perspective on the significant \"human interface\" aspect of the proposal. :+1: ",
   "created_at": "2023-12-07T09:19:01Z",
   "id": 1844975613,
   "minimized": null,
   "reactions": {
    "+1": 7,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 7
   },
   "updated_at": "2023-12-07T09:19:01Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "is there a chat room or anything for the shepherd team? should we set one up?",
   "created_at": "2023-12-27T04:27:44Z",
   "id": 1869936681,
   "minimized": null,
   "reactions": {
    "+1": 1,
    "-1": 0,
    "confused": 0,
    "eyes": 1,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 2
   },
   "updated_at": "2023-12-27T04:27:44Z",
   "user": {
    "id": 157678,
    "login": "IreneKnapp",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I would also like to get moving on this. I don't use matrix though so I would ask that a chat be bridged to IRC.",
   "created_at": "2023-12-27T08:08:11Z",
   "id": 1870054370,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2023-12-27T08:08:11Z",
   "user": {
    "id": 10137,
    "login": "ghost",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I believe it's important for services to be able to include other services as part of their implementation. I'd like this to be possible by means of an `attrsOf submodule`, which automatically takes care of adding a prefix to the final (e.g. systemd) service names. This seems feasible, and somewhat suggests that there's a redundancy between my poc's ability for a portable service to declare e.g. multiple service units. However, I'm not yet entirely sure of that, because of non-service units. Needs more thought.\r\n\r\nMotivating use case: typical web apps require a database, and agreeing on the version is somewhat [hard](https://github.com/NixOS/nixpkgs/pull/223064#issuecomment-1887148975) and even if we solve that, we may still want to run more than one version anyway. \"Having to\" agree on a version is also somewhat counter to the spirit of Nix, where store paths can always coexist rather well.",
   "created_at": "2024-01-11T13:28:54Z",
   "id": 1887165651,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-01-11T13:28:54Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "@ehmry did you make \"hackint IRC Bridge status\" contact me on matrix some time ago, or is that a coincidence? Did you set up a room on IRC?",
   "created_at": "2024-01-11T13:30:26Z",
   "id": 1887168182,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-01-11T13:30:26Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> \"Having to\" agree on a version is also somewhat counter to the spirit of Nix, where store paths can always coexist rather well.\r\n\r\nI'd argue passing an overridable version of a library as an input is pretty similar. If the goal is to have useful layering, maybe this could be the view of the lower level portable-service, with a higher level passing the stuff one way or another.",
   "created_at": "2024-01-11T13:35:46Z",
   "id": 1887177216,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-01-11T13:35:46Z",
   "user": {
    "id": 1891350,
    "login": "7c6f434c",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "@7c6f434c I agree that's a valid perspective.\r\n\r\nThe module system provides overriding. For instance, the web app service module could declare an option that contains the postgres service *module* (ie `types.deferredModule`) to use for implementing its postgres dependency. If that option has a default, that means we're either hardcoding implementations or doing some sort of dependency injection.\r\n\r\nAlso worth considering is the use case where admin wants to use an external database; just a connection configuration. Indeed this seems to lead to a distinction between a low level module (scoped to approximately just the `.service`) and a high level service that is self-sufficient and sets up a database. The high level module could import the low level module and add/\"implement\" operational dependencies such as a postgres instance.",
   "created_at": "2024-01-11T14:10:55Z",
   "id": 1887245077,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-01-11T14:10:55Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Hi again,\r\n\r\nWhile I did not have the time to make substantial further progress, I had some time to tinker on [nix2dream](https://github.com/DrRuhe/nix2dream) (Which, although sounds similar to - and also currently depends on - [dream2nix](https://github.com/nix-community/dream2nix), is not officially affiliated to dream2nix)).\r\nWith that experience, I'd like to nominate myself as a shepherd as well. \r\n",
   "created_at": "2024-01-16T21:18:54Z",
   "id": 1894528753,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-01-16T21:18:54Z",
   "user": {
    "id": 29500714,
    "login": "DrRuhe",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I would love to see this become a reality, if this becomes merged I will be a user of the runit (or openrc) backend (helping fix services, testing etc)",
   "created_at": "2024-01-21T17:36:44Z",
   "id": 1902705230,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 4,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 4
   },
   "updated_at": "2024-01-21T17:36:44Z",
   "user": {
    "id": 67164465,
    "login": "IogaMaster",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I went ahead and asked about how to make a channel. It turns out it's easy. On Matrix, join #nix-rfc-163:hackint.org. on Hackint IRC, join #nix-rfc-163. this is for the shepherd team to coordinate, not for public feedback, just to set that expectation, but I'm not gatekeeping who joins or anything.",
   "created_at": "2024-01-21T20:43:39Z",
   "id": 1902758302,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-01-21T20:43:39Z",
   "user": {
    "id": 157678,
    "login": "IreneKnapp",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "okay, I sent Matrix invites to everyone who listed a Matrix account in the maintainer list. @svanderburg @ehmry @Animeshz you're the ones not listed there, please join at your convenience and let's start building some momentum on this :D",
   "created_at": "2024-01-21T21:18:57Z",
   "id": 1902766627,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 3,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 3
   },
   "updated_at": "2024-01-21T21:18:57Z",
   "user": {
    "id": 157678,
    "login": "IreneKnapp",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "After playing a bit with nix-processmgmt the missing piece that concerns me most is how to generate documentation from code. In terms of feature parity I don't care about timers and socket activation (everything to implement those is present), I want documentation parity. For me `man configuration.nix` is a killer feature of NixOS.",
   "created_at": "2024-01-31T09:24:28Z",
   "id": 1918707841,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-01-31T09:24:28Z",
   "user": {
    "id": 10137,
    "login": "ghost",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "We could do that with the module system. Translating function calls to modules is fairly straightforward, so we could approach it that way. I didn't base my proof of concept on `nix-processmgmt`, but by doing such a translation, we could inherit its knowledge and experience.\r\n\r\n> don't care about timers and socket activation (everything to implement those is present)\r\n\r\nI don't think you have to care. It will be modular like that. :+1: \r\n",
   "created_at": "2024-01-31T11:05:08Z",
   "id": 1918884548,
   "minimized": null,
   "reactions": {
    "+1": 1,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 1
   },
   "updated_at": "2024-01-31T11:05:08Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/is-it-possible-to-define-systemd-services-in-a-submodule/39538/4\n",
   "created_at": "2024-02-11T14:20:50Z",
   "id": 1937768388,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-02-11T14:20:50Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/rfcsc-meeting-2024-03-05/40851/1\n",
   "created_at": "2024-03-05T16:05:44Z",
   "id": 1979117917,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-03-05T16:05:44Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Hey shepherds, we would appreciate a status update or some activity. If there are any blockers, please let us know.\r\n\r\nIf you all are not able to progress on this at the moment, feel free to mark as draft until a better time in the future.",
   "created_at": "2024-03-19T15:46:55Z",
   "id": 2007539874,
   "minimized": null,
   "reactions": {
    "+1": 1,
    "-1": 0,
    "confused": 1,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 2
   },
   "updated_at": "2024-03-19T15:46:55Z",
   "user": {
    "id": 7673602,
    "login": "jonringer",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/rfcsc-meeting-2024-03-19/41829/1\n",
   "created_at": "2024-03-19T16:02:08Z",
   "id": 2007574047,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-03-19T16:02:08Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> Hey shepherds, we would appreciate a status update or some activity. If there are any blockers, please let us know.\r\n> \r\n> If you all are not able to progress on this at the moment, feel free to mark as draft until a better time in the future.\r\n\r\nIs there a Matrix room for discussing the RFC?",
   "created_at": "2024-03-19T19:35:04Z",
   "id": 2007972664,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-03-19T19:35:04Z",
   "user": {
    "id": 97130632,
    "login": "nyabinary",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> Is there a Matrix room for discussing the RFC?\r\n\r\nhttps://matrix.to/#/#nix-rfc-163:hackint.org",
   "created_at": "2024-03-19T19:38:17Z",
   "id": 2007978773,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-03-19T19:38:17Z",
   "user": {
    "id": 42209822,
    "login": "Aleksanaa",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/rfcsc-meeting-2024-04-02/42643/1\n",
   "created_at": "2024-04-02T15:18:16Z",
   "id": 2032344188,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-04-02T15:18:16Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "@AndersonTorres any chance of a shepherd team meeting to get this moving onwards?",
   "created_at": "2024-04-16T14:45:14Z",
   "id": 2059272154,
   "minimized": null,
   "reactions": {
    "+1": 1,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 1
   },
   "updated_at": "2024-04-16T14:45:14Z",
   "user": {
    "id": 341954,
    "login": "lheckemann",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/rfcsc-meeting-2024-04-16/43512/1\n",
   "created_at": "2024-04-16T14:47:01Z",
   "id": 2059276178,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-04-16T14:47:01Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I have some concerns about this. \r\n\r\nOut of everyone who uses Linux, there's a mere fraction of them who don't use systemd. Out of everyone who uses Linux and doesn't use systemd, there's probably an even smaller fraction of them who'd be willing to learn and use NixOS. It seems like a large burden to put on nixpkgs maintainers to satisfy such a small group of users. \r\n\r\nOn that note, I'm not a hard no. I'd be curious to hear more about what this would entail development-wise. But I'm concerned about what it would mean for people who use systemd exclusively in terms of functionality. Would we lose functionality by making NixOS more init-agnostic? If not, how would we develop support for that many init systems plus the entirety of systemd with the already small userbase we have?",
   "created_at": "2024-04-18T19:17:02Z",
   "id": 2065017600,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 4,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 4
   },
   "updated_at": "2024-04-18T19:17:02Z",
   "user": {
    "id": 71774134,
    "login": "deviantsemicolon",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "@deviantsemicolon I think its been answered a few times and written in the detailed design section as well. Services configuration remains similar for most init systems, its just the templating language feels different. The added abstraction will sufficiently provide generic closure that will transpile into any of the service manager and provide overrides for specific service manager if its really important for the services to depend on such constructs (e.g. runlevels).",
   "created_at": "2024-04-18T19:58:24Z",
   "id": 2065161179,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 4,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 4
   },
   "updated_at": "2024-04-18T19:58:24Z",
   "user": {
    "id": 26714676,
    "login": "Animeshz",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I would also point out, @deviantsemicolon, that systemd is the only supported init for more or less every major Linux distribution. The number of people using it is unlikely to reflect the number of people who would _choose_ it, given the possibility of switching without moving to a niche and worse-supported distro.",
   "created_at": "2024-04-24T03:04:22Z",
   "id": 2073922798,
   "minimized": null,
   "reactions": {
    "+1": 3,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 3
   },
   "updated_at": "2024-04-24T03:04:22Z",
   "user": {
    "id": 13078933,
    "login": "hatzka-nezumi",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I cannot imagine an outcome of this RFC where it will not become a burden for most people that are using the default init system. We can already observe this with Darwin or musl support. For Darwin people not owning an expensive Apple device or investing many hours into a hackintosh, have no chance to debug errors and even then errors are often byzantin and require deep apple knowledge (missing headers, linking errors) and even very knowledgeable Linux people often just mark things broken for the platform.\nPeople caring about musl often have to chase after updates and fix musl support afterwards, making it an time expensive endeavor using it. When not doing that for a few months, the very few people maintaining it, sometimes need to fix things very deep down again because software doesn't comply.\n\nI cannot imagine how supporting other init systems will not suffer from the same problems.\nIt will also probably place code in maintainers modules which they don't test on a daily basis or on updates or maybe don't even want it there and it will also place reviewers into hard spots when updates break things but the person that contributed the other init system is not available to fix the breakage and the author does not have the time to deeply dig into it.\n\nWe are also using more and more systemd specific features (hardening, dynamic user) which can break things in subtle ways and where porting is not easy.\n\nAnd we are moving towards having networkd and systemd in the initrd by default, not because people love systemd so much or want to force people onto it, but because it is so much easier and more compatible and fixes many bugs in more complicated setups which before required unreasonable high complex shell scripts.\n\nAlso I don't think the question got answered, when a module maintainer does not want to support other init systems and sees it as a to high burden for him/her/it/... self especially if the module is important.",
   "created_at": "2024-04-24T07:52:07Z",
   "id": 2074306700,
   "minimized": null,
   "reactions": {
    "+1": 12,
    "-1": 6,
    "confused": 3,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 21
   },
   "updated_at": "2024-04-24T07:52:07Z",
   "user": {
    "id": 7258858,
    "login": "SuperSandro2000",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> Also I don't think the question got answered, when a module maintainer does not want to support other init systems and sees it as a to high burden for him/her/it/... self especially if the module is important.\r\n\r\nIs there any reason this would work any differently to how e.g. musl does today?  The expectation is that a package maintainer doesn't have to go out of their way to support musl, but that they also won't veto somebody else doing that work.",
   "created_at": "2024-04-24T09:08:43Z",
   "id": 2074466678,
   "minimized": null,
   "reactions": {
    "+1": 7,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 7
   },
   "updated_at": "2024-04-24T09:08:43Z",
   "user": {
    "id": 2768870,
    "login": "alyssais",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Re: maintenance burdens, one thing that is exciting to me about this RFC is that it could be a step towards sharing code between module systems like NixOS and Nix-Darwin, where currently a lot of Nix-Darwin modules are born as copy/pastes of NixOS modules but then maintained separately. We already *have* whole separate module systems for init systems other than systemd (like Nix-Darwin or the new [NixBSD](https://github.com/nixos-bsd/nixbsd), the process management features on devenv.sh, the process-compose flake-parts module, etc.) in the wider community.\r\n\r\nI understand you looking out for NixOS/Nixpkgs first, @SuperSandro2000, but I think the opportunities for code sharing here could be really good for the wider Nix world. It could reduce some currently duplicated efforts.",
   "created_at": "2024-04-25T18:14:28Z",
   "id": 2077880480,
   "minimized": null,
   "reactions": {
    "+1": 14,
    "-1": 0,
    "confused": 1,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 15
   },
   "updated_at": "2024-04-25T18:14:28Z",
   "user": {
    "id": 3180910,
    "login": "therealpxc",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> Is there any reason this would work any differently to how e.g. musl does today?\r\n\r\nMaking a package work for musl usually involves a patch, special configure flag or disabling some optional feature. Though they may get outdated when maintainers cannot/do not want to test for musl when the packages gets updated, these usually do not get in the way of a maintainer. They can also be removed again quite easily if they break and the maintainer does not want to bother with the support as it's just a bits of code integrated with the regular derivation.  \r\nThere is a slight maintenance burden here as the maintainer has to at least think about the bits of code concerning use-cases they don't care about but I'd consider this degree to be reasonable.   \r\n\r\nAs I understand the current proposal, making a modern NixOS module that makes proper use of systemd features support other service managers would be akin to rewriting the entire derivation to use an entirely different interface where you'd have to manually execute many commands that were previously abstracted away and might not even be able to support the same features of the derivation, just to support musl.  \r\nThat would not be reasonable to expect current or future maintainers to support, even if the transition was contributed by someone else and I apply the same line of reasoning to a potential portable service layer.",
   "created_at": "2024-05-01T13:29:58Z",
   "id": 2088467025,
   "minimized": null,
   "reactions": {
    "+1": 3,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 3
   },
   "updated_at": "2024-05-01T13:29:58Z",
   "user": {
    "id": 18599032,
    "login": "Atemu",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "@therealpxc I don't think anyone is arguing against the benefits of code sharing and abstractions.",
   "created_at": "2024-05-01T13:32:07Z",
   "id": 2088469531,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-05-01T13:32:07Z",
   "user": {
    "id": 18599032,
    "login": "Atemu",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/disnix-dydisnix-disnixos-etc/870/16\n",
   "created_at": "2024-05-01T20:16:15Z",
   "id": 2089066181,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-05-01T20:16:15Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/rfcsc-meeting-2024-05-14/45414/1\n",
   "created_at": "2024-05-14T15:01:00Z",
   "id": 2110472272,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-05-14T15:01:00Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Meeting notes\r\n\r\nAttendees: @Animeshz, @ehmry, @roberth \r\n\r\n- We agree that module system would be good interface\r\n  - Sander, who wasn't in the meeting, would also agree. He and Robert talked at NixCon and FOSSDEM and he's opened the RFC exactly so that the community can improve it and give it a NixOS flavor (paraphrasing)\r\n\r\n- RFC would have been more successful if it had an accompanying NixOS PR. It couldn't yet, because the design / interface design is still open.\r\n\r\n- Preserving effort already put into the Nixos modules is important.\r\n\r\n- roberth *might* be able to reserve some time for work on a non-toy implementation, translating Sander's logic into modules\r\n\r\n- emery is using nix-processmgmt with nixos already and is willing to help integrate some form of it in nixpkgs \r\n\r\n- Animesh is also using runit and openrc implementations personally for his own systems i.e. nix on top of other distros, and willing to contribute / integrate some part of it, although does not hold expertise in nix as a language.\r\n\r\n- No deadlines or timeline defined.\r\n\r\n- Looking for projects that wish to reuse Nixos modules externally and can contribute\r\n\r\n- Author of [Synit](https://synit.org/) is interested in basing on Nixos, but not without an abstraction to hook into.",
   "created_at": "2024-05-17T12:54:40Z",
   "id": 2117540933,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 9,
    "hooray": 0,
    "laugh": 0,
    "rocket": 6,
    "total_count": 15
   },
   "updated_at": "2024-05-17T12:54:40Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I'll add that it was mentioned that substantial parts of this RFC may be rewritten after we have a serious implementation, and discussion around that will probably happen on nixpkgs/nixos PRs that will reference back to this RFC.",
   "created_at": "2024-05-17T13:06:44Z",
   "id": 2117565164,
   "minimized": null,
   "reactions": {
    "+1": 3,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 3
   },
   "updated_at": "2024-05-17T13:06:44Z",
   "user": {
    "id": 10137,
    "login": "ghost",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/rfcsc-meeting-2024-05-28/46113/1\n",
   "created_at": "2024-05-28T14:56:25Z",
   "id": 2135441802,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-05-28T14:56:25Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> Looking for projects that wish to reuse Nixos modules externally and can contribute\r\n\r\nCan you elaborate on what exactly does externally mean?\r\n\r\nI do have a project relying heavily on NixOS modules and actually have been using disnix/dysnomia for that very same project a while ago. I switched since to NixOS modules because I wanted to reuse services from nixpkgs instead.\r\n\r\nI’d be happy to help this RFC move along. I personally am seduced by the appeal of easily expressing having multiple instances of a service live side by side.",
   "created_at": "2024-05-28T16:28:23Z",
   "id": 2135671275,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-05-28T16:28:49Z",
   "user": {
    "id": 1044950,
    "login": "ibizaman",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I would definitely talk to @rhelmot and @artemist about NixBSD, and also @netbsduser the Initware person.\r\n\r\nI think approaching this stuff from both ends (generalization of NixOS, dedup of Home Manager, https://github.com/nixos-bsd/nixbsd, etc.) is how we get this done. We can iteratively arrive at a design with all that.",
   "created_at": "2024-05-28T16:52:46Z",
   "id": 2135713303,
   "minimized": null,
   "reactions": {
    "+1": 5,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 5
   },
   "updated_at": "2024-05-28T16:52:46Z",
   "user": {
    "id": 1055245,
    "login": "Ericson2314",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> > Looking for projects that wish to reuse Nixos modules externally and can contribute\r\n>\r\n> Can you elaborate on what exactly does externally mean?\r\n\r\nThis means projects that are NixOS alternatives that aren't using s******. If you would like to work on making this possible from within the nixpkgs repo there will friendly reviewers to stand behind you.",
   "created_at": "2024-05-29T07:57:27Z",
   "id": 2136780744,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 1,
    "total_count": 1
   },
   "updated_at": "2024-05-29T07:57:27Z",
   "user": {
    "id": 10137,
    "login": "ghost",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/rfcsc-meeting-2024-06-10/46817/1\n",
   "created_at": "2024-06-10T15:50:08Z",
   "id": 2158724911,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-06-10T15:50:08Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "To clarify, would this allow people to use init systems other than systemd?",
   "created_at": "2024-06-22T04:01:31Z",
   "id": 2183767738,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-06-22T04:01:31Z",
   "user": {
    "id": 71774134,
    "login": "deviantsemicolon",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "@deviantsemicolon NixOS is currently highly dependent on systemd, so there's neither an ETA nor plans to make it replaceable, but surely this will fulfil the usages of nix as a portable package manager and build system -- allowing it to integrate nicely over macos, other linux distribution, ci/cd environments and more. Additionally this will serve as first step in modularising the core that it'd benefit if in case it is ever needed to shift from systemd in the future (highly unlikely though, for now).",
   "created_at": "2024-06-22T05:20:20Z",
   "id": 2183810604,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-06-22T05:20:20Z",
   "user": {
    "id": 26714676,
    "login": "Animeshz",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/rfcsc-meeting-2024-06-24/47589/1\n",
   "created_at": "2024-06-24T15:46:56Z",
   "id": 2186884091,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-06-24T15:46:56Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/brainstorming-for-rfc-assimilate-home-manager-into-nixpkgs-monorepo/48230/14\n",
   "created_at": "2024-07-03T02:02:33Z",
   "id": 2204897411,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-07-03T02:02:33Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/rfcsc-meeting-2024-07-08/48678/1\n",
   "created_at": "2024-07-08T16:54:39Z",
   "id": 2214691197,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-07-08T16:54:39Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/disnix-vs-kubernetes/1902/2\n",
   "created_at": "2024-07-19T17:26:50Z",
   "id": 2239723544,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-07-19T17:26:50Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> To clarify, would this allow people to use init systems other than systemd?\r\n\r\nI believe we can be a bit more ambitious and write a distro in this portable service framework.",
   "created_at": "2024-07-20T03:47:27Z",
   "id": 2240898840,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 2,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 2
   },
   "updated_at": "2024-07-20T03:47:27Z",
   "user": {
    "id": 5954806,
    "login": "AndersonTorres",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Tangentially related, but we could also specify \"storage tiers\": ephemeral/persistent/essential/cache. That would make `impermanence` project also a first-class citizen. See also: https://github.com/nix-community/impermanence/issues/10#issuecomment-2198392482",
   "created_at": "2024-07-22T08:15:30Z",
   "id": 2242358787,
   "minimized": null,
   "reactions": {
    "+1": 5,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 5
   },
   "updated_at": "2024-07-22T08:15:30Z",
   "user": {
    "id": 66781795,
    "login": "KFearsoff",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/rfcsc-meeting-2024-08-05/50170/1\n",
   "created_at": "2024-08-05T15:37:46Z",
   "id": 2269366600,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-08-05T15:37:46Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> I believe we can be a bit more ambitious and write a distro in this portable service framework.\r\n\r\n@AndersonTorres sounds super fun to prototype - any plans personally to start something like that at some point?",
   "created_at": "2024-08-10T21:02:13Z",
   "id": 2282276872,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-08-10T21:02:13Z",
   "user": {
    "id": 7755101,
    "login": "aanderse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "RFCSC: There hasn't been much activity here in the last few months, has there been activity through other channels? If so, please provide an update here. \r\n\r\nOtherwise, are there any actions you can foresee to take this RFC towards conclusion? If not, feel free to draft this PR.\r\n\r\n/cc'ing shepherd team: @AndersonTorres (lead) @roberth @ehmry @Animeshz @IreneKnapp ",
   "created_at": "2024-08-19T15:24:34Z",
   "id": 2296846057,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-08-19T15:24:34Z",
   "user": {
    "id": 101816,
    "login": "asymmetric",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/rfcsc-meeting-2024-08-19/50831/1\n",
   "created_at": "2024-08-19T15:33:30Z",
   "id": 2296864901,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-08-19T15:33:30Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> > I believe we can be a bit more ambitious and write a distro in this portable service framework.\n> \n> @AndersonTorres sounds super fun to prototype - any plans personally to start something like that at some point?\n\nI have only an Emacs instance and a dream :)\n\nI feel that I need to study more things about Nixpkgs itself. But yes I want to try something, especially testing other init systems.",
   "created_at": "2024-08-19T21:47:27Z",
   "id": 2297515047,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 1,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 1
   },
   "updated_at": "2024-08-19T21:47:27Z",
   "user": {
    "id": 5954806,
    "login": "AndersonTorres",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "sounds cool - please ping me if you start hacking on something\r\n\r\ni am still trying to understand how some of these concepts practically work, though 🤔\r\n\r\nlet's take the example @roberth provided in https://github.com/NixOS/nixpkgs/pull/267111 - a pragmatic implementation of some ideas Eelco wrote about [a long time ago](https://gist.github.com/edolstra/29ce9d8ea399b703a7023073b0dbc00d#nixos-services) - at first glance the ability to write something like `services.foo = <module here>` looks great and saves us the security nightmare of a flakes world where we \"don't want the PostgreSQL module to have the ability to change your X11 configuration\"... but after i play that scenario out in my mind i realize nixos is so awesome because postgres can change my x11 configuration... well not specifically that example, but see any module of a web application which uses a database, redis, web server, etc... if we fully commit to essentially containers this works fine, but i am not sure we want that.\r\n\r\nanyone have any thoughts on that?",
   "created_at": "2024-08-19T22:47:18Z",
   "id": 2297641245,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 1,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 1
   },
   "updated_at": "2024-08-19T22:47:18Z",
   "user": {
    "id": 7755101,
    "login": "aanderse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> Tangentially related, but we could also specify \"storage tiers\": ephemeral/persistent/essential/cache. That would make `impermanence` project also a first-class citizen. See also: [nix-community/impermanence#10 (comment)](https://github.com/nix-community/impermanence/issues/10#issuecomment-2198392482)\r\n\r\nIMO for a first implementation this would be kind of feature creep and even more discouraging people to adapt this.\r\n\r\n> * emery is using nix-processmgmt with nixos already and is willing to help integrate some form of it in nixpkgs\r\n\r\nSome notes from me after looking a bit on a random file:\r\n\r\n- we could probably template this from an higher level option or straight from tmpfiles.d https://github.com/svanderburg/nix-processmgmt/blob/0e9ec77cc633907aa93bf3d758013f6f699f8842/examples/services-agnostic/constructors/nginx/default.nix#L14-L21\r\n- No idea about \"runlevels\", I am to young for them, https://github.com/svanderburg/nix-processmgmt/blob/0e9ec77cc633907aa93bf3d758013f6f699f8842/examples/services-agnostic/constructors/nginx/default.nix#L42 but in all examples they seem to be 3 4 5, so maybe we can just hardcode them?\r\n- having to handle pid files is very depending on the program. Most new ones don't have that concept anymore and also many do it wrong. We need to abstract that away https://github.com/svanderburg/nix-processmgmt/blob/0e9ec77cc633907aa93bf3d758013f6f699f8842/examples/services-agnostic/constructors/docker/default.nix#L16\r\n- the modules seem smaller because they lack many options and seem to prefer a function argument approach instead of using the module system. Also writters are not used and RFC42 also not.\r\n\r\n> at first glance the ability to write something like services.foo = <module here> looks great and saves us the security nightmare of a flakes world where we \"don't want the PostgreSQL module to have the ability to change your X11 configuration\"... but after i play that scenario out in my mind i realize nixos is so awesome because postgres can change my x11 configuration... well not specifically that example, but see any module of a web application which uses a database, redis, web server, etc... if we fully commit to essentially containers this works fine, but i am not sure we want that.\r\n\r\nYeah, the module system does not really have concept of security and retrofitting that will end up in usability nightmares.\r\nInstead of using higher level options people will then result to shell scripting to achieve their goals. I can't use tmpfiles.d? I might as well just use mkdir, a ln and hope for the best.\r\n\r\nAlso any service running with root can change many configuration files and data on the disk even when it can't change the module system. To prevent that we need stronger sandboxing which currently has a strong tie to systemd.\r\n\r\n----\r\n\r\nI think we would loose features like easy socket activation.\r\nAlso the service managers seem to lack functionality to implement advanced dependency systems like the acme module has, requiring people to probably fall back to polling directories via shell which is exactly where we do not want to go back. That module is also making good use of bind mounts.\r\nI am having a real hard time imagining the acme module in that new service layer abstraction without being a pain to work with and the abstraction being in your way.",
   "created_at": "2024-08-20T12:32:07Z",
   "id": 2298746262,
   "minimized": null,
   "reactions": {
    "+1": 1,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 1
   },
   "updated_at": "2024-08-26T11:40:40Z",
   "user": {
    "id": 7258858,
    "login": "SuperSandro2000",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "FWIW I'm waiting on the delivery of some hardware before I do an init overhaul down to pid1.\r\n\r\nI don't like how long this issue has been open without progress but this is an issue we can't ignore completely because it will continue to be brought up. I think init portability will be a bit like maintaining powerpc support in nixpkgs, we don't do it because many people ask for it, we do it because it demonstrates that our basic portability is sound.",
   "created_at": "2024-08-21T06:33:13Z",
   "id": 2301241274,
   "minimized": null,
   "reactions": {
    "+1": 3,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 3
   },
   "updated_at": "2024-08-21T06:33:13Z",
   "user": {
    "id": 10137,
    "login": "ghost",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> I don't like how long this issue has been open without progress but this is an issue we can't ignore completely because it will continue to be brought up.\r\n\r\nyes, what i'm noticing here is much of this thread seems to be less about drafting the rfc per se, and more about the broad issue of abstracting service management, which, hey, fair enough, it's something people evidently care about.\r\n\r\nat the risk of perpetuating the trend,\r\n\r\nhttps://github.com/NixOS/rfcs/blob/3798ca1a0946f3fd745b6765c133838b65ae1960/rfcs/0163-portable-service-layer.md?plain=1#L433-L436\r\n\r\nit's worth noting that *systemd* has no high-level concept of dependencies. this bit of docs:\r\n\r\n    Note that transactions are generated independently of a unit's\r\n    state at runtime, hence, for example, if a start job is requested on an\r\n    already started unit, it will still generate a transaction and wake up any\r\n    inactive dependencies (and cause propagation of other jobs as per the\r\n    defined relationships). This is because the enqueued job is at the time of\r\n    execution compared to the target unit's state and is marked successful and\r\n    complete when both satisfy. However, this job also pulls in other\r\n    dependencies due to the defined relationships and thus leads to, in our\r\n    our example, start jobs for any of those inactive units getting queued as\r\n    well.\r\n\r\nserves to illustrate that transactions are related to units by suggestion, nearly by accident. by similar accident, and the collective input of everyone who converged on systemd as a *platform* (or had it converge upon them), polishing the happy path 99% of users tread, this is usually unnoticeable.\r\n\r\ndespite nixos's occasional moments butting heads with systemd's concepts (https://github.com/NixOS/nixpkgs/issues/135557, https://github.com/NixOS/nixpkgs/issues/81138, etc.), we largely get by fine even when straining systemd's expectations, thanks to the hermetic nature (closure bloat aside) of nix packaging, the ability to sprinkle tests anywhere in evaluation, generation rollbacks as deep down as the grub menu, etc.\r\n\r\nhttps://github.com/NixOS/rfcs/blob/3798ca1a0946f3fd745b6765c133838b65ae1960/rfcs/0163-portable-service-layer.md?plain=1#L428-L431\r\n\r\nhttps://github.com/NixOS/rfcs/blob/3798ca1a0946f3fd745b6765c133838b65ae1960/rfcs/0163-portable-service-layer.md?plain=1#L244-L246\r\n\r\non the complete opposite end, sysvinit offers no more runtime guarantees than any other shell scripts in a trenchcoat, but yes, a boot sequence for it is a fairly trivial function of a partial ordering expressed by dependencies.\r\n\r\nso for any sufficiently nice, general definition of a \"service\" that expresses dependencies and so on: ...accordingly nice, well-designed target platforms to compile down to—that can actually take advantage of all that generality—are thin on the ground. \r\n\r\nat that rate, [services.kubernetes], [virtualisation.oci-containers], and friends join the list of \"prior art\", which raises a point: many objections in this thread are about portability efforts adding burden to maintaining plain old systemd services. this initiative has the potential to be more qol than bother for nixos proper, though:\r\n\r\n1. right now, if you want to move your `systemd.services` a level above bare metal without rewriting them, your (in-tree) options are the native systemd-nspawn container module, or building a vm image. some nixos users would be happy to see that list grow.\r\n2. singleton services, as mentioned, are a pain. putting in my vote for the humble module-level library to aid turning current singleton services instanced, as some have drafted here. such a library would necessarily implement a number of ideas in this rfc. done right it could go over just as uncontroversially as the existence of `lib.mkEnableOption`.\r\n3. further down the line, systemd tmpfiles, for instance, could be abstracted out in places, as i've seen those abused to generally ensure the presence of certain directories, where [impermanence] as mentioned, and, more generally, reifying storage lifetimes, seems more prudent.\r\n\r\nwhat i'm getting at is that after sufficiently many tangentially-related baby steps that make nixos nicer in general, huge swaths of a portable service layer, if not the whole thing, have a chance to fall out for free.\r\n\r\nso, if the strongest intersection of opinion is just that having a portable service layer would be Nice™, i could see this rfc getting merged with most of the code samples omitted, as a largely conceptual/advisory piece, to take note of how conducive future nixos changes are to the overall goal, and once a specific implementation feels close at hand, a subsequent rfc detailing it supersedes this one.\r\n\r\n_ps._\r\n\r\nhttps://github.com/NixOS/rfcs/blob/3798ca1a0946f3fd745b6765c133838b65ae1960/rfcs/0163-portable-service-layer.md?plain=1#L862-L865\r\n\r\nfwiw, home-manager gets by, for the time being, independently specifying launchd and systemd units for a service definition on a can-be-bothered basis, ex. https://github.com/nix-community/home-manager/blob/c2cd2a52e02f1dfa1c88f95abeb89298d46023be/modules/services/nix-gc.nix#L108-L149\r\n\r\nthing is, home-manager services tend to be comparatively low-stakes, to the point that taking on an additional channel or flake input for the sake of a user-level service doesn't feel worth it—take it from someone who's running out of memorable 4-digit ports on localhost courtesy solely of nix-darwin.\r\n\r\nthere is a big difference between i3bar/godoc and nginx/postgres.\r\n\r\n[services.kubernetes]: https://nixos.org/manual/nixos/stable/options#opt-services.kubernetes.roles\r\n[virtualisation.oci-containers]: https://nixos.org/manual/nixos/stable/options#opt-virtualisation.oci-containers.containers\r\n[impermanence]: https://github.com/nix-community/impermanence",
   "created_at": "2024-08-26T19:07:21Z",
   "id": 2310882775,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-08-26T19:07:21Z",
   "user": {
    "id": 9125590,
    "login": "ylh",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/rfcsc-meeting-2024-09-02/51514/1\n",
   "created_at": "2024-09-02T15:21:11Z",
   "id": 2324976529,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-09-02T15:21:11Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/how-to-deploy-a-service-on-a-non-nixos/51597/2\n",
   "created_at": "2024-09-04T02:40:36Z",
   "id": 2327804066,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-09-04T02:40:36Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/brainstorm-for-rfc-assimilate-home-manager-into-nixpkgs-monorepo/48230/91\n",
   "created_at": "2024-09-11T01:37:38Z",
   "id": 2342444886,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-09-11T01:37:38Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/rfcsc-meeting-2024-09-16/52224/1\n",
   "created_at": "2024-09-16T15:31:01Z",
   "id": 2353249079,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-09-16T15:31:01Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> FWIW I'm waiting on the delivery of some hardware before I do an init overhaul down to pid1.\r\n\r\nAny news on this,  @ehmry ?",
   "created_at": "2024-09-17T01:36:29Z",
   "id": 2354334192,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-09-17T01:36:29Z",
   "user": {
    "id": 5954806,
    "login": "AndersonTorres",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "@AndersonTorres I've applied for funding and I'm blocked waiting for a response.",
   "created_at": "2024-09-30T08:21:49Z",
   "id": 2382430799,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 7,
    "total_count": 7
   },
   "updated_at": "2024-09-30T08:21:49Z",
   "user": {
    "id": 10137,
    "login": "ghost",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/rfcsc-meeting-2024-09-30/53690/1\n",
   "created_at": "2024-09-30T15:28:34Z",
   "id": 2383526413,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-09-30T15:28:34Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/build-docker-images-derived-from-nixos-module/53747/6\n",
   "created_at": "2024-10-03T08:34:06Z",
   "id": 2390834482,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-10-03T08:34:06Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/rfcsc-meeting-2024-10-28/55095/1\n",
   "created_at": "2024-10-28T15:25:16Z",
   "id": 2441903710,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-10-28T15:25:16Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/rfcsc-meeting-2024-11-11/55888/1\n",
   "created_at": "2024-11-11T16:15:20Z",
   "id": 2468544923,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-11-11T16:15:20Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "@roberth, @AndersonTorres, @ehmry, @Animeshz, @IreneKnapp: RFCSC: Could you please provide an update on what is blocking this RFC from moving forward?\r\n\r\n@svanderburg please update the shepherd team in the RFC's body, as suggested [here](https://github.com/NixOS/rfcs/pull/163/files#r1426607982).",
   "created_at": "2024-11-25T19:49:12Z",
   "id": 2498898180,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-11-25T19:49:12Z",
   "user": {
    "id": 101816,
    "login": "asymmetric",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/rfcsc-meeting-2024-11-25/56591/1\n",
   "created_at": "2024-11-25T19:56:52Z",
   "id": 2498911733,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-11-25T19:56:52Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/rfcsc-meeting-2024-12-09/57636/1\n",
   "created_at": "2024-12-19T16:12:24Z",
   "id": 2554858750,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2024-12-19T16:12:24Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "RFCSC:\r\n\r\nThere seem to have been no significant updates to this RFC for a while. Please provide an update, otherwise we will close it. Note that it can always be reopened if there is time and interest to work on it again.",
   "created_at": "2025-01-06T16:16:13Z",
   "id": 2573440663,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2025-01-06T16:16:13Z",
   "user": {
    "id": 2597803,
    "login": "MMesch",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I still hope that NixBSD can revive this as living, breathing, example of what we want on  multiple axes --- but I leave actually doing that that to @artemist and @rhelmot.",
   "created_at": "2025-01-06T16:21:24Z",
   "id": 2573450071,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2025-01-06T16:21:24Z",
   "user": {
    "id": 1055245,
    "login": "Ericson2314",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/lastmodified-attribute-mismatch-in-input-lastmodified-0/58395/1\n",
   "created_at": "2025-01-06T16:46:14Z",
   "id": 2573496337,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2025-01-06T16:46:14Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/rfcsc-meeting-2025-01-06/58396/1\n",
   "created_at": "2025-01-06T16:48:13Z",
   "id": 2573500486,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2025-01-06T16:48:13Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I'd like to start an alternate implementation by simplifying the proof of concept I wrote earlier and growing it collaboratively from there. That should avoid our collective analysis paralysis, while providing the necessary input for making a decision later, without turning Portable Service Layer into policy.\r\n\r\nBesides that, I believe that this RFC text approaches the problem from the wrong angle, and the original author has disengaged through their virtually complete lack of communication.\r\nWith those issues in mind, I don't see the value in keeping this particular RFC open, but I'd be glad to be corrected about that.\r\n(This thread could still be used for posting updates to those who are interested)",
   "created_at": "2025-01-06T17:14:00Z",
   "id": 2573549371,
   "minimized": null,
   "reactions": {
    "+1": 11,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 11
   },
   "updated_at": "2025-01-06T17:14:00Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/pre-rfc-decouple-services-using-structured-typing/58257/10\n",
   "created_at": "2025-01-08T11:55:22Z",
   "id": 2577491855,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2025-01-08T11:55:22Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "- https://github.com/NixOS/nixpkgs/pull/372170",
   "created_at": "2025-01-08T15:35:20Z",
   "id": 2577975635,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 3,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 2,
    "total_count": 5
   },
   "updated_at": "2025-01-08T15:35:20Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I have not been following the discussion here very closely, but I think this project could be of relevance to the discussion: https://codeberg.org/amjoseph/infuse.nix",
   "created_at": "2025-01-14T11:35:08Z",
   "id": 2589682211,
   "minimized": null,
   "reactions": {
    "+1": 2,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 2
   },
   "updated_at": "2025-01-14T11:35:19Z",
   "user": {
    "id": 48366877,
    "login": "jzbor",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "RFCSC: Without activity by the original author and with the general consensus expressed by @roberth's comment, we're now closing this RFC. Feel free to open a new RFC if necessary.",
   "created_at": "2025-01-20T16:14:38Z",
   "id": 2602813748,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2025-01-20T16:14:38Z",
   "user": {
    "id": 20525370,
    "login": "infinisil",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/rfcsc-meeting-2025-01-20/59113/1\n",
   "created_at": "2025-01-20T16:36:28Z",
   "id": 2602858496,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2025-01-20T16:36:28Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Note that Modular Services (as proposed by @roberth [above](https://github.com/NixOS/rfcs/pull/163#issuecomment-2573549371)) has finally landed!\r\n\r\n- Tracking issue: https://github.com/NixOS/nixpkgs/issues/428084\r\n- Docs as of `nixos-unstable`: https://nixos.org/manual/nixos/unstable/#modular-services",
   "created_at": "2025-08-22T15:09:53Z",
   "id": 3214718405,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 5,
    "hooray": 7,
    "laugh": 0,
    "rocket": 0,
    "total_count": 12
   },
   "updated_at": "2025-08-22T15:09:53Z",
   "user": {
    "id": 1055245,
    "login": "Ericson2314",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/different-ways-to-install-firefox/74466/11\n",
   "created_at": "2026-01-20T11:36:31Z",
   "id": 3772433929,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2026-01-20T11:36:31Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This pull request has been mentioned on **NixOS Discourse**. There might be relevant details there:\n\nhttps://discourse.nixos.org/t/nixifying-kubernetes-with-nix-csi-easykubenix-and-dinix/70899/33\n",
   "created_at": "2026-02-24T00:50:55Z",
   "id": 3948228371,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2026-02-24T00:50:55Z",
   "user": {
    "id": 45043797,
    "login": "nixos-discourse",
    "site_admin": false,
    "type": "User"
   }
  }
 ],
 "html_url": "https://github.com/NixOS/rfcs/pull/163",
 "issue": {
  "active_lock_reason": null,
  "assignee": null,
  "assignees": [],
  "body": "This RFC proposes a generic, portable service layer that makes it possible to manage services on top of (in theory) any operating system that is capable of running the Nix package manager.\r\n\r\n[Rendered](https://github.com/svanderburg/rfcs/blob/portable-service-layer/rfcs/0163-portable-service-layer.md)\r\n",
  "closed_at": "2025-01-20T16:14:38Z",
  "closed_by": {
   "id": 20525370,
   "login": "infinisil",
   "site_admin": false,
   "type": "User"
  },
  "comments": 130,
  "created_at": "2023-10-05T18:36:54Z",
  "draft": false,
  "id": 1928845284,
  "issue_field_values": [],
  "labels": [],
  "locked": false,
  "milestone": null,
  "number": 163,
  "pull_request": {
   "diff_url": "https://github.com/NixOS/rfcs/pull/163.diff",
   "merged_at": null,
   "patch_url": "https://github.com/NixOS/rfcs/pull/163.patch"
  },
  "reactions": {
   "+1": 31,
   "-1": 3,
   "confused": 0,
   "eyes": 4,
   "heart": 63,
   "hooray": 0,
   "laugh": 0,
   "rocket": 19,
   "total_count": 120
  },
  "repository_url": "https://api.github.com/repos/NixOS/rfcs",
  "state": "closed",
  "state_reason": null,
  "title": "[RFC 0163] Portable Service Layer",
  "type": null,
  "updated_at": "2026-02-24T00:50:55Z",
  "user": {
   "id": 1153271,
   "login": "svanderburg",
   "site_admin": false,
   "type": "User"
  }
 },
 "kind": "pull_request",
 "pull_request": {
  "active_lock_reason": null,
  "additions": 925,
  "assignee": null,
  "assignees": [],
  "auto_merge": null,
  "base": {
   "label": "NixOS:master",
   "ref": "master",
   "repo": {
    "archived": false,
    "default_branch": "master",
    "fork": false,
    "full_name": "NixOS/rfcs",
    "id": 81662501,
    "private": false
   },
   "sha": "c8569f6719356009204133cd00d92010889ed56d",
   "user": {
    "id": 487568,
    "login": "NixOS",
    "site_admin": false,
    "type": "Organization"
   }
  },
  "body": "This RFC proposes a generic, portable service layer that makes it possible to manage services on top of (in theory) any operating system that is capable of running the Nix package manager.\r\n\r\n[Rendered](https://github.com/svanderburg/rfcs/blob/portable-service-layer/rfcs/0163-portable-service-layer.md)\r\n",
  "changed_files": 1,
  "closed_at": "2025-01-20T16:14:38Z",
  "comments": 130,
  "commits": 3,
  "created_at": "2023-10-05T18:36:54Z",
  "deletions": 0,
  "diff_url": "https://github.com/NixOS/rfcs/pull/163.diff",
  "draft": false,
  "head": {
   "label": "svanderburg:portable-service-layer",
   "ref": "portable-service-layer",
   "repo": {
    "archived": false,
    "default_branch": "master",
    "fork": true,
    "full_name": "svanderburg/rfcs",
    "id": 699412131,
    "private": false
   },
   "sha": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "user": {
    "id": 1153271,
    "login": "svanderburg",
    "site_admin": false,
    "type": "User"
   }
  },
  "id": 1544175156,
  "labels": [],
  "locked": false,
  "maintainer_can_modify": false,
  "merged": false,
  "merged_at": null,
  "merged_by": null,
  "milestone": null,
  "number": 163,
  "patch_url": "https://github.com/NixOS/rfcs/pull/163.patch",
  "requested_reviewers": [],
  "requested_teams": [],
  "review_comments": 64,
  "state": "closed",
  "title": "[RFC 0163] Portable Service Layer",
  "updated_at": "2026-02-24T00:50:55Z",
  "user": {
   "id": 1153271,
   "login": "svanderburg",
   "site_admin": false,
   "type": "User"
  }
 },
 "ref": "NixOS/rfcs#163",
 "review_comments": [
  {
   "body": "I talked to you at Nixcon about exactly this, where I even created [a gist](https://gist.github.com/infinisil/e29ee58a8184eeb7a2e139c2b2d3e4ff) for you to demonstrate how the module system can be used to support instances of services. The module system is slowly becoming the dominant way to write composable and modular Nix code, and I really think it should be used for this too. This will make integration into NixOS easier too.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-05T19:03:05Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.",
   "id": 1347854623,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 486,
   "original_position": 486,
   "original_start_line": 482,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1660524954,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 20,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 20
   },
   "side": "RIGHT",
   "start_line": 482,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-05T19:10:27Z",
   "user": {
    "id": 20525370,
    "login": "infinisil",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I'm surprised for you to not mention your own [Disnix](https://github.com/svanderburg/disnix)/[Dysnomia](https://github.com/svanderburg/dysnomia) projects. It's clear that that's where this RFC comes from.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-05T19:05:23Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.\n+\n+For many kinds of services (e.g. PostgreSQL, Apache HTTP server etc), it\n+typically suffices to only have a single instance running on a machine. For\n+microservices, it may actually be quite useful to have the ability to easily\n+create multiple instances. For testing/experimentation purposes it may also be\n+desired to run multiple instances of a system service, such as a web server.\n+\n+It is also not easily possible in NixOS to switch to alternative implementations\n+of a service. For many services, alternative implementations (e.g. `cron`,\n+`ssh`) are not required, but some services accept complex configurations, such\n+as the Apache HTTP server and Apache Tomcat.\n+\n+Finding a universal abstraction in NixOS to facilitate all possible use cases\n+may be impossible. Moreover, it may also be desired for developers to write\n+their own abstractions on top of existing functionality.\n+\n+To support abstractions and multiple instances, the Nix process management\n+framework does not use the NixOS module system, but a convention that is based\n+on the organisation of \"ordinary\" Nix packages in the Nixpkgs repository.\n+\n+In Nixpkgs, most package follow the convention that a separate Nix file defines\n+a function in which the function header refers to the build-time dependencies to\n+construct a package from source. Packages are composed in the top-level Nix\n+expression (`all-packages.nix`) that invokes these functions with the required\n+build-time parameters. For most packages, only a single variant is composed, but\n+it is also possible to build different kinds of package variants by changing the\n+function parameters.\n+\n+The Nix process management framework follows the same principles and extends the\n+Nixpkgs convention with a number of additional concepts:\n+\n+* Every service is instantiated from a *constructor* which is a nested function:\n+  the outer function refers to the build-time dependencies that are required to\n+  build the service from source code.\n+* The inner function refers to the service *instance parameters* -- calling the\n+  constructor with different parameters makes it possible to compose a service\n+  with an alternative configuration and in such a way that they do not conflict\n+  with other instances.\n+* The build-time dependencies of constructor functions are composed in a Nix\n+  expression that is typically called: `constructors.nix` in which every\n+  constructor function is called with its required build-time dependency\n+  parameters.\n+* The process instances of a system are composed in a Nix expression that is\n+  typically called: `processes.nix` in which every constructor function is\n+  called with its required instance parameters.\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    chown ${user}:${group} ${socketDir}\n+    chown ${user}:${group} ${dataDir}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        su ${user} -c '${postgresql}/bin/initdb -D ${dataDir} --no-locale'\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a constructor function for PostgreSQL and has the\n+following properties:\n+\n+* The outer function-header (first line) refer to the build inputs of the\n+  service. For example, to construct a configuration from a high-level\n+  specification for any supported process manager, we propagate the\n+  `createManagedProcess` as a parameter.\n+* The inner-function header refers to the instance parameters of a PostgreSQL\n+  instance. When it is desired to have two instances deployed to the same\n+  machine we must make sure that there is no conflict between the state of\n+  the two instances. Avoiding a conflict is possible by assigning a unique TCP\n+  port number and making sure that the state directories do not conflict. The\n+  `instanceName` parameter is appended to the name of the state directories to\n+  make this possible.\n+* In the body of the inner function, we invoke `createManagedProcess` to\n+  agnostically generate a configuration for a supported process manager.\n+  In the configuration, we provide an initialization script and we call the\n+  `postgres` service with its required command-line parameters and environment\n+  variables.\n+\n+As explained earlier, a constructor function is a nested function that we need to compose\n+twice. The build-time dependencies are composed in a Nix expression typically called\n+`constructors.nix`:\n+\n+```nix\n+{ nix-processmgmt ? ../../nix-processmgmt\n+, pkgs\n+, stateDir\n+, logDir\n+, runtimeDir\n+, cacheDir\n+, spoolDir\n+, libDir\n+, tmpDir\n+, callingUser ? null\n+, callingGroup ? null\n+, processManager\n+, ids ? {}\n+}:\n+\n+let\n+  createManagedProcess = import \"${nix-processmgmt}/nixproc/create-managed-process/universal/create-managed-process-universal.nix\" {\n+    inherit pkgs runtimeDir stateDir logDir tmpDir processManager ids;\n+  };\n+in\n+{\n+  postgresql = import ./postgresql {\n+    inherit createManagedProcess stateDir runtimeDir;\n+    inherit (pkgs) lib postgresql su;\n+  };\n+}\n+```\n+\n+In the above Nix expression, we invoke the `postgresql` constructor function and\n+we propagated its required build-time dependencies, such as the `postgresql`\n+package and the `createManagedProcess` abstraction function.\n+\n+The instance parameters are composed in a Nix expression that is typically called\n+`processes.nix`:\n+\n+```nix\n+{ pkgs ? import <nixpkgs> { inherit system; }\n+, system ? builtins.currentSystem\n+, stateDir ? \"/var\"\n+, runtimeDir ? \"${stateDir}/run\"\n+, logDir ? \"${stateDir}/log\"\n+, spoolDir ? \"${stateDir}/spool\"\n+, cacheDir ? \"${stateDir}/cache\"\n+, libDir ? \"${stateDir}/lib\"\n+, tmpDir ? (if stateDir == \"/var\" then \"/tmp\" else \"${stateDir}/tmp\")\n+, processManager\n+, nix-processmgmt ? ../../../nix-processmgmt\n+}:\n+\n+let\n+  constructors = import ../../services-agnostic/constructors.nix {\n+    inherit pkgs stateDir runtimeDir logDir tmpDir cacheDir spoolDir libDir processManager nix-processmgmt;\n+  };\n+in\n+rec {\n+  postgresql = rec {\n+    port = 5432;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+    };\n+  };\n+\n+  postgresql-secondary = rec {\n+    port = 5433;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+      instanceSuffix = \"-secondary\";\n+    };\n+  };\n+}\n+```\n+\n+In the above Nix expression, we compose two PostgreSQL instances that run on the\n+same machine simultaneously. This is possible due to the fact that they use\n+instance parameters that avoid their states to conflict.\n+\n+The above expression imports the `constructors.nix` expression so that each\n+process instance can be composed from the PostgreSQL constructor function.\n+\n+In NixOS, we use the NixOS module system that makes it possible to define\n+multiple aspects of a system in a module (such as packages, config files,\n+systemd units and environment variables) and combine the aspects of each module\n+into a single configuration from which a complete system is deployed, which is\n+very powerful.\n+\n+Combining the conventions of the nix-processmgmt framework with the NixOS module\n+system is something that still needs to be discussed. There are variety of\n+options possible, such as:\n+\n+* Combining the convention with process instances as described earlier by\n+  referring to a process instance from a NixOS module, similar to how NixOS\n+  modules can also refer to ordinary Nix packages\n+* Using sub modules, in which every sub module can define its own variant of a\n+  service.\n+\n+### Unprivileged user deployments\n+\n+Another interesting feature of Nix is to allow unprivileged users to deploy\n+packages. With services, this is also possible, but there are some things we\n+need to take into account, such as:\n+\n+* The state directories must refer to a directory that has writable permissions\n+  for the user\n+* We cannot bind to a TCP/UDP port below 1024\n+\n+We can also introduce a configuration flag and configure a service in such a way\n+that the above criteria are met:\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir, forceDisableUserChange}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    ${lib.optionalString (!forceDisableUserChange) ''\n+      chown ${user}:${group} ${socketDir}\n+      chown ${user}:${group} ${dataDir}\n+    ''}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        ${lib.optionalString (!forceDisableUserChange) \"su ${user} -c '\"}${postgresql}/bin/initdb -D ${dataDir} --no-locale${lib.optionalString (!forceDisableUserChange) \"'\"}\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a revised PostgreSQL constructor function that\n+accepts a new parameter: `forceDisableUserChange`. When this property is\n+enabled, the service configured in such a way that it does not change users,\n+which an unprivileged user is not allowed to do. With this setting enabled, it\n+becomes possible for an unprivileged user to deploy PostgreSQL.\n+\n+# Examples and Interactions\n+[examples-and-interactions]: #examples-and-interactions\n+\n+The `processes.nix` expression shown earlier can be used with a variety of tools\n+to deploy running process instances.\n+\n+The following command deploys the configurations as `sysvinit` scripts and\n+activates the configuration automatically:\n+\n+```bash\n+$ nixproc-sysvinit-switch processes.nix\n+```\n+\n+We can deploy the same configuration as systemd units with the following command:\n+\n+```bash\n+$ nixproc-systemd-switch processes.nix\n+```\n+\n+Or with `supervisord` with the following command:\n+\n+```bash\n+$ nixproc-supervisord-switch processes.nix\n+```\n+\n+As an unprivileged user, we can use the `--state-dir` parameter to change the\n+prefix of the state directories to reside in a user's home directory\n+(`/home/sander/var`) and disable user changing:\n+\n+```bash\n+$ nixproc-sysvinit-switch --state-dir /home/sander/var --force-disable-user-change processes.nix\n+```\n+\n+# Drawbacks\n+[drawbacks]: #drawbacks\n+\n+The ideas describes in this RFC makes the Nixpkgs eco-system more powerful, but\n+these new features come at at a price:\n+\n+* Using high-level abstractions hide the low-level configuration details of\n+  specific process managers. This makes it sometimes more difficult to reason\n+  about what is happening in problematic cases\n+* More testing is required -- we need to test services in multiple deployment\n+  scenarios, which is more expensive. Fortunately, I have seen that testing for\n+  two kinds of targets often give enough certainty: if you test with a\n+  foreground process (e.g. `systemd`) and a daemon (e.g. `sysvinit`) there is a\n+  big chance that it will work with other process managers as well.\n+* You may target the lowest common denominator. This is not actually true,\n+  because with overrides it is still possible to use any feature of a process\n+  manager. Moreover, not all services need to be portable -- low-level services\n+  such as `cups` and `syslogd` can still be packaged in a process\n+  manager-specific way.\n+\n+# Alternatives\n+[alternatives]: #alternatives\n+\n+This RFC proposes a new strategy to allow multiple process instances of the\n+same service to co-exist. Co-existence is possible by avoiding conflicts --\n+making sure that state directories and port numbers do not conflict through\n+function parameters.\n+\n+It is also possible to isolate services by using concepts such as Linux\n+namespaces. Then it is not required to avoid certain kinds of conflicts.\n+Unfortunately, using these features is restricted to Linux only -- this RFC aims\n+to be operating system agnostic.\n+\n+# Prior art\n+[prior-art]: #prior-art",
   "id": 1347856747,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 849,
   "original_position": 849,
   "original_start_line": null,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1660524954,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": null,
   "start_side": null,
   "subject_type": "line",
   "updated_at": "2023-10-05T19:10:27Z",
   "user": {
    "id": 20525370,
    "login": "infinisil",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I am in full support of this general idea. However this RFC feels just like a blog post as is, it needs to be much more concrete about how this idea should be practically implemented. It's also very long and could be trimmed down a lot by removing implementation details.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-05T19:08:04Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.\n+\n+For many kinds of services (e.g. PostgreSQL, Apache HTTP server etc), it\n+typically suffices to only have a single instance running on a machine. For\n+microservices, it may actually be quite useful to have the ability to easily\n+create multiple instances. For testing/experimentation purposes it may also be\n+desired to run multiple instances of a system service, such as a web server.\n+\n+It is also not easily possible in NixOS to switch to alternative implementations\n+of a service. For many services, alternative implementations (e.g. `cron`,\n+`ssh`) are not required, but some services accept complex configurations, such\n+as the Apache HTTP server and Apache Tomcat.\n+\n+Finding a universal abstraction in NixOS to facilitate all possible use cases\n+may be impossible. Moreover, it may also be desired for developers to write\n+their own abstractions on top of existing functionality.\n+\n+To support abstractions and multiple instances, the Nix process management\n+framework does not use the NixOS module system, but a convention that is based\n+on the organisation of \"ordinary\" Nix packages in the Nixpkgs repository.\n+\n+In Nixpkgs, most package follow the convention that a separate Nix file defines\n+a function in which the function header refers to the build-time dependencies to\n+construct a package from source. Packages are composed in the top-level Nix\n+expression (`all-packages.nix`) that invokes these functions with the required\n+build-time parameters. For most packages, only a single variant is composed, but\n+it is also possible to build different kinds of package variants by changing the\n+function parameters.\n+\n+The Nix process management framework follows the same principles and extends the\n+Nixpkgs convention with a number of additional concepts:\n+\n+* Every service is instantiated from a *constructor* which is a nested function:\n+  the outer function refers to the build-time dependencies that are required to\n+  build the service from source code.\n+* The inner function refers to the service *instance parameters* -- calling the\n+  constructor with different parameters makes it possible to compose a service\n+  with an alternative configuration and in such a way that they do not conflict\n+  with other instances.\n+* The build-time dependencies of constructor functions are composed in a Nix\n+  expression that is typically called: `constructors.nix` in which every\n+  constructor function is called with its required build-time dependency\n+  parameters.\n+* The process instances of a system are composed in a Nix expression that is\n+  typically called: `processes.nix` in which every constructor function is\n+  called with its required instance parameters.\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    chown ${user}:${group} ${socketDir}\n+    chown ${user}:${group} ${dataDir}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        su ${user} -c '${postgresql}/bin/initdb -D ${dataDir} --no-locale'\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a constructor function for PostgreSQL and has the\n+following properties:\n+\n+* The outer function-header (first line) refer to the build inputs of the\n+  service. For example, to construct a configuration from a high-level\n+  specification for any supported process manager, we propagate the\n+  `createManagedProcess` as a parameter.\n+* The inner-function header refers to the instance parameters of a PostgreSQL\n+  instance. When it is desired to have two instances deployed to the same\n+  machine we must make sure that there is no conflict between the state of\n+  the two instances. Avoiding a conflict is possible by assigning a unique TCP\n+  port number and making sure that the state directories do not conflict. The\n+  `instanceName` parameter is appended to the name of the state directories to\n+  make this possible.\n+* In the body of the inner function, we invoke `createManagedProcess` to\n+  agnostically generate a configuration for a supported process manager.\n+  In the configuration, we provide an initialization script and we call the\n+  `postgres` service with its required command-line parameters and environment\n+  variables.\n+\n+As explained earlier, a constructor function is a nested function that we need to compose\n+twice. The build-time dependencies are composed in a Nix expression typically called\n+`constructors.nix`:\n+\n+```nix\n+{ nix-processmgmt ? ../../nix-processmgmt\n+, pkgs\n+, stateDir\n+, logDir\n+, runtimeDir\n+, cacheDir\n+, spoolDir\n+, libDir\n+, tmpDir\n+, callingUser ? null\n+, callingGroup ? null\n+, processManager\n+, ids ? {}\n+}:\n+\n+let\n+  createManagedProcess = import \"${nix-processmgmt}/nixproc/create-managed-process/universal/create-managed-process-universal.nix\" {\n+    inherit pkgs runtimeDir stateDir logDir tmpDir processManager ids;\n+  };\n+in\n+{\n+  postgresql = import ./postgresql {\n+    inherit createManagedProcess stateDir runtimeDir;\n+    inherit (pkgs) lib postgresql su;\n+  };\n+}\n+```\n+\n+In the above Nix expression, we invoke the `postgresql` constructor function and\n+we propagated its required build-time dependencies, such as the `postgresql`\n+package and the `createManagedProcess` abstraction function.\n+\n+The instance parameters are composed in a Nix expression that is typically called\n+`processes.nix`:\n+\n+```nix\n+{ pkgs ? import <nixpkgs> { inherit system; }\n+, system ? builtins.currentSystem\n+, stateDir ? \"/var\"\n+, runtimeDir ? \"${stateDir}/run\"\n+, logDir ? \"${stateDir}/log\"\n+, spoolDir ? \"${stateDir}/spool\"\n+, cacheDir ? \"${stateDir}/cache\"\n+, libDir ? \"${stateDir}/lib\"\n+, tmpDir ? (if stateDir == \"/var\" then \"/tmp\" else \"${stateDir}/tmp\")\n+, processManager\n+, nix-processmgmt ? ../../../nix-processmgmt\n+}:\n+\n+let\n+  constructors = import ../../services-agnostic/constructors.nix {\n+    inherit pkgs stateDir runtimeDir logDir tmpDir cacheDir spoolDir libDir processManager nix-processmgmt;\n+  };\n+in\n+rec {\n+  postgresql = rec {\n+    port = 5432;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+    };\n+  };\n+\n+  postgresql-secondary = rec {\n+    port = 5433;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+      instanceSuffix = \"-secondary\";\n+    };\n+  };\n+}\n+```\n+\n+In the above Nix expression, we compose two PostgreSQL instances that run on the\n+same machine simultaneously. This is possible due to the fact that they use\n+instance parameters that avoid their states to conflict.\n+\n+The above expression imports the `constructors.nix` expression so that each\n+process instance can be composed from the PostgreSQL constructor function.\n+\n+In NixOS, we use the NixOS module system that makes it possible to define\n+multiple aspects of a system in a module (such as packages, config files,\n+systemd units and environment variables) and combine the aspects of each module\n+into a single configuration from which a complete system is deployed, which is\n+very powerful.\n+\n+Combining the conventions of the nix-processmgmt framework with the NixOS module\n+system is something that still needs to be discussed. There are variety of\n+options possible, such as:\n+\n+* Combining the convention with process instances as described earlier by\n+  referring to a process instance from a NixOS module, similar to how NixOS\n+  modules can also refer to ordinary Nix packages\n+* Using sub modules, in which every sub module can define its own variant of a\n+  service.\n+\n+### Unprivileged user deployments\n+\n+Another interesting feature of Nix is to allow unprivileged users to deploy\n+packages. With services, this is also possible, but there are some things we\n+need to take into account, such as:\n+\n+* The state directories must refer to a directory that has writable permissions\n+  for the user\n+* We cannot bind to a TCP/UDP port below 1024\n+\n+We can also introduce a configuration flag and configure a service in such a way\n+that the above criteria are met:\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir, forceDisableUserChange}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    ${lib.optionalString (!forceDisableUserChange) ''\n+      chown ${user}:${group} ${socketDir}\n+      chown ${user}:${group} ${dataDir}\n+    ''}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        ${lib.optionalString (!forceDisableUserChange) \"su ${user} -c '\"}${postgresql}/bin/initdb -D ${dataDir} --no-locale${lib.optionalString (!forceDisableUserChange) \"'\"}\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a revised PostgreSQL constructor function that\n+accepts a new parameter: `forceDisableUserChange`. When this property is\n+enabled, the service configured in such a way that it does not change users,\n+which an unprivileged user is not allowed to do. With this setting enabled, it\n+becomes possible for an unprivileged user to deploy PostgreSQL.\n+\n+# Examples and Interactions\n+[examples-and-interactions]: #examples-and-interactions\n+\n+The `processes.nix` expression shown earlier can be used with a variety of tools\n+to deploy running process instances.\n+\n+The following command deploys the configurations as `sysvinit` scripts and\n+activates the configuration automatically:\n+\n+```bash\n+$ nixproc-sysvinit-switch processes.nix\n+```\n+\n+We can deploy the same configuration as systemd units with the following command:\n+\n+```bash\n+$ nixproc-systemd-switch processes.nix\n+```\n+\n+Or with `supervisord` with the following command:\n+\n+```bash\n+$ nixproc-supervisord-switch processes.nix\n+```\n+\n+As an unprivileged user, we can use the `--state-dir` parameter to change the\n+prefix of the state directories to reside in a user's home directory\n+(`/home/sander/var`) and disable user changing:\n+\n+```bash\n+$ nixproc-sysvinit-switch --state-dir /home/sander/var --force-disable-user-change processes.nix\n+```\n+\n+# Drawbacks\n+[drawbacks]: #drawbacks\n+\n+The ideas describes in this RFC makes the Nixpkgs eco-system more powerful, but\n+these new features come at at a price:\n+\n+* Using high-level abstractions hide the low-level configuration details of\n+  specific process managers. This makes it sometimes more difficult to reason\n+  about what is happening in problematic cases\n+* More testing is required -- we need to test services in multiple deployment\n+  scenarios, which is more expensive. Fortunately, I have seen that testing for\n+  two kinds of targets often give enough certainty: if you test with a\n+  foreground process (e.g. `systemd`) and a daemon (e.g. `sysvinit`) there is a\n+  big chance that it will work with other process managers as well.\n+* You may target the lowest common denominator. This is not actually true,\n+  because with overrides it is still possible to use any feature of a process\n+  manager. Moreover, not all services need to be portable -- low-level services\n+  such as `cups` and `syslogd` can still be packaged in a process\n+  manager-specific way.\n+\n+# Alternatives\n+[alternatives]: #alternatives\n+\n+This RFC proposes a new strategy to allow multiple process instances of the\n+same service to co-exist. Co-existence is possible by avoiding conflicts --\n+making sure that state directories and port numbers do not conflict through\n+function parameters.\n+\n+It is also possible to isolate services by using concepts such as Linux\n+namespaces. Then it is not required to avoid certain kinds of conflicts.\n+Unfortunately, using these features is restricted to Linux only -- this RFC aims\n+to be operating system agnostic.\n+\n+# Prior art\n+[prior-art]: #prior-art\n+\n+This RFC is not the first approach that combines Nix with a process management\n+solution:\n+\n+* The service deployment chapter (chapter 9) of\n+  [Eelco Dolstra's PhD thesis](https://github.com/edolstra/edolstra.github.io/blob/master/pubs/phd-thesis.pdf)\n+  describes a very early service deployment approach that uses generated\n+  start/stop scripts implemented in `bash`. Because shell scripts are so common,\n+  they can be used on a variety of operating systems -- in addition to Linux, it\n+  can also be used on Darwin and FreeBSD.\n+* [NixOS](https://nixos.org) uses `systemd` as its process management solution.\n+  Services can be deployed by generating systemd unit configuration files in a\n+  Nix expression. NixOS deploys `systemd` units globally and requires super\n+  user privileges to get deployed. Moreover, you cannot use the service layer\n+  independently from the rest of NixOS.\n+* [nix-darwin](https://github.com/LnL7/nix-darwin) provides various NixOS\n+  features on macOS/Darwin, including a service management layer that uses the\n+  process manager of macOS: `launchd`.\n+* [home-manager](https://github.com/nix-community/home-manager) offers a service\n+  layer that works with systemd user services, rather than system services. It\n+  contains its own set of service configurations that can be deployed as user\n+  services.\n+* [system-manager](https://github.com/numtide/system-manager) addresses the\n+  limitation of NixOS to use systemd units independently and can deploy many\n+  services in the NixOS repository on any Ubuntu-based distribution.\n+\n+Aside from the first option, the biggest drawback is that these solutions are\n+not generic and there is only little reuse possible between service\n+configurations.\n+\n+# Unresolved questions\n+[unresolved]: #unresolved-questions\n+\n+The integration with NixOS module system still needs to be worked out in detail.\n+Currently, I can think of two scenarios. For this a discussion in the Nix\n+community is required.",
   "id": 1347859356,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 885,
   "original_position": 885,
   "original_start_line": 883,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1660524954,
   "reactions": {
    "+1": 5,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 5
   },
   "side": "RIGHT",
   "start_line": 883,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-05T19:11:37Z",
   "user": {
    "id": 20525370,
    "login": "infinisil",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This proposal might combine very well with the new `pkgs/by-name` directory introduced with [RFC 140](https://github.com/NixOS/rfcs/pull/140), because it could be extended to support a standard `service.nix` file to specify a standardised service definition for a package, without having to be tied to NixOS.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-05T19:10:09Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.\n+\n+For many kinds of services (e.g. PostgreSQL, Apache HTTP server etc), it\n+typically suffices to only have a single instance running on a machine. For\n+microservices, it may actually be quite useful to have the ability to easily\n+create multiple instances. For testing/experimentation purposes it may also be\n+desired to run multiple instances of a system service, such as a web server.\n+\n+It is also not easily possible in NixOS to switch to alternative implementations\n+of a service. For many services, alternative implementations (e.g. `cron`,\n+`ssh`) are not required, but some services accept complex configurations, such\n+as the Apache HTTP server and Apache Tomcat.\n+\n+Finding a universal abstraction in NixOS to facilitate all possible use cases\n+may be impossible. Moreover, it may also be desired for developers to write\n+their own abstractions on top of existing functionality.\n+\n+To support abstractions and multiple instances, the Nix process management\n+framework does not use the NixOS module system, but a convention that is based\n+on the organisation of \"ordinary\" Nix packages in the Nixpkgs repository.\n+\n+In Nixpkgs, most package follow the convention that a separate Nix file defines\n+a function in which the function header refers to the build-time dependencies to\n+construct a package from source. Packages are composed in the top-level Nix\n+expression (`all-packages.nix`) that invokes these functions with the required\n+build-time parameters. For most packages, only a single variant is composed, but\n+it is also possible to build different kinds of package variants by changing the\n+function parameters.\n+\n+The Nix process management framework follows the same principles and extends the\n+Nixpkgs convention with a number of additional concepts:\n+\n+* Every service is instantiated from a *constructor* which is a nested function:\n+  the outer function refers to the build-time dependencies that are required to\n+  build the service from source code.\n+* The inner function refers to the service *instance parameters* -- calling the\n+  constructor with different parameters makes it possible to compose a service\n+  with an alternative configuration and in such a way that they do not conflict\n+  with other instances.\n+* The build-time dependencies of constructor functions are composed in a Nix\n+  expression that is typically called: `constructors.nix` in which every\n+  constructor function is called with its required build-time dependency\n+  parameters.\n+* The process instances of a system are composed in a Nix expression that is\n+  typically called: `processes.nix` in which every constructor function is\n+  called with its required instance parameters.\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    chown ${user}:${group} ${socketDir}\n+    chown ${user}:${group} ${dataDir}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        su ${user} -c '${postgresql}/bin/initdb -D ${dataDir} --no-locale'\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a constructor function for PostgreSQL and has the\n+following properties:\n+\n+* The outer function-header (first line) refer to the build inputs of the\n+  service. For example, to construct a configuration from a high-level\n+  specification for any supported process manager, we propagate the\n+  `createManagedProcess` as a parameter.\n+* The inner-function header refers to the instance parameters of a PostgreSQL\n+  instance. When it is desired to have two instances deployed to the same\n+  machine we must make sure that there is no conflict between the state of\n+  the two instances. Avoiding a conflict is possible by assigning a unique TCP\n+  port number and making sure that the state directories do not conflict. The\n+  `instanceName` parameter is appended to the name of the state directories to\n+  make this possible.\n+* In the body of the inner function, we invoke `createManagedProcess` to\n+  agnostically generate a configuration for a supported process manager.\n+  In the configuration, we provide an initialization script and we call the\n+  `postgres` service with its required command-line parameters and environment\n+  variables.\n+\n+As explained earlier, a constructor function is a nested function that we need to compose\n+twice. The build-time dependencies are composed in a Nix expression typically called\n+`constructors.nix`:\n+\n+```nix\n+{ nix-processmgmt ? ../../nix-processmgmt\n+, pkgs\n+, stateDir\n+, logDir\n+, runtimeDir\n+, cacheDir\n+, spoolDir\n+, libDir\n+, tmpDir\n+, callingUser ? null\n+, callingGroup ? null\n+, processManager\n+, ids ? {}\n+}:\n+\n+let\n+  createManagedProcess = import \"${nix-processmgmt}/nixproc/create-managed-process/universal/create-managed-process-universal.nix\" {\n+    inherit pkgs runtimeDir stateDir logDir tmpDir processManager ids;\n+  };\n+in\n+{\n+  postgresql = import ./postgresql {\n+    inherit createManagedProcess stateDir runtimeDir;\n+    inherit (pkgs) lib postgresql su;\n+  };\n+}\n+```\n+\n+In the above Nix expression, we invoke the `postgresql` constructor function and\n+we propagated its required build-time dependencies, such as the `postgresql`\n+package and the `createManagedProcess` abstraction function.\n+\n+The instance parameters are composed in a Nix expression that is typically called\n+`processes.nix`:\n+\n+```nix\n+{ pkgs ? import <nixpkgs> { inherit system; }\n+, system ? builtins.currentSystem\n+, stateDir ? \"/var\"\n+, runtimeDir ? \"${stateDir}/run\"\n+, logDir ? \"${stateDir}/log\"\n+, spoolDir ? \"${stateDir}/spool\"\n+, cacheDir ? \"${stateDir}/cache\"\n+, libDir ? \"${stateDir}/lib\"\n+, tmpDir ? (if stateDir == \"/var\" then \"/tmp\" else \"${stateDir}/tmp\")\n+, processManager\n+, nix-processmgmt ? ../../../nix-processmgmt\n+}:\n+\n+let\n+  constructors = import ../../services-agnostic/constructors.nix {\n+    inherit pkgs stateDir runtimeDir logDir tmpDir cacheDir spoolDir libDir processManager nix-processmgmt;\n+  };\n+in\n+rec {\n+  postgresql = rec {\n+    port = 5432;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+    };\n+  };\n+\n+  postgresql-secondary = rec {\n+    port = 5433;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+      instanceSuffix = \"-secondary\";\n+    };\n+  };\n+}\n+```\n+\n+In the above Nix expression, we compose two PostgreSQL instances that run on the\n+same machine simultaneously. This is possible due to the fact that they use\n+instance parameters that avoid their states to conflict.\n+\n+The above expression imports the `constructors.nix` expression so that each\n+process instance can be composed from the PostgreSQL constructor function.\n+\n+In NixOS, we use the NixOS module system that makes it possible to define\n+multiple aspects of a system in a module (such as packages, config files,\n+systemd units and environment variables) and combine the aspects of each module\n+into a single configuration from which a complete system is deployed, which is\n+very powerful.\n+\n+Combining the conventions of the nix-processmgmt framework with the NixOS module\n+system is something that still needs to be discussed. There are variety of\n+options possible, such as:\n+\n+* Combining the convention with process instances as described earlier by\n+  referring to a process instance from a NixOS module, similar to how NixOS\n+  modules can also refer to ordinary Nix packages\n+* Using sub modules, in which every sub module can define its own variant of a\n+  service.\n+\n+### Unprivileged user deployments\n+\n+Another interesting feature of Nix is to allow unprivileged users to deploy\n+packages. With services, this is also possible, but there are some things we\n+need to take into account, such as:\n+\n+* The state directories must refer to a directory that has writable permissions\n+  for the user\n+* We cannot bind to a TCP/UDP port below 1024\n+\n+We can also introduce a configuration flag and configure a service in such a way\n+that the above criteria are met:\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir, forceDisableUserChange}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    ${lib.optionalString (!forceDisableUserChange) ''\n+      chown ${user}:${group} ${socketDir}\n+      chown ${user}:${group} ${dataDir}\n+    ''}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        ${lib.optionalString (!forceDisableUserChange) \"su ${user} -c '\"}${postgresql}/bin/initdb -D ${dataDir} --no-locale${lib.optionalString (!forceDisableUserChange) \"'\"}\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a revised PostgreSQL constructor function that\n+accepts a new parameter: `forceDisableUserChange`. When this property is\n+enabled, the service configured in such a way that it does not change users,\n+which an unprivileged user is not allowed to do. With this setting enabled, it\n+becomes possible for an unprivileged user to deploy PostgreSQL.\n+\n+# Examples and Interactions\n+[examples-and-interactions]: #examples-and-interactions\n+\n+The `processes.nix` expression shown earlier can be used with a variety of tools\n+to deploy running process instances.\n+\n+The following command deploys the configurations as `sysvinit` scripts and\n+activates the configuration automatically:\n+\n+```bash\n+$ nixproc-sysvinit-switch processes.nix\n+```\n+\n+We can deploy the same configuration as systemd units with the following command:\n+\n+```bash\n+$ nixproc-systemd-switch processes.nix\n+```\n+\n+Or with `supervisord` with the following command:\n+\n+```bash\n+$ nixproc-supervisord-switch processes.nix\n+```\n+\n+As an unprivileged user, we can use the `--state-dir` parameter to change the\n+prefix of the state directories to reside in a user's home directory\n+(`/home/sander/var`) and disable user changing:\n+\n+```bash\n+$ nixproc-sysvinit-switch --state-dir /home/sander/var --force-disable-user-change processes.nix\n+```\n+\n+# Drawbacks\n+[drawbacks]: #drawbacks\n+\n+The ideas describes in this RFC makes the Nixpkgs eco-system more powerful, but\n+these new features come at at a price:\n+\n+* Using high-level abstractions hide the low-level configuration details of\n+  specific process managers. This makes it sometimes more difficult to reason\n+  about what is happening in problematic cases\n+* More testing is required -- we need to test services in multiple deployment\n+  scenarios, which is more expensive. Fortunately, I have seen that testing for\n+  two kinds of targets often give enough certainty: if you test with a\n+  foreground process (e.g. `systemd`) and a daemon (e.g. `sysvinit`) there is a\n+  big chance that it will work with other process managers as well.\n+* You may target the lowest common denominator. This is not actually true,\n+  because with overrides it is still possible to use any feature of a process\n+  manager. Moreover, not all services need to be portable -- low-level services\n+  such as `cups` and `syslogd` can still be packaged in a process\n+  manager-specific way.\n+\n+# Alternatives\n+[alternatives]: #alternatives\n+\n+This RFC proposes a new strategy to allow multiple process instances of the\n+same service to co-exist. Co-existence is possible by avoiding conflicts --\n+making sure that state directories and port numbers do not conflict through\n+function parameters.\n+\n+It is also possible to isolate services by using concepts such as Linux\n+namespaces. Then it is not required to avoid certain kinds of conflicts.\n+Unfortunately, using these features is restricted to Linux only -- this RFC aims\n+to be operating system agnostic.\n+\n+# Prior art\n+[prior-art]: #prior-art\n+\n+This RFC is not the first approach that combines Nix with a process management\n+solution:\n+\n+* The service deployment chapter (chapter 9) of\n+  [Eelco Dolstra's PhD thesis](https://github.com/edolstra/edolstra.github.io/blob/master/pubs/phd-thesis.pdf)\n+  describes a very early service deployment approach that uses generated\n+  start/stop scripts implemented in `bash`. Because shell scripts are so common,\n+  they can be used on a variety of operating systems -- in addition to Linux, it\n+  can also be used on Darwin and FreeBSD.\n+* [NixOS](https://nixos.org) uses `systemd` as its process management solution.\n+  Services can be deployed by generating systemd unit configuration files in a\n+  Nix expression. NixOS deploys `systemd` units globally and requires super\n+  user privileges to get deployed. Moreover, you cannot use the service layer\n+  independently from the rest of NixOS.\n+* [nix-darwin](https://github.com/LnL7/nix-darwin) provides various NixOS\n+  features on macOS/Darwin, including a service management layer that uses the\n+  process manager of macOS: `launchd`.\n+* [home-manager](https://github.com/nix-community/home-manager) offers a service\n+  layer that works with systemd user services, rather than system services. It\n+  contains its own set of service configurations that can be deployed as user\n+  services.\n+* [system-manager](https://github.com/numtide/system-manager) addresses the\n+  limitation of NixOS to use systemd units independently and can deploy many\n+  services in the NixOS repository on any Ubuntu-based distribution.\n+\n+Aside from the first option, the biggest drawback is that these solutions are\n+not generic and there is only little reuse possible between service\n+configurations.\n+\n+# Unresolved questions\n+[unresolved]: #unresolved-questions\n+\n+The integration with NixOS module system still needs to be worked out in detail.\n+Currently, I can think of two scenarios. For this a discussion in the Nix\n+community is required.",
   "id": 1347861030,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 885,
   "original_position": 885,
   "original_start_line": 883,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1660524954,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 883,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-05T19:10:27Z",
   "user": {
    "id": 20525370,
    "login": "infinisil",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "@infinisil can you please elaborate on your comment a bit? I'd like to make sure I understand you clearly. I'm not sure if you mean that this `service.nix` file would be an interface (`options`) without implementation (`config`), or something else... and to what extent the separation would go \r\n\r\nI can imagine a `service.nix` which declares a common set of options for a module and then several other files (`systemd.nix`, `supervisord.nix`, `launchd.nix`, etc...) which define different implementations of these options (maybe even declaring some implementation specific options of their own). I love how this could tie into RFC 140!\r\n\r\nOr did you have something else in mind?",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-06T03:56:09Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.\n+\n+For many kinds of services (e.g. PostgreSQL, Apache HTTP server etc), it\n+typically suffices to only have a single instance running on a machine. For\n+microservices, it may actually be quite useful to have the ability to easily\n+create multiple instances. For testing/experimentation purposes it may also be\n+desired to run multiple instances of a system service, such as a web server.\n+\n+It is also not easily possible in NixOS to switch to alternative implementations\n+of a service. For many services, alternative implementations (e.g. `cron`,\n+`ssh`) are not required, but some services accept complex configurations, such\n+as the Apache HTTP server and Apache Tomcat.\n+\n+Finding a universal abstraction in NixOS to facilitate all possible use cases\n+may be impossible. Moreover, it may also be desired for developers to write\n+their own abstractions on top of existing functionality.\n+\n+To support abstractions and multiple instances, the Nix process management\n+framework does not use the NixOS module system, but a convention that is based\n+on the organisation of \"ordinary\" Nix packages in the Nixpkgs repository.\n+\n+In Nixpkgs, most package follow the convention that a separate Nix file defines\n+a function in which the function header refers to the build-time dependencies to\n+construct a package from source. Packages are composed in the top-level Nix\n+expression (`all-packages.nix`) that invokes these functions with the required\n+build-time parameters. For most packages, only a single variant is composed, but\n+it is also possible to build different kinds of package variants by changing the\n+function parameters.\n+\n+The Nix process management framework follows the same principles and extends the\n+Nixpkgs convention with a number of additional concepts:\n+\n+* Every service is instantiated from a *constructor* which is a nested function:\n+  the outer function refers to the build-time dependencies that are required to\n+  build the service from source code.\n+* The inner function refers to the service *instance parameters* -- calling the\n+  constructor with different parameters makes it possible to compose a service\n+  with an alternative configuration and in such a way that they do not conflict\n+  with other instances.\n+* The build-time dependencies of constructor functions are composed in a Nix\n+  expression that is typically called: `constructors.nix` in which every\n+  constructor function is called with its required build-time dependency\n+  parameters.\n+* The process instances of a system are composed in a Nix expression that is\n+  typically called: `processes.nix` in which every constructor function is\n+  called with its required instance parameters.\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    chown ${user}:${group} ${socketDir}\n+    chown ${user}:${group} ${dataDir}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        su ${user} -c '${postgresql}/bin/initdb -D ${dataDir} --no-locale'\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a constructor function for PostgreSQL and has the\n+following properties:\n+\n+* The outer function-header (first line) refer to the build inputs of the\n+  service. For example, to construct a configuration from a high-level\n+  specification for any supported process manager, we propagate the\n+  `createManagedProcess` as a parameter.\n+* The inner-function header refers to the instance parameters of a PostgreSQL\n+  instance. When it is desired to have two instances deployed to the same\n+  machine we must make sure that there is no conflict between the state of\n+  the two instances. Avoiding a conflict is possible by assigning a unique TCP\n+  port number and making sure that the state directories do not conflict. The\n+  `instanceName` parameter is appended to the name of the state directories to\n+  make this possible.\n+* In the body of the inner function, we invoke `createManagedProcess` to\n+  agnostically generate a configuration for a supported process manager.\n+  In the configuration, we provide an initialization script and we call the\n+  `postgres` service with its required command-line parameters and environment\n+  variables.\n+\n+As explained earlier, a constructor function is a nested function that we need to compose\n+twice. The build-time dependencies are composed in a Nix expression typically called\n+`constructors.nix`:\n+\n+```nix\n+{ nix-processmgmt ? ../../nix-processmgmt\n+, pkgs\n+, stateDir\n+, logDir\n+, runtimeDir\n+, cacheDir\n+, spoolDir\n+, libDir\n+, tmpDir\n+, callingUser ? null\n+, callingGroup ? null\n+, processManager\n+, ids ? {}\n+}:\n+\n+let\n+  createManagedProcess = import \"${nix-processmgmt}/nixproc/create-managed-process/universal/create-managed-process-universal.nix\" {\n+    inherit pkgs runtimeDir stateDir logDir tmpDir processManager ids;\n+  };\n+in\n+{\n+  postgresql = import ./postgresql {\n+    inherit createManagedProcess stateDir runtimeDir;\n+    inherit (pkgs) lib postgresql su;\n+  };\n+}\n+```\n+\n+In the above Nix expression, we invoke the `postgresql` constructor function and\n+we propagated its required build-time dependencies, such as the `postgresql`\n+package and the `createManagedProcess` abstraction function.\n+\n+The instance parameters are composed in a Nix expression that is typically called\n+`processes.nix`:\n+\n+```nix\n+{ pkgs ? import <nixpkgs> { inherit system; }\n+, system ? builtins.currentSystem\n+, stateDir ? \"/var\"\n+, runtimeDir ? \"${stateDir}/run\"\n+, logDir ? \"${stateDir}/log\"\n+, spoolDir ? \"${stateDir}/spool\"\n+, cacheDir ? \"${stateDir}/cache\"\n+, libDir ? \"${stateDir}/lib\"\n+, tmpDir ? (if stateDir == \"/var\" then \"/tmp\" else \"${stateDir}/tmp\")\n+, processManager\n+, nix-processmgmt ? ../../../nix-processmgmt\n+}:\n+\n+let\n+  constructors = import ../../services-agnostic/constructors.nix {\n+    inherit pkgs stateDir runtimeDir logDir tmpDir cacheDir spoolDir libDir processManager nix-processmgmt;\n+  };\n+in\n+rec {\n+  postgresql = rec {\n+    port = 5432;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+    };\n+  };\n+\n+  postgresql-secondary = rec {\n+    port = 5433;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+      instanceSuffix = \"-secondary\";\n+    };\n+  };\n+}\n+```\n+\n+In the above Nix expression, we compose two PostgreSQL instances that run on the\n+same machine simultaneously. This is possible due to the fact that they use\n+instance parameters that avoid their states to conflict.\n+\n+The above expression imports the `constructors.nix` expression so that each\n+process instance can be composed from the PostgreSQL constructor function.\n+\n+In NixOS, we use the NixOS module system that makes it possible to define\n+multiple aspects of a system in a module (such as packages, config files,\n+systemd units and environment variables) and combine the aspects of each module\n+into a single configuration from which a complete system is deployed, which is\n+very powerful.\n+\n+Combining the conventions of the nix-processmgmt framework with the NixOS module\n+system is something that still needs to be discussed. There are variety of\n+options possible, such as:\n+\n+* Combining the convention with process instances as described earlier by\n+  referring to a process instance from a NixOS module, similar to how NixOS\n+  modules can also refer to ordinary Nix packages\n+* Using sub modules, in which every sub module can define its own variant of a\n+  service.\n+\n+### Unprivileged user deployments\n+\n+Another interesting feature of Nix is to allow unprivileged users to deploy\n+packages. With services, this is also possible, but there are some things we\n+need to take into account, such as:\n+\n+* The state directories must refer to a directory that has writable permissions\n+  for the user\n+* We cannot bind to a TCP/UDP port below 1024\n+\n+We can also introduce a configuration flag and configure a service in such a way\n+that the above criteria are met:\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir, forceDisableUserChange}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    ${lib.optionalString (!forceDisableUserChange) ''\n+      chown ${user}:${group} ${socketDir}\n+      chown ${user}:${group} ${dataDir}\n+    ''}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        ${lib.optionalString (!forceDisableUserChange) \"su ${user} -c '\"}${postgresql}/bin/initdb -D ${dataDir} --no-locale${lib.optionalString (!forceDisableUserChange) \"'\"}\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a revised PostgreSQL constructor function that\n+accepts a new parameter: `forceDisableUserChange`. When this property is\n+enabled, the service configured in such a way that it does not change users,\n+which an unprivileged user is not allowed to do. With this setting enabled, it\n+becomes possible for an unprivileged user to deploy PostgreSQL.\n+\n+# Examples and Interactions\n+[examples-and-interactions]: #examples-and-interactions\n+\n+The `processes.nix` expression shown earlier can be used with a variety of tools\n+to deploy running process instances.\n+\n+The following command deploys the configurations as `sysvinit` scripts and\n+activates the configuration automatically:\n+\n+```bash\n+$ nixproc-sysvinit-switch processes.nix\n+```\n+\n+We can deploy the same configuration as systemd units with the following command:\n+\n+```bash\n+$ nixproc-systemd-switch processes.nix\n+```\n+\n+Or with `supervisord` with the following command:\n+\n+```bash\n+$ nixproc-supervisord-switch processes.nix\n+```\n+\n+As an unprivileged user, we can use the `--state-dir` parameter to change the\n+prefix of the state directories to reside in a user's home directory\n+(`/home/sander/var`) and disable user changing:\n+\n+```bash\n+$ nixproc-sysvinit-switch --state-dir /home/sander/var --force-disable-user-change processes.nix\n+```\n+\n+# Drawbacks\n+[drawbacks]: #drawbacks\n+\n+The ideas describes in this RFC makes the Nixpkgs eco-system more powerful, but\n+these new features come at at a price:\n+\n+* Using high-level abstractions hide the low-level configuration details of\n+  specific process managers. This makes it sometimes more difficult to reason\n+  about what is happening in problematic cases\n+* More testing is required -- we need to test services in multiple deployment\n+  scenarios, which is more expensive. Fortunately, I have seen that testing for\n+  two kinds of targets often give enough certainty: if you test with a\n+  foreground process (e.g. `systemd`) and a daemon (e.g. `sysvinit`) there is a\n+  big chance that it will work with other process managers as well.\n+* You may target the lowest common denominator. This is not actually true,\n+  because with overrides it is still possible to use any feature of a process\n+  manager. Moreover, not all services need to be portable -- low-level services\n+  such as `cups` and `syslogd` can still be packaged in a process\n+  manager-specific way.\n+\n+# Alternatives\n+[alternatives]: #alternatives\n+\n+This RFC proposes a new strategy to allow multiple process instances of the\n+same service to co-exist. Co-existence is possible by avoiding conflicts --\n+making sure that state directories and port numbers do not conflict through\n+function parameters.\n+\n+It is also possible to isolate services by using concepts such as Linux\n+namespaces. Then it is not required to avoid certain kinds of conflicts.\n+Unfortunately, using these features is restricted to Linux only -- this RFC aims\n+to be operating system agnostic.\n+\n+# Prior art\n+[prior-art]: #prior-art\n+\n+This RFC is not the first approach that combines Nix with a process management\n+solution:\n+\n+* The service deployment chapter (chapter 9) of\n+  [Eelco Dolstra's PhD thesis](https://github.com/edolstra/edolstra.github.io/blob/master/pubs/phd-thesis.pdf)\n+  describes a very early service deployment approach that uses generated\n+  start/stop scripts implemented in `bash`. Because shell scripts are so common,\n+  they can be used on a variety of operating systems -- in addition to Linux, it\n+  can also be used on Darwin and FreeBSD.\n+* [NixOS](https://nixos.org) uses `systemd` as its process management solution.\n+  Services can be deployed by generating systemd unit configuration files in a\n+  Nix expression. NixOS deploys `systemd` units globally and requires super\n+  user privileges to get deployed. Moreover, you cannot use the service layer\n+  independently from the rest of NixOS.\n+* [nix-darwin](https://github.com/LnL7/nix-darwin) provides various NixOS\n+  features on macOS/Darwin, including a service management layer that uses the\n+  process manager of macOS: `launchd`.\n+* [home-manager](https://github.com/nix-community/home-manager) offers a service\n+  layer that works with systemd user services, rather than system services. It\n+  contains its own set of service configurations that can be deployed as user\n+  services.\n+* [system-manager](https://github.com/numtide/system-manager) addresses the\n+  limitation of NixOS to use systemd units independently and can deploy many\n+  services in the NixOS repository on any Ubuntu-based distribution.\n+\n+Aside from the first option, the biggest drawback is that these solutions are\n+not generic and there is only little reuse possible between service\n+configurations.\n+\n+# Unresolved questions\n+[unresolved]: #unresolved-questions\n+\n+The integration with NixOS module system still needs to be worked out in detail.\n+Currently, I can think of two scenarios. For this a discussion in the Nix\n+community is required.",
   "id": 1348215130,
   "in_reply_to_id": 1347861030,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 885,
   "original_position": 885,
   "original_start_line": 883,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1661072637,
   "reactions": {
    "+1": 4,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 4
   },
   "side": "RIGHT",
   "start_line": 883,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-06T03:56:09Z",
   "user": {
    "id": 7755101,
    "login": "aanderse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "FYI: We already have support for building portable service images https://nixos.org/manual/nixpkgs/stable/#sec-pkgs-portableService though I'd love it to be generalized to using the host's nix store instead of building a completely new image",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-06T04:58:04Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a",
   "id": 1348243867,
   "line": 107,
   "original_commit_id": "831405257e5c25126d6d3437d5075f62842baa42",
   "original_line": 107,
   "original_position": 107,
   "original_start_line": null,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 107,
   "pull_request_review_id": 1661115188,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 1,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 1
   },
   "side": "RIGHT",
   "start_line": null,
   "start_side": null,
   "subject_type": "line",
   "updated_at": "2023-10-06T04:58:04Z",
   "user": {
    "id": 628387,
    "login": "arianvp",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Note that we have a community-maintained set of patches that make systemd run on musl",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-06T04:59:09Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage",
   "id": 1348244293,
   "line": 135,
   "original_commit_id": "831405257e5c25126d6d3437d5075f62842baa42",
   "original_line": 135,
   "original_position": 135,
   "original_start_line": null,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 135,
   "pull_request_review_id": 1661115869,
   "reactions": {
    "+1": 0,
    "-1": 1,
    "confused": 0,
    "eyes": 7,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 8
   },
   "side": "RIGHT",
   "start_line": null,
   "start_side": null,
   "subject_type": "line",
   "updated_at": "2023-10-06T04:59:09Z",
   "user": {
    "id": 628387,
    "login": "arianvp",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Looking for something like this, I wast pointed to https://devenv.sh/services/ as the most likely current candidate. I think it would make sense to add that to prior art as well.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-06T10:12:05Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.\n+\n+For many kinds of services (e.g. PostgreSQL, Apache HTTP server etc), it\n+typically suffices to only have a single instance running on a machine. For\n+microservices, it may actually be quite useful to have the ability to easily\n+create multiple instances. For testing/experimentation purposes it may also be\n+desired to run multiple instances of a system service, such as a web server.\n+\n+It is also not easily possible in NixOS to switch to alternative implementations\n+of a service. For many services, alternative implementations (e.g. `cron`,\n+`ssh`) are not required, but some services accept complex configurations, such\n+as the Apache HTTP server and Apache Tomcat.\n+\n+Finding a universal abstraction in NixOS to facilitate all possible use cases\n+may be impossible. Moreover, it may also be desired for developers to write\n+their own abstractions on top of existing functionality.\n+\n+To support abstractions and multiple instances, the Nix process management\n+framework does not use the NixOS module system, but a convention that is based\n+on the organisation of \"ordinary\" Nix packages in the Nixpkgs repository.\n+\n+In Nixpkgs, most package follow the convention that a separate Nix file defines\n+a function in which the function header refers to the build-time dependencies to\n+construct a package from source. Packages are composed in the top-level Nix\n+expression (`all-packages.nix`) that invokes these functions with the required\n+build-time parameters. For most packages, only a single variant is composed, but\n+it is also possible to build different kinds of package variants by changing the\n+function parameters.\n+\n+The Nix process management framework follows the same principles and extends the\n+Nixpkgs convention with a number of additional concepts:\n+\n+* Every service is instantiated from a *constructor* which is a nested function:\n+  the outer function refers to the build-time dependencies that are required to\n+  build the service from source code.\n+* The inner function refers to the service *instance parameters* -- calling the\n+  constructor with different parameters makes it possible to compose a service\n+  with an alternative configuration and in such a way that they do not conflict\n+  with other instances.\n+* The build-time dependencies of constructor functions are composed in a Nix\n+  expression that is typically called: `constructors.nix` in which every\n+  constructor function is called with its required build-time dependency\n+  parameters.\n+* The process instances of a system are composed in a Nix expression that is\n+  typically called: `processes.nix` in which every constructor function is\n+  called with its required instance parameters.\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    chown ${user}:${group} ${socketDir}\n+    chown ${user}:${group} ${dataDir}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        su ${user} -c '${postgresql}/bin/initdb -D ${dataDir} --no-locale'\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a constructor function for PostgreSQL and has the\n+following properties:\n+\n+* The outer function-header (first line) refer to the build inputs of the\n+  service. For example, to construct a configuration from a high-level\n+  specification for any supported process manager, we propagate the\n+  `createManagedProcess` as a parameter.\n+* The inner-function header refers to the instance parameters of a PostgreSQL\n+  instance. When it is desired to have two instances deployed to the same\n+  machine we must make sure that there is no conflict between the state of\n+  the two instances. Avoiding a conflict is possible by assigning a unique TCP\n+  port number and making sure that the state directories do not conflict. The\n+  `instanceName` parameter is appended to the name of the state directories to\n+  make this possible.\n+* In the body of the inner function, we invoke `createManagedProcess` to\n+  agnostically generate a configuration for a supported process manager.\n+  In the configuration, we provide an initialization script and we call the\n+  `postgres` service with its required command-line parameters and environment\n+  variables.\n+\n+As explained earlier, a constructor function is a nested function that we need to compose\n+twice. The build-time dependencies are composed in a Nix expression typically called\n+`constructors.nix`:\n+\n+```nix\n+{ nix-processmgmt ? ../../nix-processmgmt\n+, pkgs\n+, stateDir\n+, logDir\n+, runtimeDir\n+, cacheDir\n+, spoolDir\n+, libDir\n+, tmpDir\n+, callingUser ? null\n+, callingGroup ? null\n+, processManager\n+, ids ? {}\n+}:\n+\n+let\n+  createManagedProcess = import \"${nix-processmgmt}/nixproc/create-managed-process/universal/create-managed-process-universal.nix\" {\n+    inherit pkgs runtimeDir stateDir logDir tmpDir processManager ids;\n+  };\n+in\n+{\n+  postgresql = import ./postgresql {\n+    inherit createManagedProcess stateDir runtimeDir;\n+    inherit (pkgs) lib postgresql su;\n+  };\n+}\n+```\n+\n+In the above Nix expression, we invoke the `postgresql` constructor function and\n+we propagated its required build-time dependencies, such as the `postgresql`\n+package and the `createManagedProcess` abstraction function.\n+\n+The instance parameters are composed in a Nix expression that is typically called\n+`processes.nix`:\n+\n+```nix\n+{ pkgs ? import <nixpkgs> { inherit system; }\n+, system ? builtins.currentSystem\n+, stateDir ? \"/var\"\n+, runtimeDir ? \"${stateDir}/run\"\n+, logDir ? \"${stateDir}/log\"\n+, spoolDir ? \"${stateDir}/spool\"\n+, cacheDir ? \"${stateDir}/cache\"\n+, libDir ? \"${stateDir}/lib\"\n+, tmpDir ? (if stateDir == \"/var\" then \"/tmp\" else \"${stateDir}/tmp\")\n+, processManager\n+, nix-processmgmt ? ../../../nix-processmgmt\n+}:\n+\n+let\n+  constructors = import ../../services-agnostic/constructors.nix {\n+    inherit pkgs stateDir runtimeDir logDir tmpDir cacheDir spoolDir libDir processManager nix-processmgmt;\n+  };\n+in\n+rec {\n+  postgresql = rec {\n+    port = 5432;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+    };\n+  };\n+\n+  postgresql-secondary = rec {\n+    port = 5433;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+      instanceSuffix = \"-secondary\";\n+    };\n+  };\n+}\n+```\n+\n+In the above Nix expression, we compose two PostgreSQL instances that run on the\n+same machine simultaneously. This is possible due to the fact that they use\n+instance parameters that avoid their states to conflict.\n+\n+The above expression imports the `constructors.nix` expression so that each\n+process instance can be composed from the PostgreSQL constructor function.\n+\n+In NixOS, we use the NixOS module system that makes it possible to define\n+multiple aspects of a system in a module (such as packages, config files,\n+systemd units and environment variables) and combine the aspects of each module\n+into a single configuration from which a complete system is deployed, which is\n+very powerful.\n+\n+Combining the conventions of the nix-processmgmt framework with the NixOS module\n+system is something that still needs to be discussed. There are variety of\n+options possible, such as:\n+\n+* Combining the convention with process instances as described earlier by\n+  referring to a process instance from a NixOS module, similar to how NixOS\n+  modules can also refer to ordinary Nix packages\n+* Using sub modules, in which every sub module can define its own variant of a\n+  service.\n+\n+### Unprivileged user deployments\n+\n+Another interesting feature of Nix is to allow unprivileged users to deploy\n+packages. With services, this is also possible, but there are some things we\n+need to take into account, such as:\n+\n+* The state directories must refer to a directory that has writable permissions\n+  for the user\n+* We cannot bind to a TCP/UDP port below 1024\n+\n+We can also introduce a configuration flag and configure a service in such a way\n+that the above criteria are met:\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir, forceDisableUserChange}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    ${lib.optionalString (!forceDisableUserChange) ''\n+      chown ${user}:${group} ${socketDir}\n+      chown ${user}:${group} ${dataDir}\n+    ''}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        ${lib.optionalString (!forceDisableUserChange) \"su ${user} -c '\"}${postgresql}/bin/initdb -D ${dataDir} --no-locale${lib.optionalString (!forceDisableUserChange) \"'\"}\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a revised PostgreSQL constructor function that\n+accepts a new parameter: `forceDisableUserChange`. When this property is\n+enabled, the service configured in such a way that it does not change users,\n+which an unprivileged user is not allowed to do. With this setting enabled, it\n+becomes possible for an unprivileged user to deploy PostgreSQL.\n+\n+# Examples and Interactions\n+[examples-and-interactions]: #examples-and-interactions\n+\n+The `processes.nix` expression shown earlier can be used with a variety of tools\n+to deploy running process instances.\n+\n+The following command deploys the configurations as `sysvinit` scripts and\n+activates the configuration automatically:\n+\n+```bash\n+$ nixproc-sysvinit-switch processes.nix\n+```\n+\n+We can deploy the same configuration as systemd units with the following command:\n+\n+```bash\n+$ nixproc-systemd-switch processes.nix\n+```\n+\n+Or with `supervisord` with the following command:\n+\n+```bash\n+$ nixproc-supervisord-switch processes.nix\n+```\n+\n+As an unprivileged user, we can use the `--state-dir` parameter to change the\n+prefix of the state directories to reside in a user's home directory\n+(`/home/sander/var`) and disable user changing:\n+\n+```bash\n+$ nixproc-sysvinit-switch --state-dir /home/sander/var --force-disable-user-change processes.nix\n+```\n+\n+# Drawbacks\n+[drawbacks]: #drawbacks\n+\n+The ideas describes in this RFC makes the Nixpkgs eco-system more powerful, but\n+these new features come at at a price:\n+\n+* Using high-level abstractions hide the low-level configuration details of\n+  specific process managers. This makes it sometimes more difficult to reason\n+  about what is happening in problematic cases\n+* More testing is required -- we need to test services in multiple deployment\n+  scenarios, which is more expensive. Fortunately, I have seen that testing for\n+  two kinds of targets often give enough certainty: if you test with a\n+  foreground process (e.g. `systemd`) and a daemon (e.g. `sysvinit`) there is a\n+  big chance that it will work with other process managers as well.\n+* You may target the lowest common denominator. This is not actually true,\n+  because with overrides it is still possible to use any feature of a process\n+  manager. Moreover, not all services need to be portable -- low-level services\n+  such as `cups` and `syslogd` can still be packaged in a process\n+  manager-specific way.\n+\n+# Alternatives\n+[alternatives]: #alternatives\n+\n+This RFC proposes a new strategy to allow multiple process instances of the\n+same service to co-exist. Co-existence is possible by avoiding conflicts --\n+making sure that state directories and port numbers do not conflict through\n+function parameters.\n+\n+It is also possible to isolate services by using concepts such as Linux\n+namespaces. Then it is not required to avoid certain kinds of conflicts.\n+Unfortunately, using these features is restricted to Linux only -- this RFC aims\n+to be operating system agnostic.\n+\n+# Prior art\n+[prior-art]: #prior-art",
   "id": 1348520867,
   "in_reply_to_id": 1347856747,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 849,
   "original_position": 849,
   "original_start_line": null,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1661542340,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": null,
   "start_side": null,
   "subject_type": "line",
   "updated_at": "2023-10-06T10:12:06Z",
   "user": {
    "id": 72429,
    "login": "alper",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I'm going to add examples for the other two scenarios as well. Indeed, the gist that you gave me is a way to use the modular-style for multi-instance purposes.\r\n\r\nI'd also like to explore the third option that I gave -- the hybrid approach between the functional style and the NixOS module system.\r\n\r\nI'd like to show all three scenarios, because each of them have pros and cons. The benefit of the functional/pkgs style is that it is less memory consuming and components are configured on a need-to-know basis. The downside is obviously that you can only define one system aspect and not combine multiple aspects into one (e.g. such as adding programs to the PATH etc). Also, the NixOS module system has a \"type system\" which the functional style doesn't use.\r\n\r\nThe NixOS modular style is beneficial because it does fit well in the current conventions, it can be used to combine multiple system aspects into one and use of the type system to prevent certain kinds of errors. One of its major drawbacks is memory consumption/evaluation time.\r\n\r\nThe hybrid approach is something I still need to explore. Maybe it can be good a trade-off between the convenience of combining multiple aspects into one and the need-to-know properties of the functional style.\r\n\r\nI'd like to see that we consciously pick the most suitable option out of these three.\r\n\r\nExploring this may take some time, maybe I'll ask you for some help @infinisil ",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-06T10:14:19Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.",
   "id": 1348524039,
   "in_reply_to_id": 1347854623,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 486,
   "original_position": 486,
   "original_start_line": 482,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1661546392,
   "reactions": {
    "+1": 3,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 3
   },
   "side": "RIGHT",
   "start_line": 482,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-06T10:14:19Z",
   "user": {
    "id": 1153271,
    "login": "svanderburg",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Maybe that's true. On the other hand, the challenge is that the Nix process management is addressing multiple concerns at the same time that can be implemented in multiple ways. Choosing the implementation strategy is also something I'd like discuss. A notable example is the style in which process instances are specified.\r\n\r\nFor that reason, I think it's good to also provide a bit of context rather than only an implementation strategy, because this still needs to be decided.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-06T10:21:28Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.\n+\n+For many kinds of services (e.g. PostgreSQL, Apache HTTP server etc), it\n+typically suffices to only have a single instance running on a machine. For\n+microservices, it may actually be quite useful to have the ability to easily\n+create multiple instances. For testing/experimentation purposes it may also be\n+desired to run multiple instances of a system service, such as a web server.\n+\n+It is also not easily possible in NixOS to switch to alternative implementations\n+of a service. For many services, alternative implementations (e.g. `cron`,\n+`ssh`) are not required, but some services accept complex configurations, such\n+as the Apache HTTP server and Apache Tomcat.\n+\n+Finding a universal abstraction in NixOS to facilitate all possible use cases\n+may be impossible. Moreover, it may also be desired for developers to write\n+their own abstractions on top of existing functionality.\n+\n+To support abstractions and multiple instances, the Nix process management\n+framework does not use the NixOS module system, but a convention that is based\n+on the organisation of \"ordinary\" Nix packages in the Nixpkgs repository.\n+\n+In Nixpkgs, most package follow the convention that a separate Nix file defines\n+a function in which the function header refers to the build-time dependencies to\n+construct a package from source. Packages are composed in the top-level Nix\n+expression (`all-packages.nix`) that invokes these functions with the required\n+build-time parameters. For most packages, only a single variant is composed, but\n+it is also possible to build different kinds of package variants by changing the\n+function parameters.\n+\n+The Nix process management framework follows the same principles and extends the\n+Nixpkgs convention with a number of additional concepts:\n+\n+* Every service is instantiated from a *constructor* which is a nested function:\n+  the outer function refers to the build-time dependencies that are required to\n+  build the service from source code.\n+* The inner function refers to the service *instance parameters* -- calling the\n+  constructor with different parameters makes it possible to compose a service\n+  with an alternative configuration and in such a way that they do not conflict\n+  with other instances.\n+* The build-time dependencies of constructor functions are composed in a Nix\n+  expression that is typically called: `constructors.nix` in which every\n+  constructor function is called with its required build-time dependency\n+  parameters.\n+* The process instances of a system are composed in a Nix expression that is\n+  typically called: `processes.nix` in which every constructor function is\n+  called with its required instance parameters.\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    chown ${user}:${group} ${socketDir}\n+    chown ${user}:${group} ${dataDir}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        su ${user} -c '${postgresql}/bin/initdb -D ${dataDir} --no-locale'\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a constructor function for PostgreSQL and has the\n+following properties:\n+\n+* The outer function-header (first line) refer to the build inputs of the\n+  service. For example, to construct a configuration from a high-level\n+  specification for any supported process manager, we propagate the\n+  `createManagedProcess` as a parameter.\n+* The inner-function header refers to the instance parameters of a PostgreSQL\n+  instance. When it is desired to have two instances deployed to the same\n+  machine we must make sure that there is no conflict between the state of\n+  the two instances. Avoiding a conflict is possible by assigning a unique TCP\n+  port number and making sure that the state directories do not conflict. The\n+  `instanceName` parameter is appended to the name of the state directories to\n+  make this possible.\n+* In the body of the inner function, we invoke `createManagedProcess` to\n+  agnostically generate a configuration for a supported process manager.\n+  In the configuration, we provide an initialization script and we call the\n+  `postgres` service with its required command-line parameters and environment\n+  variables.\n+\n+As explained earlier, a constructor function is a nested function that we need to compose\n+twice. The build-time dependencies are composed in a Nix expression typically called\n+`constructors.nix`:\n+\n+```nix\n+{ nix-processmgmt ? ../../nix-processmgmt\n+, pkgs\n+, stateDir\n+, logDir\n+, runtimeDir\n+, cacheDir\n+, spoolDir\n+, libDir\n+, tmpDir\n+, callingUser ? null\n+, callingGroup ? null\n+, processManager\n+, ids ? {}\n+}:\n+\n+let\n+  createManagedProcess = import \"${nix-processmgmt}/nixproc/create-managed-process/universal/create-managed-process-universal.nix\" {\n+    inherit pkgs runtimeDir stateDir logDir tmpDir processManager ids;\n+  };\n+in\n+{\n+  postgresql = import ./postgresql {\n+    inherit createManagedProcess stateDir runtimeDir;\n+    inherit (pkgs) lib postgresql su;\n+  };\n+}\n+```\n+\n+In the above Nix expression, we invoke the `postgresql` constructor function and\n+we propagated its required build-time dependencies, such as the `postgresql`\n+package and the `createManagedProcess` abstraction function.\n+\n+The instance parameters are composed in a Nix expression that is typically called\n+`processes.nix`:\n+\n+```nix\n+{ pkgs ? import <nixpkgs> { inherit system; }\n+, system ? builtins.currentSystem\n+, stateDir ? \"/var\"\n+, runtimeDir ? \"${stateDir}/run\"\n+, logDir ? \"${stateDir}/log\"\n+, spoolDir ? \"${stateDir}/spool\"\n+, cacheDir ? \"${stateDir}/cache\"\n+, libDir ? \"${stateDir}/lib\"\n+, tmpDir ? (if stateDir == \"/var\" then \"/tmp\" else \"${stateDir}/tmp\")\n+, processManager\n+, nix-processmgmt ? ../../../nix-processmgmt\n+}:\n+\n+let\n+  constructors = import ../../services-agnostic/constructors.nix {\n+    inherit pkgs stateDir runtimeDir logDir tmpDir cacheDir spoolDir libDir processManager nix-processmgmt;\n+  };\n+in\n+rec {\n+  postgresql = rec {\n+    port = 5432;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+    };\n+  };\n+\n+  postgresql-secondary = rec {\n+    port = 5433;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+      instanceSuffix = \"-secondary\";\n+    };\n+  };\n+}\n+```\n+\n+In the above Nix expression, we compose two PostgreSQL instances that run on the\n+same machine simultaneously. This is possible due to the fact that they use\n+instance parameters that avoid their states to conflict.\n+\n+The above expression imports the `constructors.nix` expression so that each\n+process instance can be composed from the PostgreSQL constructor function.\n+\n+In NixOS, we use the NixOS module system that makes it possible to define\n+multiple aspects of a system in a module (such as packages, config files,\n+systemd units and environment variables) and combine the aspects of each module\n+into a single configuration from which a complete system is deployed, which is\n+very powerful.\n+\n+Combining the conventions of the nix-processmgmt framework with the NixOS module\n+system is something that still needs to be discussed. There are variety of\n+options possible, such as:\n+\n+* Combining the convention with process instances as described earlier by\n+  referring to a process instance from a NixOS module, similar to how NixOS\n+  modules can also refer to ordinary Nix packages\n+* Using sub modules, in which every sub module can define its own variant of a\n+  service.\n+\n+### Unprivileged user deployments\n+\n+Another interesting feature of Nix is to allow unprivileged users to deploy\n+packages. With services, this is also possible, but there are some things we\n+need to take into account, such as:\n+\n+* The state directories must refer to a directory that has writable permissions\n+  for the user\n+* We cannot bind to a TCP/UDP port below 1024\n+\n+We can also introduce a configuration flag and configure a service in such a way\n+that the above criteria are met:\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir, forceDisableUserChange}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    ${lib.optionalString (!forceDisableUserChange) ''\n+      chown ${user}:${group} ${socketDir}\n+      chown ${user}:${group} ${dataDir}\n+    ''}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        ${lib.optionalString (!forceDisableUserChange) \"su ${user} -c '\"}${postgresql}/bin/initdb -D ${dataDir} --no-locale${lib.optionalString (!forceDisableUserChange) \"'\"}\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a revised PostgreSQL constructor function that\n+accepts a new parameter: `forceDisableUserChange`. When this property is\n+enabled, the service configured in such a way that it does not change users,\n+which an unprivileged user is not allowed to do. With this setting enabled, it\n+becomes possible for an unprivileged user to deploy PostgreSQL.\n+\n+# Examples and Interactions\n+[examples-and-interactions]: #examples-and-interactions\n+\n+The `processes.nix` expression shown earlier can be used with a variety of tools\n+to deploy running process instances.\n+\n+The following command deploys the configurations as `sysvinit` scripts and\n+activates the configuration automatically:\n+\n+```bash\n+$ nixproc-sysvinit-switch processes.nix\n+```\n+\n+We can deploy the same configuration as systemd units with the following command:\n+\n+```bash\n+$ nixproc-systemd-switch processes.nix\n+```\n+\n+Or with `supervisord` with the following command:\n+\n+```bash\n+$ nixproc-supervisord-switch processes.nix\n+```\n+\n+As an unprivileged user, we can use the `--state-dir` parameter to change the\n+prefix of the state directories to reside in a user's home directory\n+(`/home/sander/var`) and disable user changing:\n+\n+```bash\n+$ nixproc-sysvinit-switch --state-dir /home/sander/var --force-disable-user-change processes.nix\n+```\n+\n+# Drawbacks\n+[drawbacks]: #drawbacks\n+\n+The ideas describes in this RFC makes the Nixpkgs eco-system more powerful, but\n+these new features come at at a price:\n+\n+* Using high-level abstractions hide the low-level configuration details of\n+  specific process managers. This makes it sometimes more difficult to reason\n+  about what is happening in problematic cases\n+* More testing is required -- we need to test services in multiple deployment\n+  scenarios, which is more expensive. Fortunately, I have seen that testing for\n+  two kinds of targets often give enough certainty: if you test with a\n+  foreground process (e.g. `systemd`) and a daemon (e.g. `sysvinit`) there is a\n+  big chance that it will work with other process managers as well.\n+* You may target the lowest common denominator. This is not actually true,\n+  because with overrides it is still possible to use any feature of a process\n+  manager. Moreover, not all services need to be portable -- low-level services\n+  such as `cups` and `syslogd` can still be packaged in a process\n+  manager-specific way.\n+\n+# Alternatives\n+[alternatives]: #alternatives\n+\n+This RFC proposes a new strategy to allow multiple process instances of the\n+same service to co-exist. Co-existence is possible by avoiding conflicts --\n+making sure that state directories and port numbers do not conflict through\n+function parameters.\n+\n+It is also possible to isolate services by using concepts such as Linux\n+namespaces. Then it is not required to avoid certain kinds of conflicts.\n+Unfortunately, using these features is restricted to Linux only -- this RFC aims\n+to be operating system agnostic.\n+\n+# Prior art\n+[prior-art]: #prior-art\n+\n+This RFC is not the first approach that combines Nix with a process management\n+solution:\n+\n+* The service deployment chapter (chapter 9) of\n+  [Eelco Dolstra's PhD thesis](https://github.com/edolstra/edolstra.github.io/blob/master/pubs/phd-thesis.pdf)\n+  describes a very early service deployment approach that uses generated\n+  start/stop scripts implemented in `bash`. Because shell scripts are so common,\n+  they can be used on a variety of operating systems -- in addition to Linux, it\n+  can also be used on Darwin and FreeBSD.\n+* [NixOS](https://nixos.org) uses `systemd` as its process management solution.\n+  Services can be deployed by generating systemd unit configuration files in a\n+  Nix expression. NixOS deploys `systemd` units globally and requires super\n+  user privileges to get deployed. Moreover, you cannot use the service layer\n+  independently from the rest of NixOS.\n+* [nix-darwin](https://github.com/LnL7/nix-darwin) provides various NixOS\n+  features on macOS/Darwin, including a service management layer that uses the\n+  process manager of macOS: `launchd`.\n+* [home-manager](https://github.com/nix-community/home-manager) offers a service\n+  layer that works with systemd user services, rather than system services. It\n+  contains its own set of service configurations that can be deployed as user\n+  services.\n+* [system-manager](https://github.com/numtide/system-manager) addresses the\n+  limitation of NixOS to use systemd units independently and can deploy many\n+  services in the NixOS repository on any Ubuntu-based distribution.\n+\n+Aside from the first option, the biggest drawback is that these solutions are\n+not generic and there is only little reuse possible between service\n+configurations.\n+\n+# Unresolved questions\n+[unresolved]: #unresolved-questions\n+\n+The integration with NixOS module system still needs to be worked out in detail.\n+Currently, I can think of two scenarios. For this a discussion in the Nix\n+community is required.",
   "id": 1348530745,
   "in_reply_to_id": 1347859356,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 885,
   "original_position": 885,
   "original_start_line": 883,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1661557727,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 3,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 3
   },
   "side": "RIGHT",
   "start_line": 883,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-06T10:21:28Z",
   "user": {
    "id": 1153271,
    "login": "svanderburg",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This effort should be better quantified.\r\nNix repo does have CI jobs only for MacOS and (Ubuntu) Linux only. We don't have a precise measure about how hard is to support FreeBSD or Cygwin.\r\n\r\nFurther, I am not aware of \"portability policies\" adopted by Nix source code repo.\r\nCertainly we have the ongoing (and very advanced, I dare to say) https://github.com/NixOS/rfcs/pull/132, but before this I have seen no such attempt of portability _as a policy_.\r\n\r\nEven further, as far as I can remember, the default install of Nix requires some, ehr,  post-install service management. And even among Linux distros it is not very portable.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-06T14:14:44Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.",
   "id": 1348780418,
   "line": 39,
   "original_commit_id": "dbd38c2cd817cfaa971c677be177bca91f37ee71",
   "original_line": 39,
   "original_position": 39,
   "original_start_line": 37,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 39,
   "pull_request_review_id": 1661959534,
   "reactions": {
    "+1": 1,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 1
   },
   "side": "RIGHT",
   "start_line": 37,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-06T14:44:54Z",
   "user": {
    "id": 5954806,
    "login": "AndersonTorres",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> I talked to you at Nixcon about exactly this [...]\r\n\r\n... too, but I did not write a gist. My suggestion was to use `imports`.\r\n\r\nAfter all, anything you can do by calling an arbitrary function, you can do by _importing into a submodule_.\r\n\r\nUsing it might look something like\r\n\r\n```nix\r\n# NixOS configuration.nix\r\n{ config, lib, serviceModules, ... }:\r\n{\r\n  services.app-db = {\r\n    imports = [ serviceModules.postgresql ];\r\n    listenPort = 15432;\r\n  };\r\n}\r\n```\r\n\r\n\r\n\r\n> One of its major drawbacks is memory consumption/evaluation time.\r\n\r\nThat's mostly a scaling problem with the current (anti?)pattern of using a massive module list that's always loaded.\r\nUsing imports as above, that's solved.\r\n\r\nA constant factor overhead does remain, but it's a very small price worth paying for a few service definitions, as opposed to, say, large package sets.\r\n",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-06T15:01:25Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.",
   "id": 1348841973,
   "in_reply_to_id": 1347854623,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 486,
   "original_position": 486,
   "original_start_line": 482,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1662073150,
   "reactions": {
    "+1": 5,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 5
   },
   "side": "RIGHT",
   "start_line": 482,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-06T15:01:26Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Or kubenix, or arion (which piggybacks on NixOS instead of rewriting the service library again). Maybe more?\r\nYou might want to draw the line. They're just custom service managers for which this RFC proposes to create a common interface. If someone has a repo for services on S6, would that change the design? Probably not.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-06T15:09:12Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.\n+\n+For many kinds of services (e.g. PostgreSQL, Apache HTTP server etc), it\n+typically suffices to only have a single instance running on a machine. For\n+microservices, it may actually be quite useful to have the ability to easily\n+create multiple instances. For testing/experimentation purposes it may also be\n+desired to run multiple instances of a system service, such as a web server.\n+\n+It is also not easily possible in NixOS to switch to alternative implementations\n+of a service. For many services, alternative implementations (e.g. `cron`,\n+`ssh`) are not required, but some services accept complex configurations, such\n+as the Apache HTTP server and Apache Tomcat.\n+\n+Finding a universal abstraction in NixOS to facilitate all possible use cases\n+may be impossible. Moreover, it may also be desired for developers to write\n+their own abstractions on top of existing functionality.\n+\n+To support abstractions and multiple instances, the Nix process management\n+framework does not use the NixOS module system, but a convention that is based\n+on the organisation of \"ordinary\" Nix packages in the Nixpkgs repository.\n+\n+In Nixpkgs, most package follow the convention that a separate Nix file defines\n+a function in which the function header refers to the build-time dependencies to\n+construct a package from source. Packages are composed in the top-level Nix\n+expression (`all-packages.nix`) that invokes these functions with the required\n+build-time parameters. For most packages, only a single variant is composed, but\n+it is also possible to build different kinds of package variants by changing the\n+function parameters.\n+\n+The Nix process management framework follows the same principles and extends the\n+Nixpkgs convention with a number of additional concepts:\n+\n+* Every service is instantiated from a *constructor* which is a nested function:\n+  the outer function refers to the build-time dependencies that are required to\n+  build the service from source code.\n+* The inner function refers to the service *instance parameters* -- calling the\n+  constructor with different parameters makes it possible to compose a service\n+  with an alternative configuration and in such a way that they do not conflict\n+  with other instances.\n+* The build-time dependencies of constructor functions are composed in a Nix\n+  expression that is typically called: `constructors.nix` in which every\n+  constructor function is called with its required build-time dependency\n+  parameters.\n+* The process instances of a system are composed in a Nix expression that is\n+  typically called: `processes.nix` in which every constructor function is\n+  called with its required instance parameters.\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    chown ${user}:${group} ${socketDir}\n+    chown ${user}:${group} ${dataDir}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        su ${user} -c '${postgresql}/bin/initdb -D ${dataDir} --no-locale'\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a constructor function for PostgreSQL and has the\n+following properties:\n+\n+* The outer function-header (first line) refer to the build inputs of the\n+  service. For example, to construct a configuration from a high-level\n+  specification for any supported process manager, we propagate the\n+  `createManagedProcess` as a parameter.\n+* The inner-function header refers to the instance parameters of a PostgreSQL\n+  instance. When it is desired to have two instances deployed to the same\n+  machine we must make sure that there is no conflict between the state of\n+  the two instances. Avoiding a conflict is possible by assigning a unique TCP\n+  port number and making sure that the state directories do not conflict. The\n+  `instanceName` parameter is appended to the name of the state directories to\n+  make this possible.\n+* In the body of the inner function, we invoke `createManagedProcess` to\n+  agnostically generate a configuration for a supported process manager.\n+  In the configuration, we provide an initialization script and we call the\n+  `postgres` service with its required command-line parameters and environment\n+  variables.\n+\n+As explained earlier, a constructor function is a nested function that we need to compose\n+twice. The build-time dependencies are composed in a Nix expression typically called\n+`constructors.nix`:\n+\n+```nix\n+{ nix-processmgmt ? ../../nix-processmgmt\n+, pkgs\n+, stateDir\n+, logDir\n+, runtimeDir\n+, cacheDir\n+, spoolDir\n+, libDir\n+, tmpDir\n+, callingUser ? null\n+, callingGroup ? null\n+, processManager\n+, ids ? {}\n+}:\n+\n+let\n+  createManagedProcess = import \"${nix-processmgmt}/nixproc/create-managed-process/universal/create-managed-process-universal.nix\" {\n+    inherit pkgs runtimeDir stateDir logDir tmpDir processManager ids;\n+  };\n+in\n+{\n+  postgresql = import ./postgresql {\n+    inherit createManagedProcess stateDir runtimeDir;\n+    inherit (pkgs) lib postgresql su;\n+  };\n+}\n+```\n+\n+In the above Nix expression, we invoke the `postgresql` constructor function and\n+we propagated its required build-time dependencies, such as the `postgresql`\n+package and the `createManagedProcess` abstraction function.\n+\n+The instance parameters are composed in a Nix expression that is typically called\n+`processes.nix`:\n+\n+```nix\n+{ pkgs ? import <nixpkgs> { inherit system; }\n+, system ? builtins.currentSystem\n+, stateDir ? \"/var\"\n+, runtimeDir ? \"${stateDir}/run\"\n+, logDir ? \"${stateDir}/log\"\n+, spoolDir ? \"${stateDir}/spool\"\n+, cacheDir ? \"${stateDir}/cache\"\n+, libDir ? \"${stateDir}/lib\"\n+, tmpDir ? (if stateDir == \"/var\" then \"/tmp\" else \"${stateDir}/tmp\")\n+, processManager\n+, nix-processmgmt ? ../../../nix-processmgmt\n+}:\n+\n+let\n+  constructors = import ../../services-agnostic/constructors.nix {\n+    inherit pkgs stateDir runtimeDir logDir tmpDir cacheDir spoolDir libDir processManager nix-processmgmt;\n+  };\n+in\n+rec {\n+  postgresql = rec {\n+    port = 5432;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+    };\n+  };\n+\n+  postgresql-secondary = rec {\n+    port = 5433;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+      instanceSuffix = \"-secondary\";\n+    };\n+  };\n+}\n+```\n+\n+In the above Nix expression, we compose two PostgreSQL instances that run on the\n+same machine simultaneously. This is possible due to the fact that they use\n+instance parameters that avoid their states to conflict.\n+\n+The above expression imports the `constructors.nix` expression so that each\n+process instance can be composed from the PostgreSQL constructor function.\n+\n+In NixOS, we use the NixOS module system that makes it possible to define\n+multiple aspects of a system in a module (such as packages, config files,\n+systemd units and environment variables) and combine the aspects of each module\n+into a single configuration from which a complete system is deployed, which is\n+very powerful.\n+\n+Combining the conventions of the nix-processmgmt framework with the NixOS module\n+system is something that still needs to be discussed. There are variety of\n+options possible, such as:\n+\n+* Combining the convention with process instances as described earlier by\n+  referring to a process instance from a NixOS module, similar to how NixOS\n+  modules can also refer to ordinary Nix packages\n+* Using sub modules, in which every sub module can define its own variant of a\n+  service.\n+\n+### Unprivileged user deployments\n+\n+Another interesting feature of Nix is to allow unprivileged users to deploy\n+packages. With services, this is also possible, but there are some things we\n+need to take into account, such as:\n+\n+* The state directories must refer to a directory that has writable permissions\n+  for the user\n+* We cannot bind to a TCP/UDP port below 1024\n+\n+We can also introduce a configuration flag and configure a service in such a way\n+that the above criteria are met:\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir, forceDisableUserChange}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    ${lib.optionalString (!forceDisableUserChange) ''\n+      chown ${user}:${group} ${socketDir}\n+      chown ${user}:${group} ${dataDir}\n+    ''}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        ${lib.optionalString (!forceDisableUserChange) \"su ${user} -c '\"}${postgresql}/bin/initdb -D ${dataDir} --no-locale${lib.optionalString (!forceDisableUserChange) \"'\"}\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a revised PostgreSQL constructor function that\n+accepts a new parameter: `forceDisableUserChange`. When this property is\n+enabled, the service configured in such a way that it does not change users,\n+which an unprivileged user is not allowed to do. With this setting enabled, it\n+becomes possible for an unprivileged user to deploy PostgreSQL.\n+\n+# Examples and Interactions\n+[examples-and-interactions]: #examples-and-interactions\n+\n+The `processes.nix` expression shown earlier can be used with a variety of tools\n+to deploy running process instances.\n+\n+The following command deploys the configurations as `sysvinit` scripts and\n+activates the configuration automatically:\n+\n+```bash\n+$ nixproc-sysvinit-switch processes.nix\n+```\n+\n+We can deploy the same configuration as systemd units with the following command:\n+\n+```bash\n+$ nixproc-systemd-switch processes.nix\n+```\n+\n+Or with `supervisord` with the following command:\n+\n+```bash\n+$ nixproc-supervisord-switch processes.nix\n+```\n+\n+As an unprivileged user, we can use the `--state-dir` parameter to change the\n+prefix of the state directories to reside in a user's home directory\n+(`/home/sander/var`) and disable user changing:\n+\n+```bash\n+$ nixproc-sysvinit-switch --state-dir /home/sander/var --force-disable-user-change processes.nix\n+```\n+\n+# Drawbacks\n+[drawbacks]: #drawbacks\n+\n+The ideas describes in this RFC makes the Nixpkgs eco-system more powerful, but\n+these new features come at at a price:\n+\n+* Using high-level abstractions hide the low-level configuration details of\n+  specific process managers. This makes it sometimes more difficult to reason\n+  about what is happening in problematic cases\n+* More testing is required -- we need to test services in multiple deployment\n+  scenarios, which is more expensive. Fortunately, I have seen that testing for\n+  two kinds of targets often give enough certainty: if you test with a\n+  foreground process (e.g. `systemd`) and a daemon (e.g. `sysvinit`) there is a\n+  big chance that it will work with other process managers as well.\n+* You may target the lowest common denominator. This is not actually true,\n+  because with overrides it is still possible to use any feature of a process\n+  manager. Moreover, not all services need to be portable -- low-level services\n+  such as `cups` and `syslogd` can still be packaged in a process\n+  manager-specific way.\n+\n+# Alternatives\n+[alternatives]: #alternatives\n+\n+This RFC proposes a new strategy to allow multiple process instances of the\n+same service to co-exist. Co-existence is possible by avoiding conflicts --\n+making sure that state directories and port numbers do not conflict through\n+function parameters.\n+\n+It is also possible to isolate services by using concepts such as Linux\n+namespaces. Then it is not required to avoid certain kinds of conflicts.\n+Unfortunately, using these features is restricted to Linux only -- this RFC aims\n+to be operating system agnostic.\n+\n+# Prior art\n+[prior-art]: #prior-art",
   "id": 1348852092,
   "in_reply_to_id": 1347856747,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 849,
   "original_position": 849,
   "original_start_line": null,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1662092682,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": null,
   "start_side": null,
   "subject_type": "line",
   "updated_at": "2023-10-06T15:09:13Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> portability _as a policy_.\r\n\r\nIt's a policy-free package manager ;)\r\n\r\nThis kind of thing is more a matter of resources, prioritization (usually by those who have the resources), and a hint of perseverence.\r\n\r\nRecently we did merge https://github.com/NixOS/nix/pull/8887 but that's really just a starting point, an empty space for others to contribute and nowhere near a commitment or policy. If someone can seriously help out, please do.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-06T15:20:41Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.",
   "id": 1348867114,
   "in_reply_to_id": 1348780418,
   "line": 39,
   "original_commit_id": "dbd38c2cd817cfaa971c677be177bca91f37ee71",
   "original_line": 39,
   "original_position": 39,
   "original_start_line": 37,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 39,
   "pull_request_review_id": 1662119007,
   "reactions": {
    "+1": 1,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 1
   },
   "side": "RIGHT",
   "start_line": 37,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-06T15:20:41Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I've expanded this section a bit. The reason why I didn't add my own projects is because they are only loosely coupled to process management -- in its current state, whenever Disnix needs to manage processes it invokes the Nix process management framework for that purpose.\r\n\r\nI have also added `devenv`.\r\n",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-06T17:27:57Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.\n+\n+For many kinds of services (e.g. PostgreSQL, Apache HTTP server etc), it\n+typically suffices to only have a single instance running on a machine. For\n+microservices, it may actually be quite useful to have the ability to easily\n+create multiple instances. For testing/experimentation purposes it may also be\n+desired to run multiple instances of a system service, such as a web server.\n+\n+It is also not easily possible in NixOS to switch to alternative implementations\n+of a service. For many services, alternative implementations (e.g. `cron`,\n+`ssh`) are not required, but some services accept complex configurations, such\n+as the Apache HTTP server and Apache Tomcat.\n+\n+Finding a universal abstraction in NixOS to facilitate all possible use cases\n+may be impossible. Moreover, it may also be desired for developers to write\n+their own abstractions on top of existing functionality.\n+\n+To support abstractions and multiple instances, the Nix process management\n+framework does not use the NixOS module system, but a convention that is based\n+on the organisation of \"ordinary\" Nix packages in the Nixpkgs repository.\n+\n+In Nixpkgs, most package follow the convention that a separate Nix file defines\n+a function in which the function header refers to the build-time dependencies to\n+construct a package from source. Packages are composed in the top-level Nix\n+expression (`all-packages.nix`) that invokes these functions with the required\n+build-time parameters. For most packages, only a single variant is composed, but\n+it is also possible to build different kinds of package variants by changing the\n+function parameters.\n+\n+The Nix process management framework follows the same principles and extends the\n+Nixpkgs convention with a number of additional concepts:\n+\n+* Every service is instantiated from a *constructor* which is a nested function:\n+  the outer function refers to the build-time dependencies that are required to\n+  build the service from source code.\n+* The inner function refers to the service *instance parameters* -- calling the\n+  constructor with different parameters makes it possible to compose a service\n+  with an alternative configuration and in such a way that they do not conflict\n+  with other instances.\n+* The build-time dependencies of constructor functions are composed in a Nix\n+  expression that is typically called: `constructors.nix` in which every\n+  constructor function is called with its required build-time dependency\n+  parameters.\n+* The process instances of a system are composed in a Nix expression that is\n+  typically called: `processes.nix` in which every constructor function is\n+  called with its required instance parameters.\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    chown ${user}:${group} ${socketDir}\n+    chown ${user}:${group} ${dataDir}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        su ${user} -c '${postgresql}/bin/initdb -D ${dataDir} --no-locale'\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a constructor function for PostgreSQL and has the\n+following properties:\n+\n+* The outer function-header (first line) refer to the build inputs of the\n+  service. For example, to construct a configuration from a high-level\n+  specification for any supported process manager, we propagate the\n+  `createManagedProcess` as a parameter.\n+* The inner-function header refers to the instance parameters of a PostgreSQL\n+  instance. When it is desired to have two instances deployed to the same\n+  machine we must make sure that there is no conflict between the state of\n+  the two instances. Avoiding a conflict is possible by assigning a unique TCP\n+  port number and making sure that the state directories do not conflict. The\n+  `instanceName` parameter is appended to the name of the state directories to\n+  make this possible.\n+* In the body of the inner function, we invoke `createManagedProcess` to\n+  agnostically generate a configuration for a supported process manager.\n+  In the configuration, we provide an initialization script and we call the\n+  `postgres` service with its required command-line parameters and environment\n+  variables.\n+\n+As explained earlier, a constructor function is a nested function that we need to compose\n+twice. The build-time dependencies are composed in a Nix expression typically called\n+`constructors.nix`:\n+\n+```nix\n+{ nix-processmgmt ? ../../nix-processmgmt\n+, pkgs\n+, stateDir\n+, logDir\n+, runtimeDir\n+, cacheDir\n+, spoolDir\n+, libDir\n+, tmpDir\n+, callingUser ? null\n+, callingGroup ? null\n+, processManager\n+, ids ? {}\n+}:\n+\n+let\n+  createManagedProcess = import \"${nix-processmgmt}/nixproc/create-managed-process/universal/create-managed-process-universal.nix\" {\n+    inherit pkgs runtimeDir stateDir logDir tmpDir processManager ids;\n+  };\n+in\n+{\n+  postgresql = import ./postgresql {\n+    inherit createManagedProcess stateDir runtimeDir;\n+    inherit (pkgs) lib postgresql su;\n+  };\n+}\n+```\n+\n+In the above Nix expression, we invoke the `postgresql` constructor function and\n+we propagated its required build-time dependencies, such as the `postgresql`\n+package and the `createManagedProcess` abstraction function.\n+\n+The instance parameters are composed in a Nix expression that is typically called\n+`processes.nix`:\n+\n+```nix\n+{ pkgs ? import <nixpkgs> { inherit system; }\n+, system ? builtins.currentSystem\n+, stateDir ? \"/var\"\n+, runtimeDir ? \"${stateDir}/run\"\n+, logDir ? \"${stateDir}/log\"\n+, spoolDir ? \"${stateDir}/spool\"\n+, cacheDir ? \"${stateDir}/cache\"\n+, libDir ? \"${stateDir}/lib\"\n+, tmpDir ? (if stateDir == \"/var\" then \"/tmp\" else \"${stateDir}/tmp\")\n+, processManager\n+, nix-processmgmt ? ../../../nix-processmgmt\n+}:\n+\n+let\n+  constructors = import ../../services-agnostic/constructors.nix {\n+    inherit pkgs stateDir runtimeDir logDir tmpDir cacheDir spoolDir libDir processManager nix-processmgmt;\n+  };\n+in\n+rec {\n+  postgresql = rec {\n+    port = 5432;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+    };\n+  };\n+\n+  postgresql-secondary = rec {\n+    port = 5433;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+      instanceSuffix = \"-secondary\";\n+    };\n+  };\n+}\n+```\n+\n+In the above Nix expression, we compose two PostgreSQL instances that run on the\n+same machine simultaneously. This is possible due to the fact that they use\n+instance parameters that avoid their states to conflict.\n+\n+The above expression imports the `constructors.nix` expression so that each\n+process instance can be composed from the PostgreSQL constructor function.\n+\n+In NixOS, we use the NixOS module system that makes it possible to define\n+multiple aspects of a system in a module (such as packages, config files,\n+systemd units and environment variables) and combine the aspects of each module\n+into a single configuration from which a complete system is deployed, which is\n+very powerful.\n+\n+Combining the conventions of the nix-processmgmt framework with the NixOS module\n+system is something that still needs to be discussed. There are variety of\n+options possible, such as:\n+\n+* Combining the convention with process instances as described earlier by\n+  referring to a process instance from a NixOS module, similar to how NixOS\n+  modules can also refer to ordinary Nix packages\n+* Using sub modules, in which every sub module can define its own variant of a\n+  service.\n+\n+### Unprivileged user deployments\n+\n+Another interesting feature of Nix is to allow unprivileged users to deploy\n+packages. With services, this is also possible, but there are some things we\n+need to take into account, such as:\n+\n+* The state directories must refer to a directory that has writable permissions\n+  for the user\n+* We cannot bind to a TCP/UDP port below 1024\n+\n+We can also introduce a configuration flag and configure a service in such a way\n+that the above criteria are met:\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir, forceDisableUserChange}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    ${lib.optionalString (!forceDisableUserChange) ''\n+      chown ${user}:${group} ${socketDir}\n+      chown ${user}:${group} ${dataDir}\n+    ''}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        ${lib.optionalString (!forceDisableUserChange) \"su ${user} -c '\"}${postgresql}/bin/initdb -D ${dataDir} --no-locale${lib.optionalString (!forceDisableUserChange) \"'\"}\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a revised PostgreSQL constructor function that\n+accepts a new parameter: `forceDisableUserChange`. When this property is\n+enabled, the service configured in such a way that it does not change users,\n+which an unprivileged user is not allowed to do. With this setting enabled, it\n+becomes possible for an unprivileged user to deploy PostgreSQL.\n+\n+# Examples and Interactions\n+[examples-and-interactions]: #examples-and-interactions\n+\n+The `processes.nix` expression shown earlier can be used with a variety of tools\n+to deploy running process instances.\n+\n+The following command deploys the configurations as `sysvinit` scripts and\n+activates the configuration automatically:\n+\n+```bash\n+$ nixproc-sysvinit-switch processes.nix\n+```\n+\n+We can deploy the same configuration as systemd units with the following command:\n+\n+```bash\n+$ nixproc-systemd-switch processes.nix\n+```\n+\n+Or with `supervisord` with the following command:\n+\n+```bash\n+$ nixproc-supervisord-switch processes.nix\n+```\n+\n+As an unprivileged user, we can use the `--state-dir` parameter to change the\n+prefix of the state directories to reside in a user's home directory\n+(`/home/sander/var`) and disable user changing:\n+\n+```bash\n+$ nixproc-sysvinit-switch --state-dir /home/sander/var --force-disable-user-change processes.nix\n+```\n+\n+# Drawbacks\n+[drawbacks]: #drawbacks\n+\n+The ideas describes in this RFC makes the Nixpkgs eco-system more powerful, but\n+these new features come at at a price:\n+\n+* Using high-level abstractions hide the low-level configuration details of\n+  specific process managers. This makes it sometimes more difficult to reason\n+  about what is happening in problematic cases\n+* More testing is required -- we need to test services in multiple deployment\n+  scenarios, which is more expensive. Fortunately, I have seen that testing for\n+  two kinds of targets often give enough certainty: if you test with a\n+  foreground process (e.g. `systemd`) and a daemon (e.g. `sysvinit`) there is a\n+  big chance that it will work with other process managers as well.\n+* You may target the lowest common denominator. This is not actually true,\n+  because with overrides it is still possible to use any feature of a process\n+  manager. Moreover, not all services need to be portable -- low-level services\n+  such as `cups` and `syslogd` can still be packaged in a process\n+  manager-specific way.\n+\n+# Alternatives\n+[alternatives]: #alternatives\n+\n+This RFC proposes a new strategy to allow multiple process instances of the\n+same service to co-exist. Co-existence is possible by avoiding conflicts --\n+making sure that state directories and port numbers do not conflict through\n+function parameters.\n+\n+It is also possible to isolate services by using concepts such as Linux\n+namespaces. Then it is not required to avoid certain kinds of conflicts.\n+Unfortunately, using these features is restricted to Linux only -- this RFC aims\n+to be operating system agnostic.\n+\n+# Prior art\n+[prior-art]: #prior-art",
   "id": 1349122810,
   "in_reply_to_id": 1347856747,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 849,
   "original_position": 849,
   "original_start_line": null,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1662498738,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": null,
   "start_side": null,
   "subject_type": "line",
   "updated_at": "2023-10-06T17:27:57Z",
   "user": {
    "id": 1153271,
    "login": "svanderburg",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Nix does not have an official policy to support other platforms than Linux and macOS/Darwin, but the reason is just simply because of a lack of resources: we don't have manpower and resources (e.g. Hydra build machines) to effectively maintain other ports. On the other hand, Nix does not restrict other platforms and accepts contributions for other platforms.\r\n\r\nI myself, for example, have contributed fixes for exotic platforms in several occasions.\r\n\r\nIf the user base for other platforms grows, then there is little reason to ignore them.\r\n\r\nFrom a historic perspective -- in the early days e.g. 2009-2012 we had all kinds of exotic build machines in our Hydra cluster at Delft University of Technology, such as `FreeBSD`, `OpenBSD` and `OpenSolaris`. Nix could be used on all of these operating systems. Moreover, in the `stdenv/` directory of Nixpkgs you can still find traces of code that makes it possible to build stuff on such platforms.\r\n",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-06T17:46:00Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.",
   "id": 1349145724,
   "in_reply_to_id": 1348780418,
   "line": 39,
   "original_commit_id": "dbd38c2cd817cfaa971c677be177bca91f37ee71",
   "original_line": 39,
   "original_position": 39,
   "original_start_line": 37,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 39,
   "pull_request_review_id": 1662531554,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 37,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-06T17:46:00Z",
   "user": {
    "id": 1153271,
    "login": "svanderburg",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I think I prefer something closer to @roberth's, because explicitly collecting all the instances in one place is also not terribly modular. (Feels to SQL like me, with out enough type vs table distinction.)",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-06T19:25:39Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.",
   "id": 1349233687,
   "in_reply_to_id": 1347854623,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 486,
   "original_position": 486,
   "original_start_line": 482,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1662669660,
   "reactions": {
    "+1": 9,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 9
   },
   "side": "RIGHT",
   "start_line": 482,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-06T19:25:39Z",
   "user": {
    "id": 1055245,
    "login": "Ericson2314",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Why not group the \"best options\"  and say \"Aside from the last two options...\"? ",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-07T17:42:10Z",
   "diff_hunk": "@@ -872,9 +872,19 @@ solution:\n * [system-manager](https://github.com/numtide/system-manager) addresses the\n   limitation of NixOS to use systemd units independently and can deploy many\n   services in the NixOS repository on any Ubuntu-based distribution.\n-\n-Aside from the first option, the biggest drawback is that these solutions are\n-not generic and there is only little reuse possible between service\n+* [devenv](https://devenv.sh) also makes it possible to run multiple processes\n+  (of different services) in a Nix-based development environment. It works a\n+  with a variety container-friendly/non-PID-1 based process managers, such as\n+  process-compose, overmind, and honcho.\n+* [Dysnomia](https://github.com/svanderburg/dysnomia) is a system to manage the\n+  life-cycle of services deployed by\n+  [Disnix](https://github.com/svanderburg/disnix) in a generic way. It has its\n+  own `process` module that deploys services as daemons running the background.\n+  It can also use the Nix process management framework to deploy services that\n+  are managed by any kind process manager supported by the framework.\n+\n+Aside from the first and last option, the biggest drawback is that these",
   "id": 1349550740,
   "line": null,
   "original_commit_id": "1776c5854323ab0f2d5d384a096753f7540fccf3",
   "original_line": 886,
   "original_position": 18,
   "original_start_line": null,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1663085271,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": null,
   "start_side": null,
   "subject_type": "line",
   "updated_at": "2023-10-07T17:42:17Z",
   "user": {
    "id": 5954806,
    "login": "AndersonTorres",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "```suggestion\r\n* [NixNG](https://github.com/nix-community/NixNG) is considered a late sibling to NixOS and shares many designs of it. It also implements the function of generating services for several kinds of init systems from the same nix expressions.\r\n* [nixos-init-freedom](https://git.sr.ht/~guido/nixos-init-freedom) puts some efforts on replacing systemd-as-pid-1 with s6. Instead of implementing a generic service layer, it translates parts of systemd module configuration into s6 configuration.\r\n```",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-08T03:38:26Z",
   "diff_hunk": "@@ -0,0 +1,903 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.\n+\n+For many kinds of services (e.g. PostgreSQL, Apache HTTP server etc), it\n+typically suffices to only have a single instance running on a machine. For\n+microservices, it may actually be quite useful to have the ability to easily\n+create multiple instances. For testing/experimentation purposes it may also be\n+desired to run multiple instances of a system service, such as a web server.\n+\n+It is also not easily possible in NixOS to switch to alternative implementations\n+of a service. For many services, alternative implementations (e.g. `cron`,\n+`ssh`) are not required, but some services accept complex configurations, such\n+as the Apache HTTP server and Apache Tomcat.\n+\n+Finding a universal abstraction in NixOS to facilitate all possible use cases\n+may be impossible. Moreover, it may also be desired for developers to write\n+their own abstractions on top of existing functionality.\n+\n+To support abstractions and multiple instances, the Nix process management\n+framework does not use the NixOS module system, but a convention that is based\n+on the organisation of \"ordinary\" Nix packages in the Nixpkgs repository.\n+\n+In Nixpkgs, most package follow the convention that a separate Nix file defines\n+a function in which the function header refers to the build-time dependencies to\n+construct a package from source. Packages are composed in the top-level Nix\n+expression (`all-packages.nix`) that invokes these functions with the required\n+build-time parameters. For most packages, only a single variant is composed, but\n+it is also possible to build different kinds of package variants by changing the\n+function parameters.\n+\n+The Nix process management framework follows the same principles and extends the\n+Nixpkgs convention with a number of additional concepts:\n+\n+* Every service is instantiated from a *constructor* which is a nested function:\n+  the outer function refers to the build-time dependencies that are required to\n+  build the service from source code.\n+* The inner function refers to the service *instance parameters* -- calling the\n+  constructor with different parameters makes it possible to compose a service\n+  with an alternative configuration and in such a way that they do not conflict\n+  with other instances.\n+* The build-time dependencies of constructor functions are composed in a Nix\n+  expression that is typically called: `constructors.nix` in which every\n+  constructor function is called with its required build-time dependency\n+  parameters.\n+* The process instances of a system are composed in a Nix expression that is\n+  typically called: `processes.nix` in which every constructor function is\n+  called with its required instance parameters.\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    chown ${user}:${group} ${socketDir}\n+    chown ${user}:${group} ${dataDir}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        su ${user} -c '${postgresql}/bin/initdb -D ${dataDir} --no-locale'\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a constructor function for PostgreSQL and has the\n+following properties:\n+\n+* The outer function-header (first line) refer to the build inputs of the\n+  service. For example, to construct a configuration from a high-level\n+  specification for any supported process manager, we propagate the\n+  `createManagedProcess` as a parameter.\n+* The inner-function header refers to the instance parameters of a PostgreSQL\n+  instance. When it is desired to have two instances deployed to the same\n+  machine we must make sure that there is no conflict between the state of\n+  the two instances. Avoiding a conflict is possible by assigning a unique TCP\n+  port number and making sure that the state directories do not conflict. The\n+  `instanceName` parameter is appended to the name of the state directories to\n+  make this possible.\n+* In the body of the inner function, we invoke `createManagedProcess` to\n+  agnostically generate a configuration for a supported process manager.\n+  In the configuration, we provide an initialization script and we call the\n+  `postgres` service with its required command-line parameters and environment\n+  variables.\n+\n+As explained earlier, a constructor function is a nested function that we need to compose\n+twice. The build-time dependencies are composed in a Nix expression typically called\n+`constructors.nix`:\n+\n+```nix\n+{ nix-processmgmt ? ../../nix-processmgmt\n+, pkgs\n+, stateDir\n+, logDir\n+, runtimeDir\n+, cacheDir\n+, spoolDir\n+, libDir\n+, tmpDir\n+, callingUser ? null\n+, callingGroup ? null\n+, processManager\n+, ids ? {}\n+}:\n+\n+let\n+  createManagedProcess = import \"${nix-processmgmt}/nixproc/create-managed-process/universal/create-managed-process-universal.nix\" {\n+    inherit pkgs runtimeDir stateDir logDir tmpDir processManager ids;\n+  };\n+in\n+{\n+  postgresql = import ./postgresql {\n+    inherit createManagedProcess stateDir runtimeDir;\n+    inherit (pkgs) lib postgresql su;\n+  };\n+}\n+```\n+\n+In the above Nix expression, we invoke the `postgresql` constructor function and\n+we propagated its required build-time dependencies, such as the `postgresql`\n+package and the `createManagedProcess` abstraction function.\n+\n+The instance parameters are composed in a Nix expression that is typically called\n+`processes.nix`:\n+\n+```nix\n+{ pkgs ? import <nixpkgs> { inherit system; }\n+, system ? builtins.currentSystem\n+, stateDir ? \"/var\"\n+, runtimeDir ? \"${stateDir}/run\"\n+, logDir ? \"${stateDir}/log\"\n+, spoolDir ? \"${stateDir}/spool\"\n+, cacheDir ? \"${stateDir}/cache\"\n+, libDir ? \"${stateDir}/lib\"\n+, tmpDir ? (if stateDir == \"/var\" then \"/tmp\" else \"${stateDir}/tmp\")\n+, processManager\n+, nix-processmgmt ? ../../../nix-processmgmt\n+}:\n+\n+let\n+  constructors = import ../../services-agnostic/constructors.nix {\n+    inherit pkgs stateDir runtimeDir logDir tmpDir cacheDir spoolDir libDir processManager nix-processmgmt;\n+  };\n+in\n+rec {\n+  postgresql = rec {\n+    port = 5432;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+    };\n+  };\n+\n+  postgresql-secondary = rec {\n+    port = 5433;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+      instanceSuffix = \"-secondary\";\n+    };\n+  };\n+}\n+```\n+\n+In the above Nix expression, we compose two PostgreSQL instances that run on the\n+same machine simultaneously. This is possible due to the fact that they use\n+instance parameters that avoid their states to conflict.\n+\n+The above expression imports the `constructors.nix` expression so that each\n+process instance can be composed from the PostgreSQL constructor function.\n+\n+In NixOS, we use the NixOS module system that makes it possible to define\n+multiple aspects of a system in a module (such as packages, config files,\n+systemd units and environment variables) and combine the aspects of each module\n+into a single configuration from which a complete system is deployed, which is\n+very powerful.\n+\n+Combining the conventions of the nix-processmgmt framework with the NixOS module\n+system is something that still needs to be discussed. There are variety of\n+options possible, such as:\n+\n+* Combining the convention with process instances as described earlier by\n+  referring to a process instance from a NixOS module, similar to how NixOS\n+  modules can also refer to ordinary Nix packages\n+* Using sub modules, in which every sub module can define its own variant of a\n+  service.\n+\n+### Unprivileged user deployments\n+\n+Another interesting feature of Nix is to allow unprivileged users to deploy\n+packages. With services, this is also possible, but there are some things we\n+need to take into account, such as:\n+\n+* The state directories must refer to a directory that has writable permissions\n+  for the user\n+* We cannot bind to a TCP/UDP port below 1024\n+\n+We can also introduce a configuration flag and configure a service in such a way\n+that the above criteria are met:\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir, forceDisableUserChange}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    ${lib.optionalString (!forceDisableUserChange) ''\n+      chown ${user}:${group} ${socketDir}\n+      chown ${user}:${group} ${dataDir}\n+    ''}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        ${lib.optionalString (!forceDisableUserChange) \"su ${user} -c '\"}${postgresql}/bin/initdb -D ${dataDir} --no-locale${lib.optionalString (!forceDisableUserChange) \"'\"}\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a revised PostgreSQL constructor function that\n+accepts a new parameter: `forceDisableUserChange`. When this property is\n+enabled, the service configured in such a way that it does not change users,\n+which an unprivileged user is not allowed to do. With this setting enabled, it\n+becomes possible for an unprivileged user to deploy PostgreSQL.\n+\n+# Examples and Interactions\n+[examples-and-interactions]: #examples-and-interactions\n+\n+The `processes.nix` expression shown earlier can be used with a variety of tools\n+to deploy running process instances.\n+\n+The following command deploys the configurations as `sysvinit` scripts and\n+activates the configuration automatically:\n+\n+```bash\n+$ nixproc-sysvinit-switch processes.nix\n+```\n+\n+We can deploy the same configuration as systemd units with the following command:\n+\n+```bash\n+$ nixproc-systemd-switch processes.nix\n+```\n+\n+Or with `supervisord` with the following command:\n+\n+```bash\n+$ nixproc-supervisord-switch processes.nix\n+```\n+\n+As an unprivileged user, we can use the `--state-dir` parameter to change the\n+prefix of the state directories to reside in a user's home directory\n+(`/home/sander/var`) and disable user changing:\n+\n+```bash\n+$ nixproc-sysvinit-switch --state-dir /home/sander/var --force-disable-user-change processes.nix\n+```\n+\n+# Drawbacks\n+[drawbacks]: #drawbacks\n+\n+The ideas describes in this RFC makes the Nixpkgs eco-system more powerful, but\n+these new features come at at a price:\n+\n+* Using high-level abstractions hide the low-level configuration details of\n+  specific process managers. This makes it sometimes more difficult to reason\n+  about what is happening in problematic cases\n+* More testing is required -- we need to test services in multiple deployment\n+  scenarios, which is more expensive. Fortunately, I have seen that testing for\n+  two kinds of targets often give enough certainty: if you test with a\n+  foreground process (e.g. `systemd`) and a daemon (e.g. `sysvinit`) there is a\n+  big chance that it will work with other process managers as well.\n+* You may target the lowest common denominator. This is not actually true,\n+  because with overrides it is still possible to use any feature of a process\n+  manager. Moreover, not all services need to be portable -- low-level services\n+  such as `cups` and `syslogd` can still be packaged in a process\n+  manager-specific way.\n+\n+# Alternatives\n+[alternatives]: #alternatives\n+\n+This RFC proposes a new strategy to allow multiple process instances of the\n+same service to co-exist. Co-existence is possible by avoiding conflicts --\n+making sure that state directories and port numbers do not conflict through\n+function parameters.\n+\n+It is also possible to isolate services by using concepts such as Linux\n+namespaces. Then it is not required to avoid certain kinds of conflicts.\n+Unfortunately, using these features is restricted to Linux only -- this RFC aims\n+to be operating system agnostic.\n+\n+# Prior art\n+[prior-art]: #prior-art\n+\n+This RFC is not the first approach that combines Nix with a process management\n+solution:\n+\n+* The service deployment chapter (chapter 9) of\n+  [Eelco Dolstra's PhD thesis](https://github.com/edolstra/edolstra.github.io/blob/master/pubs/phd-thesis.pdf)\n+  describes a very early service deployment approach that uses generated\n+  start/stop scripts implemented in `bash`. Because shell scripts are so common,\n+  they can be used on a variety of operating systems -- in addition to Linux, it\n+  can also be used on Darwin and FreeBSD.\n+* [NixOS](https://nixos.org) uses `systemd` as its process management solution.\n+  Services can be deployed by generating systemd unit configuration files in a\n+  Nix expression. NixOS deploys `systemd` units globally and requires super\n+  user privileges to get deployed. Moreover, you cannot use the service layer\n+  independently from the rest of NixOS.\n+* [nix-darwin](https://github.com/LnL7/nix-darwin) provides various NixOS\n+  features on macOS/Darwin, including a service management layer that uses the\n+  process manager of macOS: `launchd`.\n+* [home-manager](https://github.com/nix-community/home-manager) offers a service\n+  layer that works with systemd user services, rather than system services. It\n+  contains its own set of service configurations that can be deployed as user\n+  services.\n+* [system-manager](https://github.com/numtide/system-manager) addresses the\n+  limitation of NixOS to use systemd units independently and can deploy many\n+  services in the NixOS repository on any Ubuntu-based distribution.\n+* [devenv](https://devenv.sh) also makes it possible to run multiple processes\n+  (of different services) in a Nix-based development environment. It works a\n+  with a variety container-friendly/non-PID-1 based process managers, such as\n+  process-compose, overmind, and honcho.\n+* [Dysnomia](https://github.com/svanderburg/dysnomia) is a system to manage the\n+  life-cycle of services deployed by\n+  [Disnix](https://github.com/svanderburg/disnix) in a generic way. It has its\n+  own `process` module that deploys services as daemons running the background.\n+  It can also use the Nix process management framework to deploy services that\n+  are managed by any kind process manager supported by the framework.\n+",
   "id": 1349616472,
   "line": null,
   "original_commit_id": "1776c5854323ab0f2d5d384a096753f7540fccf3",
   "original_line": 885,
   "original_position": 885,
   "original_start_line": null,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1663150918,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": null,
   "start_side": null,
   "subject_type": "line",
   "updated_at": "2023-10-08T19:42:52Z",
   "user": {
    "id": 42209822,
    "login": "Aleksanaa",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> Feels to SQL like me, with out enough type vs table distinction.\r\n\r\nDo you have a link to a page that expands on this issue? I at least am not familiar enough with SQL (or its intersection with type theory) to understand this remark.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-08T13:40:29Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.",
   "id": 1349699122,
   "in_reply_to_id": 1347854623,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 486,
   "original_position": 486,
   "original_start_line": 482,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1663238052,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 482,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-08T13:40:29Z",
   "user": {
    "id": 3662816,
    "login": "8573",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Pretty much something like that yes. One of the potential future ideas with `pkgs/by-name` was to use it not only for package builds, but also the package-specific modules. So in addition to the current `package.nix` file in each directory, there could be a `module.nix` file or so too. This would then also be automatically discovered in some way and added to NixOS (or as here, more generic process management).\r\n\r\nThe reason I'm suggesting it here is because I imagine we need some kind of migration from the current NixOS modules to more generic modules, and combining that with a transition to `pkgs/by-name` could work nicely. I think of `pkgs/by-name` as a sort of strictly type-checked part of Nixpkgs, where CI prevents you from e.g. committing `package.nix` files that aren't packages. Similarly it could check that `module.nix` is a module with specific options or so.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-08T21:27:56Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.\n+\n+For many kinds of services (e.g. PostgreSQL, Apache HTTP server etc), it\n+typically suffices to only have a single instance running on a machine. For\n+microservices, it may actually be quite useful to have the ability to easily\n+create multiple instances. For testing/experimentation purposes it may also be\n+desired to run multiple instances of a system service, such as a web server.\n+\n+It is also not easily possible in NixOS to switch to alternative implementations\n+of a service. For many services, alternative implementations (e.g. `cron`,\n+`ssh`) are not required, but some services accept complex configurations, such\n+as the Apache HTTP server and Apache Tomcat.\n+\n+Finding a universal abstraction in NixOS to facilitate all possible use cases\n+may be impossible. Moreover, it may also be desired for developers to write\n+their own abstractions on top of existing functionality.\n+\n+To support abstractions and multiple instances, the Nix process management\n+framework does not use the NixOS module system, but a convention that is based\n+on the organisation of \"ordinary\" Nix packages in the Nixpkgs repository.\n+\n+In Nixpkgs, most package follow the convention that a separate Nix file defines\n+a function in which the function header refers to the build-time dependencies to\n+construct a package from source. Packages are composed in the top-level Nix\n+expression (`all-packages.nix`) that invokes these functions with the required\n+build-time parameters. For most packages, only a single variant is composed, but\n+it is also possible to build different kinds of package variants by changing the\n+function parameters.\n+\n+The Nix process management framework follows the same principles and extends the\n+Nixpkgs convention with a number of additional concepts:\n+\n+* Every service is instantiated from a *constructor* which is a nested function:\n+  the outer function refers to the build-time dependencies that are required to\n+  build the service from source code.\n+* The inner function refers to the service *instance parameters* -- calling the\n+  constructor with different parameters makes it possible to compose a service\n+  with an alternative configuration and in such a way that they do not conflict\n+  with other instances.\n+* The build-time dependencies of constructor functions are composed in a Nix\n+  expression that is typically called: `constructors.nix` in which every\n+  constructor function is called with its required build-time dependency\n+  parameters.\n+* The process instances of a system are composed in a Nix expression that is\n+  typically called: `processes.nix` in which every constructor function is\n+  called with its required instance parameters.\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    chown ${user}:${group} ${socketDir}\n+    chown ${user}:${group} ${dataDir}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        su ${user} -c '${postgresql}/bin/initdb -D ${dataDir} --no-locale'\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a constructor function for PostgreSQL and has the\n+following properties:\n+\n+* The outer function-header (first line) refer to the build inputs of the\n+  service. For example, to construct a configuration from a high-level\n+  specification for any supported process manager, we propagate the\n+  `createManagedProcess` as a parameter.\n+* The inner-function header refers to the instance parameters of a PostgreSQL\n+  instance. When it is desired to have two instances deployed to the same\n+  machine we must make sure that there is no conflict between the state of\n+  the two instances. Avoiding a conflict is possible by assigning a unique TCP\n+  port number and making sure that the state directories do not conflict. The\n+  `instanceName` parameter is appended to the name of the state directories to\n+  make this possible.\n+* In the body of the inner function, we invoke `createManagedProcess` to\n+  agnostically generate a configuration for a supported process manager.\n+  In the configuration, we provide an initialization script and we call the\n+  `postgres` service with its required command-line parameters and environment\n+  variables.\n+\n+As explained earlier, a constructor function is a nested function that we need to compose\n+twice. The build-time dependencies are composed in a Nix expression typically called\n+`constructors.nix`:\n+\n+```nix\n+{ nix-processmgmt ? ../../nix-processmgmt\n+, pkgs\n+, stateDir\n+, logDir\n+, runtimeDir\n+, cacheDir\n+, spoolDir\n+, libDir\n+, tmpDir\n+, callingUser ? null\n+, callingGroup ? null\n+, processManager\n+, ids ? {}\n+}:\n+\n+let\n+  createManagedProcess = import \"${nix-processmgmt}/nixproc/create-managed-process/universal/create-managed-process-universal.nix\" {\n+    inherit pkgs runtimeDir stateDir logDir tmpDir processManager ids;\n+  };\n+in\n+{\n+  postgresql = import ./postgresql {\n+    inherit createManagedProcess stateDir runtimeDir;\n+    inherit (pkgs) lib postgresql su;\n+  };\n+}\n+```\n+\n+In the above Nix expression, we invoke the `postgresql` constructor function and\n+we propagated its required build-time dependencies, such as the `postgresql`\n+package and the `createManagedProcess` abstraction function.\n+\n+The instance parameters are composed in a Nix expression that is typically called\n+`processes.nix`:\n+\n+```nix\n+{ pkgs ? import <nixpkgs> { inherit system; }\n+, system ? builtins.currentSystem\n+, stateDir ? \"/var\"\n+, runtimeDir ? \"${stateDir}/run\"\n+, logDir ? \"${stateDir}/log\"\n+, spoolDir ? \"${stateDir}/spool\"\n+, cacheDir ? \"${stateDir}/cache\"\n+, libDir ? \"${stateDir}/lib\"\n+, tmpDir ? (if stateDir == \"/var\" then \"/tmp\" else \"${stateDir}/tmp\")\n+, processManager\n+, nix-processmgmt ? ../../../nix-processmgmt\n+}:\n+\n+let\n+  constructors = import ../../services-agnostic/constructors.nix {\n+    inherit pkgs stateDir runtimeDir logDir tmpDir cacheDir spoolDir libDir processManager nix-processmgmt;\n+  };\n+in\n+rec {\n+  postgresql = rec {\n+    port = 5432;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+    };\n+  };\n+\n+  postgresql-secondary = rec {\n+    port = 5433;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+      instanceSuffix = \"-secondary\";\n+    };\n+  };\n+}\n+```\n+\n+In the above Nix expression, we compose two PostgreSQL instances that run on the\n+same machine simultaneously. This is possible due to the fact that they use\n+instance parameters that avoid their states to conflict.\n+\n+The above expression imports the `constructors.nix` expression so that each\n+process instance can be composed from the PostgreSQL constructor function.\n+\n+In NixOS, we use the NixOS module system that makes it possible to define\n+multiple aspects of a system in a module (such as packages, config files,\n+systemd units and environment variables) and combine the aspects of each module\n+into a single configuration from which a complete system is deployed, which is\n+very powerful.\n+\n+Combining the conventions of the nix-processmgmt framework with the NixOS module\n+system is something that still needs to be discussed. There are variety of\n+options possible, such as:\n+\n+* Combining the convention with process instances as described earlier by\n+  referring to a process instance from a NixOS module, similar to how NixOS\n+  modules can also refer to ordinary Nix packages\n+* Using sub modules, in which every sub module can define its own variant of a\n+  service.\n+\n+### Unprivileged user deployments\n+\n+Another interesting feature of Nix is to allow unprivileged users to deploy\n+packages. With services, this is also possible, but there are some things we\n+need to take into account, such as:\n+\n+* The state directories must refer to a directory that has writable permissions\n+  for the user\n+* We cannot bind to a TCP/UDP port below 1024\n+\n+We can also introduce a configuration flag and configure a service in such a way\n+that the above criteria are met:\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir, forceDisableUserChange}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    ${lib.optionalString (!forceDisableUserChange) ''\n+      chown ${user}:${group} ${socketDir}\n+      chown ${user}:${group} ${dataDir}\n+    ''}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        ${lib.optionalString (!forceDisableUserChange) \"su ${user} -c '\"}${postgresql}/bin/initdb -D ${dataDir} --no-locale${lib.optionalString (!forceDisableUserChange) \"'\"}\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a revised PostgreSQL constructor function that\n+accepts a new parameter: `forceDisableUserChange`. When this property is\n+enabled, the service configured in such a way that it does not change users,\n+which an unprivileged user is not allowed to do. With this setting enabled, it\n+becomes possible for an unprivileged user to deploy PostgreSQL.\n+\n+# Examples and Interactions\n+[examples-and-interactions]: #examples-and-interactions\n+\n+The `processes.nix` expression shown earlier can be used with a variety of tools\n+to deploy running process instances.\n+\n+The following command deploys the configurations as `sysvinit` scripts and\n+activates the configuration automatically:\n+\n+```bash\n+$ nixproc-sysvinit-switch processes.nix\n+```\n+\n+We can deploy the same configuration as systemd units with the following command:\n+\n+```bash\n+$ nixproc-systemd-switch processes.nix\n+```\n+\n+Or with `supervisord` with the following command:\n+\n+```bash\n+$ nixproc-supervisord-switch processes.nix\n+```\n+\n+As an unprivileged user, we can use the `--state-dir` parameter to change the\n+prefix of the state directories to reside in a user's home directory\n+(`/home/sander/var`) and disable user changing:\n+\n+```bash\n+$ nixproc-sysvinit-switch --state-dir /home/sander/var --force-disable-user-change processes.nix\n+```\n+\n+# Drawbacks\n+[drawbacks]: #drawbacks\n+\n+The ideas describes in this RFC makes the Nixpkgs eco-system more powerful, but\n+these new features come at at a price:\n+\n+* Using high-level abstractions hide the low-level configuration details of\n+  specific process managers. This makes it sometimes more difficult to reason\n+  about what is happening in problematic cases\n+* More testing is required -- we need to test services in multiple deployment\n+  scenarios, which is more expensive. Fortunately, I have seen that testing for\n+  two kinds of targets often give enough certainty: if you test with a\n+  foreground process (e.g. `systemd`) and a daemon (e.g. `sysvinit`) there is a\n+  big chance that it will work with other process managers as well.\n+* You may target the lowest common denominator. This is not actually true,\n+  because with overrides it is still possible to use any feature of a process\n+  manager. Moreover, not all services need to be portable -- low-level services\n+  such as `cups` and `syslogd` can still be packaged in a process\n+  manager-specific way.\n+\n+# Alternatives\n+[alternatives]: #alternatives\n+\n+This RFC proposes a new strategy to allow multiple process instances of the\n+same service to co-exist. Co-existence is possible by avoiding conflicts --\n+making sure that state directories and port numbers do not conflict through\n+function parameters.\n+\n+It is also possible to isolate services by using concepts such as Linux\n+namespaces. Then it is not required to avoid certain kinds of conflicts.\n+Unfortunately, using these features is restricted to Linux only -- this RFC aims\n+to be operating system agnostic.\n+\n+# Prior art\n+[prior-art]: #prior-art\n+\n+This RFC is not the first approach that combines Nix with a process management\n+solution:\n+\n+* The service deployment chapter (chapter 9) of\n+  [Eelco Dolstra's PhD thesis](https://github.com/edolstra/edolstra.github.io/blob/master/pubs/phd-thesis.pdf)\n+  describes a very early service deployment approach that uses generated\n+  start/stop scripts implemented in `bash`. Because shell scripts are so common,\n+  they can be used on a variety of operating systems -- in addition to Linux, it\n+  can also be used on Darwin and FreeBSD.\n+* [NixOS](https://nixos.org) uses `systemd` as its process management solution.\n+  Services can be deployed by generating systemd unit configuration files in a\n+  Nix expression. NixOS deploys `systemd` units globally and requires super\n+  user privileges to get deployed. Moreover, you cannot use the service layer\n+  independently from the rest of NixOS.\n+* [nix-darwin](https://github.com/LnL7/nix-darwin) provides various NixOS\n+  features on macOS/Darwin, including a service management layer that uses the\n+  process manager of macOS: `launchd`.\n+* [home-manager](https://github.com/nix-community/home-manager) offers a service\n+  layer that works with systemd user services, rather than system services. It\n+  contains its own set of service configurations that can be deployed as user\n+  services.\n+* [system-manager](https://github.com/numtide/system-manager) addresses the\n+  limitation of NixOS to use systemd units independently and can deploy many\n+  services in the NixOS repository on any Ubuntu-based distribution.\n+\n+Aside from the first option, the biggest drawback is that these solutions are\n+not generic and there is only little reuse possible between service\n+configurations.\n+\n+# Unresolved questions\n+[unresolved]: #unresolved-questions\n+\n+The integration with NixOS module system still needs to be worked out in detail.\n+Currently, I can think of two scenarios. For this a discussion in the Nix\n+community is required.",
   "id": 1349779498,
   "in_reply_to_id": 1347861030,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 885,
   "original_position": 885,
   "original_start_line": 883,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1663603265,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 883,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-08T21:27:56Z",
   "user": {
    "id": 20525370,
    "login": "infinisil",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> It's a policy-free package manager ;)\r\n\r\nTechnically, there is RFC #46 for Nixpkgs. But I understood the point.\r\n",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-08T21:37:32Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.",
   "id": 1349780612,
   "in_reply_to_id": 1348780418,
   "line": 39,
   "original_commit_id": "dbd38c2cd817cfaa971c677be177bca91f37ee71",
   "original_line": 39,
   "original_position": 39,
   "original_start_line": 37,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 39,
   "pull_request_review_id": 1663604224,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 37,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-08T21:37:32Z",
   "user": {
    "id": 5954806,
    "login": "AndersonTorres",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I love it! Thanks for confirming.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-08T21:50:15Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.\n+\n+For many kinds of services (e.g. PostgreSQL, Apache HTTP server etc), it\n+typically suffices to only have a single instance running on a machine. For\n+microservices, it may actually be quite useful to have the ability to easily\n+create multiple instances. For testing/experimentation purposes it may also be\n+desired to run multiple instances of a system service, such as a web server.\n+\n+It is also not easily possible in NixOS to switch to alternative implementations\n+of a service. For many services, alternative implementations (e.g. `cron`,\n+`ssh`) are not required, but some services accept complex configurations, such\n+as the Apache HTTP server and Apache Tomcat.\n+\n+Finding a universal abstraction in NixOS to facilitate all possible use cases\n+may be impossible. Moreover, it may also be desired for developers to write\n+their own abstractions on top of existing functionality.\n+\n+To support abstractions and multiple instances, the Nix process management\n+framework does not use the NixOS module system, but a convention that is based\n+on the organisation of \"ordinary\" Nix packages in the Nixpkgs repository.\n+\n+In Nixpkgs, most package follow the convention that a separate Nix file defines\n+a function in which the function header refers to the build-time dependencies to\n+construct a package from source. Packages are composed in the top-level Nix\n+expression (`all-packages.nix`) that invokes these functions with the required\n+build-time parameters. For most packages, only a single variant is composed, but\n+it is also possible to build different kinds of package variants by changing the\n+function parameters.\n+\n+The Nix process management framework follows the same principles and extends the\n+Nixpkgs convention with a number of additional concepts:\n+\n+* Every service is instantiated from a *constructor* which is a nested function:\n+  the outer function refers to the build-time dependencies that are required to\n+  build the service from source code.\n+* The inner function refers to the service *instance parameters* -- calling the\n+  constructor with different parameters makes it possible to compose a service\n+  with an alternative configuration and in such a way that they do not conflict\n+  with other instances.\n+* The build-time dependencies of constructor functions are composed in a Nix\n+  expression that is typically called: `constructors.nix` in which every\n+  constructor function is called with its required build-time dependency\n+  parameters.\n+* The process instances of a system are composed in a Nix expression that is\n+  typically called: `processes.nix` in which every constructor function is\n+  called with its required instance parameters.\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    chown ${user}:${group} ${socketDir}\n+    chown ${user}:${group} ${dataDir}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        su ${user} -c '${postgresql}/bin/initdb -D ${dataDir} --no-locale'\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a constructor function for PostgreSQL and has the\n+following properties:\n+\n+* The outer function-header (first line) refer to the build inputs of the\n+  service. For example, to construct a configuration from a high-level\n+  specification for any supported process manager, we propagate the\n+  `createManagedProcess` as a parameter.\n+* The inner-function header refers to the instance parameters of a PostgreSQL\n+  instance. When it is desired to have two instances deployed to the same\n+  machine we must make sure that there is no conflict between the state of\n+  the two instances. Avoiding a conflict is possible by assigning a unique TCP\n+  port number and making sure that the state directories do not conflict. The\n+  `instanceName` parameter is appended to the name of the state directories to\n+  make this possible.\n+* In the body of the inner function, we invoke `createManagedProcess` to\n+  agnostically generate a configuration for a supported process manager.\n+  In the configuration, we provide an initialization script and we call the\n+  `postgres` service with its required command-line parameters and environment\n+  variables.\n+\n+As explained earlier, a constructor function is a nested function that we need to compose\n+twice. The build-time dependencies are composed in a Nix expression typically called\n+`constructors.nix`:\n+\n+```nix\n+{ nix-processmgmt ? ../../nix-processmgmt\n+, pkgs\n+, stateDir\n+, logDir\n+, runtimeDir\n+, cacheDir\n+, spoolDir\n+, libDir\n+, tmpDir\n+, callingUser ? null\n+, callingGroup ? null\n+, processManager\n+, ids ? {}\n+}:\n+\n+let\n+  createManagedProcess = import \"${nix-processmgmt}/nixproc/create-managed-process/universal/create-managed-process-universal.nix\" {\n+    inherit pkgs runtimeDir stateDir logDir tmpDir processManager ids;\n+  };\n+in\n+{\n+  postgresql = import ./postgresql {\n+    inherit createManagedProcess stateDir runtimeDir;\n+    inherit (pkgs) lib postgresql su;\n+  };\n+}\n+```\n+\n+In the above Nix expression, we invoke the `postgresql` constructor function and\n+we propagated its required build-time dependencies, such as the `postgresql`\n+package and the `createManagedProcess` abstraction function.\n+\n+The instance parameters are composed in a Nix expression that is typically called\n+`processes.nix`:\n+\n+```nix\n+{ pkgs ? import <nixpkgs> { inherit system; }\n+, system ? builtins.currentSystem\n+, stateDir ? \"/var\"\n+, runtimeDir ? \"${stateDir}/run\"\n+, logDir ? \"${stateDir}/log\"\n+, spoolDir ? \"${stateDir}/spool\"\n+, cacheDir ? \"${stateDir}/cache\"\n+, libDir ? \"${stateDir}/lib\"\n+, tmpDir ? (if stateDir == \"/var\" then \"/tmp\" else \"${stateDir}/tmp\")\n+, processManager\n+, nix-processmgmt ? ../../../nix-processmgmt\n+}:\n+\n+let\n+  constructors = import ../../services-agnostic/constructors.nix {\n+    inherit pkgs stateDir runtimeDir logDir tmpDir cacheDir spoolDir libDir processManager nix-processmgmt;\n+  };\n+in\n+rec {\n+  postgresql = rec {\n+    port = 5432;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+    };\n+  };\n+\n+  postgresql-secondary = rec {\n+    port = 5433;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+      instanceSuffix = \"-secondary\";\n+    };\n+  };\n+}\n+```\n+\n+In the above Nix expression, we compose two PostgreSQL instances that run on the\n+same machine simultaneously. This is possible due to the fact that they use\n+instance parameters that avoid their states to conflict.\n+\n+The above expression imports the `constructors.nix` expression so that each\n+process instance can be composed from the PostgreSQL constructor function.\n+\n+In NixOS, we use the NixOS module system that makes it possible to define\n+multiple aspects of a system in a module (such as packages, config files,\n+systemd units and environment variables) and combine the aspects of each module\n+into a single configuration from which a complete system is deployed, which is\n+very powerful.\n+\n+Combining the conventions of the nix-processmgmt framework with the NixOS module\n+system is something that still needs to be discussed. There are variety of\n+options possible, such as:\n+\n+* Combining the convention with process instances as described earlier by\n+  referring to a process instance from a NixOS module, similar to how NixOS\n+  modules can also refer to ordinary Nix packages\n+* Using sub modules, in which every sub module can define its own variant of a\n+  service.\n+\n+### Unprivileged user deployments\n+\n+Another interesting feature of Nix is to allow unprivileged users to deploy\n+packages. With services, this is also possible, but there are some things we\n+need to take into account, such as:\n+\n+* The state directories must refer to a directory that has writable permissions\n+  for the user\n+* We cannot bind to a TCP/UDP port below 1024\n+\n+We can also introduce a configuration flag and configure a service in such a way\n+that the above criteria are met:\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir, forceDisableUserChange}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    ${lib.optionalString (!forceDisableUserChange) ''\n+      chown ${user}:${group} ${socketDir}\n+      chown ${user}:${group} ${dataDir}\n+    ''}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        ${lib.optionalString (!forceDisableUserChange) \"su ${user} -c '\"}${postgresql}/bin/initdb -D ${dataDir} --no-locale${lib.optionalString (!forceDisableUserChange) \"'\"}\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a revised PostgreSQL constructor function that\n+accepts a new parameter: `forceDisableUserChange`. When this property is\n+enabled, the service configured in such a way that it does not change users,\n+which an unprivileged user is not allowed to do. With this setting enabled, it\n+becomes possible for an unprivileged user to deploy PostgreSQL.\n+\n+# Examples and Interactions\n+[examples-and-interactions]: #examples-and-interactions\n+\n+The `processes.nix` expression shown earlier can be used with a variety of tools\n+to deploy running process instances.\n+\n+The following command deploys the configurations as `sysvinit` scripts and\n+activates the configuration automatically:\n+\n+```bash\n+$ nixproc-sysvinit-switch processes.nix\n+```\n+\n+We can deploy the same configuration as systemd units with the following command:\n+\n+```bash\n+$ nixproc-systemd-switch processes.nix\n+```\n+\n+Or with `supervisord` with the following command:\n+\n+```bash\n+$ nixproc-supervisord-switch processes.nix\n+```\n+\n+As an unprivileged user, we can use the `--state-dir` parameter to change the\n+prefix of the state directories to reside in a user's home directory\n+(`/home/sander/var`) and disable user changing:\n+\n+```bash\n+$ nixproc-sysvinit-switch --state-dir /home/sander/var --force-disable-user-change processes.nix\n+```\n+\n+# Drawbacks\n+[drawbacks]: #drawbacks\n+\n+The ideas describes in this RFC makes the Nixpkgs eco-system more powerful, but\n+these new features come at at a price:\n+\n+* Using high-level abstractions hide the low-level configuration details of\n+  specific process managers. This makes it sometimes more difficult to reason\n+  about what is happening in problematic cases\n+* More testing is required -- we need to test services in multiple deployment\n+  scenarios, which is more expensive. Fortunately, I have seen that testing for\n+  two kinds of targets often give enough certainty: if you test with a\n+  foreground process (e.g. `systemd`) and a daemon (e.g. `sysvinit`) there is a\n+  big chance that it will work with other process managers as well.\n+* You may target the lowest common denominator. This is not actually true,\n+  because with overrides it is still possible to use any feature of a process\n+  manager. Moreover, not all services need to be portable -- low-level services\n+  such as `cups` and `syslogd` can still be packaged in a process\n+  manager-specific way.\n+\n+# Alternatives\n+[alternatives]: #alternatives\n+\n+This RFC proposes a new strategy to allow multiple process instances of the\n+same service to co-exist. Co-existence is possible by avoiding conflicts --\n+making sure that state directories and port numbers do not conflict through\n+function parameters.\n+\n+It is also possible to isolate services by using concepts such as Linux\n+namespaces. Then it is not required to avoid certain kinds of conflicts.\n+Unfortunately, using these features is restricted to Linux only -- this RFC aims\n+to be operating system agnostic.\n+\n+# Prior art\n+[prior-art]: #prior-art\n+\n+This RFC is not the first approach that combines Nix with a process management\n+solution:\n+\n+* The service deployment chapter (chapter 9) of\n+  [Eelco Dolstra's PhD thesis](https://github.com/edolstra/edolstra.github.io/blob/master/pubs/phd-thesis.pdf)\n+  describes a very early service deployment approach that uses generated\n+  start/stop scripts implemented in `bash`. Because shell scripts are so common,\n+  they can be used on a variety of operating systems -- in addition to Linux, it\n+  can also be used on Darwin and FreeBSD.\n+* [NixOS](https://nixos.org) uses `systemd` as its process management solution.\n+  Services can be deployed by generating systemd unit configuration files in a\n+  Nix expression. NixOS deploys `systemd` units globally and requires super\n+  user privileges to get deployed. Moreover, you cannot use the service layer\n+  independently from the rest of NixOS.\n+* [nix-darwin](https://github.com/LnL7/nix-darwin) provides various NixOS\n+  features on macOS/Darwin, including a service management layer that uses the\n+  process manager of macOS: `launchd`.\n+* [home-manager](https://github.com/nix-community/home-manager) offers a service\n+  layer that works with systemd user services, rather than system services. It\n+  contains its own set of service configurations that can be deployed as user\n+  services.\n+* [system-manager](https://github.com/numtide/system-manager) addresses the\n+  limitation of NixOS to use systemd units independently and can deploy many\n+  services in the NixOS repository on any Ubuntu-based distribution.\n+\n+Aside from the first option, the biggest drawback is that these solutions are\n+not generic and there is only little reuse possible between service\n+configurations.\n+\n+# Unresolved questions\n+[unresolved]: #unresolved-questions\n+\n+The integration with NixOS module system still needs to be worked out in detail.\n+Currently, I can think of two scenarios. For this a discussion in the Nix\n+community is required.",
   "id": 1349782607,
   "in_reply_to_id": 1347861030,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 885,
   "original_position": 885,
   "original_start_line": 883,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1663605637,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 883,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-08T21:52:21Z",
   "user": {
    "id": 7755101,
    "login": "aanderse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> a standardised service definition\r\n\r\nIn other words, an API?",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-09T02:41:21Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.\n+\n+For many kinds of services (e.g. PostgreSQL, Apache HTTP server etc), it\n+typically suffices to only have a single instance running on a machine. For\n+microservices, it may actually be quite useful to have the ability to easily\n+create multiple instances. For testing/experimentation purposes it may also be\n+desired to run multiple instances of a system service, such as a web server.\n+\n+It is also not easily possible in NixOS to switch to alternative implementations\n+of a service. For many services, alternative implementations (e.g. `cron`,\n+`ssh`) are not required, but some services accept complex configurations, such\n+as the Apache HTTP server and Apache Tomcat.\n+\n+Finding a universal abstraction in NixOS to facilitate all possible use cases\n+may be impossible. Moreover, it may also be desired for developers to write\n+their own abstractions on top of existing functionality.\n+\n+To support abstractions and multiple instances, the Nix process management\n+framework does not use the NixOS module system, but a convention that is based\n+on the organisation of \"ordinary\" Nix packages in the Nixpkgs repository.\n+\n+In Nixpkgs, most package follow the convention that a separate Nix file defines\n+a function in which the function header refers to the build-time dependencies to\n+construct a package from source. Packages are composed in the top-level Nix\n+expression (`all-packages.nix`) that invokes these functions with the required\n+build-time parameters. For most packages, only a single variant is composed, but\n+it is also possible to build different kinds of package variants by changing the\n+function parameters.\n+\n+The Nix process management framework follows the same principles and extends the\n+Nixpkgs convention with a number of additional concepts:\n+\n+* Every service is instantiated from a *constructor* which is a nested function:\n+  the outer function refers to the build-time dependencies that are required to\n+  build the service from source code.\n+* The inner function refers to the service *instance parameters* -- calling the\n+  constructor with different parameters makes it possible to compose a service\n+  with an alternative configuration and in such a way that they do not conflict\n+  with other instances.\n+* The build-time dependencies of constructor functions are composed in a Nix\n+  expression that is typically called: `constructors.nix` in which every\n+  constructor function is called with its required build-time dependency\n+  parameters.\n+* The process instances of a system are composed in a Nix expression that is\n+  typically called: `processes.nix` in which every constructor function is\n+  called with its required instance parameters.\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    chown ${user}:${group} ${socketDir}\n+    chown ${user}:${group} ${dataDir}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        su ${user} -c '${postgresql}/bin/initdb -D ${dataDir} --no-locale'\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a constructor function for PostgreSQL and has the\n+following properties:\n+\n+* The outer function-header (first line) refer to the build inputs of the\n+  service. For example, to construct a configuration from a high-level\n+  specification for any supported process manager, we propagate the\n+  `createManagedProcess` as a parameter.\n+* The inner-function header refers to the instance parameters of a PostgreSQL\n+  instance. When it is desired to have two instances deployed to the same\n+  machine we must make sure that there is no conflict between the state of\n+  the two instances. Avoiding a conflict is possible by assigning a unique TCP\n+  port number and making sure that the state directories do not conflict. The\n+  `instanceName` parameter is appended to the name of the state directories to\n+  make this possible.\n+* In the body of the inner function, we invoke `createManagedProcess` to\n+  agnostically generate a configuration for a supported process manager.\n+  In the configuration, we provide an initialization script and we call the\n+  `postgres` service with its required command-line parameters and environment\n+  variables.\n+\n+As explained earlier, a constructor function is a nested function that we need to compose\n+twice. The build-time dependencies are composed in a Nix expression typically called\n+`constructors.nix`:\n+\n+```nix\n+{ nix-processmgmt ? ../../nix-processmgmt\n+, pkgs\n+, stateDir\n+, logDir\n+, runtimeDir\n+, cacheDir\n+, spoolDir\n+, libDir\n+, tmpDir\n+, callingUser ? null\n+, callingGroup ? null\n+, processManager\n+, ids ? {}\n+}:\n+\n+let\n+  createManagedProcess = import \"${nix-processmgmt}/nixproc/create-managed-process/universal/create-managed-process-universal.nix\" {\n+    inherit pkgs runtimeDir stateDir logDir tmpDir processManager ids;\n+  };\n+in\n+{\n+  postgresql = import ./postgresql {\n+    inherit createManagedProcess stateDir runtimeDir;\n+    inherit (pkgs) lib postgresql su;\n+  };\n+}\n+```\n+\n+In the above Nix expression, we invoke the `postgresql` constructor function and\n+we propagated its required build-time dependencies, such as the `postgresql`\n+package and the `createManagedProcess` abstraction function.\n+\n+The instance parameters are composed in a Nix expression that is typically called\n+`processes.nix`:\n+\n+```nix\n+{ pkgs ? import <nixpkgs> { inherit system; }\n+, system ? builtins.currentSystem\n+, stateDir ? \"/var\"\n+, runtimeDir ? \"${stateDir}/run\"\n+, logDir ? \"${stateDir}/log\"\n+, spoolDir ? \"${stateDir}/spool\"\n+, cacheDir ? \"${stateDir}/cache\"\n+, libDir ? \"${stateDir}/lib\"\n+, tmpDir ? (if stateDir == \"/var\" then \"/tmp\" else \"${stateDir}/tmp\")\n+, processManager\n+, nix-processmgmt ? ../../../nix-processmgmt\n+}:\n+\n+let\n+  constructors = import ../../services-agnostic/constructors.nix {\n+    inherit pkgs stateDir runtimeDir logDir tmpDir cacheDir spoolDir libDir processManager nix-processmgmt;\n+  };\n+in\n+rec {\n+  postgresql = rec {\n+    port = 5432;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+    };\n+  };\n+\n+  postgresql-secondary = rec {\n+    port = 5433;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+      instanceSuffix = \"-secondary\";\n+    };\n+  };\n+}\n+```\n+\n+In the above Nix expression, we compose two PostgreSQL instances that run on the\n+same machine simultaneously. This is possible due to the fact that they use\n+instance parameters that avoid their states to conflict.\n+\n+The above expression imports the `constructors.nix` expression so that each\n+process instance can be composed from the PostgreSQL constructor function.\n+\n+In NixOS, we use the NixOS module system that makes it possible to define\n+multiple aspects of a system in a module (such as packages, config files,\n+systemd units and environment variables) and combine the aspects of each module\n+into a single configuration from which a complete system is deployed, which is\n+very powerful.\n+\n+Combining the conventions of the nix-processmgmt framework with the NixOS module\n+system is something that still needs to be discussed. There are variety of\n+options possible, such as:\n+\n+* Combining the convention with process instances as described earlier by\n+  referring to a process instance from a NixOS module, similar to how NixOS\n+  modules can also refer to ordinary Nix packages\n+* Using sub modules, in which every sub module can define its own variant of a\n+  service.\n+\n+### Unprivileged user deployments\n+\n+Another interesting feature of Nix is to allow unprivileged users to deploy\n+packages. With services, this is also possible, but there are some things we\n+need to take into account, such as:\n+\n+* The state directories must refer to a directory that has writable permissions\n+  for the user\n+* We cannot bind to a TCP/UDP port below 1024\n+\n+We can also introduce a configuration flag and configure a service in such a way\n+that the above criteria are met:\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir, forceDisableUserChange}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    ${lib.optionalString (!forceDisableUserChange) ''\n+      chown ${user}:${group} ${socketDir}\n+      chown ${user}:${group} ${dataDir}\n+    ''}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        ${lib.optionalString (!forceDisableUserChange) \"su ${user} -c '\"}${postgresql}/bin/initdb -D ${dataDir} --no-locale${lib.optionalString (!forceDisableUserChange) \"'\"}\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a revised PostgreSQL constructor function that\n+accepts a new parameter: `forceDisableUserChange`. When this property is\n+enabled, the service configured in such a way that it does not change users,\n+which an unprivileged user is not allowed to do. With this setting enabled, it\n+becomes possible for an unprivileged user to deploy PostgreSQL.\n+\n+# Examples and Interactions\n+[examples-and-interactions]: #examples-and-interactions\n+\n+The `processes.nix` expression shown earlier can be used with a variety of tools\n+to deploy running process instances.\n+\n+The following command deploys the configurations as `sysvinit` scripts and\n+activates the configuration automatically:\n+\n+```bash\n+$ nixproc-sysvinit-switch processes.nix\n+```\n+\n+We can deploy the same configuration as systemd units with the following command:\n+\n+```bash\n+$ nixproc-systemd-switch processes.nix\n+```\n+\n+Or with `supervisord` with the following command:\n+\n+```bash\n+$ nixproc-supervisord-switch processes.nix\n+```\n+\n+As an unprivileged user, we can use the `--state-dir` parameter to change the\n+prefix of the state directories to reside in a user's home directory\n+(`/home/sander/var`) and disable user changing:\n+\n+```bash\n+$ nixproc-sysvinit-switch --state-dir /home/sander/var --force-disable-user-change processes.nix\n+```\n+\n+# Drawbacks\n+[drawbacks]: #drawbacks\n+\n+The ideas describes in this RFC makes the Nixpkgs eco-system more powerful, but\n+these new features come at at a price:\n+\n+* Using high-level abstractions hide the low-level configuration details of\n+  specific process managers. This makes it sometimes more difficult to reason\n+  about what is happening in problematic cases\n+* More testing is required -- we need to test services in multiple deployment\n+  scenarios, which is more expensive. Fortunately, I have seen that testing for\n+  two kinds of targets often give enough certainty: if you test with a\n+  foreground process (e.g. `systemd`) and a daemon (e.g. `sysvinit`) there is a\n+  big chance that it will work with other process managers as well.\n+* You may target the lowest common denominator. This is not actually true,\n+  because with overrides it is still possible to use any feature of a process\n+  manager. Moreover, not all services need to be portable -- low-level services\n+  such as `cups` and `syslogd` can still be packaged in a process\n+  manager-specific way.\n+\n+# Alternatives\n+[alternatives]: #alternatives\n+\n+This RFC proposes a new strategy to allow multiple process instances of the\n+same service to co-exist. Co-existence is possible by avoiding conflicts --\n+making sure that state directories and port numbers do not conflict through\n+function parameters.\n+\n+It is also possible to isolate services by using concepts such as Linux\n+namespaces. Then it is not required to avoid certain kinds of conflicts.\n+Unfortunately, using these features is restricted to Linux only -- this RFC aims\n+to be operating system agnostic.\n+\n+# Prior art\n+[prior-art]: #prior-art\n+\n+This RFC is not the first approach that combines Nix with a process management\n+solution:\n+\n+* The service deployment chapter (chapter 9) of\n+  [Eelco Dolstra's PhD thesis](https://github.com/edolstra/edolstra.github.io/blob/master/pubs/phd-thesis.pdf)\n+  describes a very early service deployment approach that uses generated\n+  start/stop scripts implemented in `bash`. Because shell scripts are so common,\n+  they can be used on a variety of operating systems -- in addition to Linux, it\n+  can also be used on Darwin and FreeBSD.\n+* [NixOS](https://nixos.org) uses `systemd` as its process management solution.\n+  Services can be deployed by generating systemd unit configuration files in a\n+  Nix expression. NixOS deploys `systemd` units globally and requires super\n+  user privileges to get deployed. Moreover, you cannot use the service layer\n+  independently from the rest of NixOS.\n+* [nix-darwin](https://github.com/LnL7/nix-darwin) provides various NixOS\n+  features on macOS/Darwin, including a service management layer that uses the\n+  process manager of macOS: `launchd`.\n+* [home-manager](https://github.com/nix-community/home-manager) offers a service\n+  layer that works with systemd user services, rather than system services. It\n+  contains its own set of service configurations that can be deployed as user\n+  services.\n+* [system-manager](https://github.com/numtide/system-manager) addresses the\n+  limitation of NixOS to use systemd units independently and can deploy many\n+  services in the NixOS repository on any Ubuntu-based distribution.\n+\n+Aside from the first option, the biggest drawback is that these solutions are\n+not generic and there is only little reuse possible between service\n+configurations.\n+\n+# Unresolved questions\n+[unresolved]: #unresolved-questions\n+\n+The integration with NixOS module system still needs to be worked out in detail.\n+Currently, I can think of two scenarios. For this a discussion in the Nix\n+community is required.",
   "id": 1349826824,
   "in_reply_to_id": 1347861030,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 885,
   "original_position": 885,
   "original_start_line": 883,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1663671614,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 883,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-09T02:41:21Z",
   "user": {
    "id": 5954806,
    "login": "AndersonTorres",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> This would then also be automatically discovered in some way and added to NixOS\r\n\r\nWe don't even have a module based implementation yet, so it's way too soon to start automating and imposing restrictions. This is a very different problem.\r\nI'm all for grouping files by functionality instead of technical type, but easy on the rules please.\r\n\r\n\r\n\r\n> an API?\r\n\r\nAn interface.\r\n\r\n\"Application programming\" just muddies the waters. It's 3 syllables either way.\r\n",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-09T09:19:43Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.\n+\n+For many kinds of services (e.g. PostgreSQL, Apache HTTP server etc), it\n+typically suffices to only have a single instance running on a machine. For\n+microservices, it may actually be quite useful to have the ability to easily\n+create multiple instances. For testing/experimentation purposes it may also be\n+desired to run multiple instances of a system service, such as a web server.\n+\n+It is also not easily possible in NixOS to switch to alternative implementations\n+of a service. For many services, alternative implementations (e.g. `cron`,\n+`ssh`) are not required, but some services accept complex configurations, such\n+as the Apache HTTP server and Apache Tomcat.\n+\n+Finding a universal abstraction in NixOS to facilitate all possible use cases\n+may be impossible. Moreover, it may also be desired for developers to write\n+their own abstractions on top of existing functionality.\n+\n+To support abstractions and multiple instances, the Nix process management\n+framework does not use the NixOS module system, but a convention that is based\n+on the organisation of \"ordinary\" Nix packages in the Nixpkgs repository.\n+\n+In Nixpkgs, most package follow the convention that a separate Nix file defines\n+a function in which the function header refers to the build-time dependencies to\n+construct a package from source. Packages are composed in the top-level Nix\n+expression (`all-packages.nix`) that invokes these functions with the required\n+build-time parameters. For most packages, only a single variant is composed, but\n+it is also possible to build different kinds of package variants by changing the\n+function parameters.\n+\n+The Nix process management framework follows the same principles and extends the\n+Nixpkgs convention with a number of additional concepts:\n+\n+* Every service is instantiated from a *constructor* which is a nested function:\n+  the outer function refers to the build-time dependencies that are required to\n+  build the service from source code.\n+* The inner function refers to the service *instance parameters* -- calling the\n+  constructor with different parameters makes it possible to compose a service\n+  with an alternative configuration and in such a way that they do not conflict\n+  with other instances.\n+* The build-time dependencies of constructor functions are composed in a Nix\n+  expression that is typically called: `constructors.nix` in which every\n+  constructor function is called with its required build-time dependency\n+  parameters.\n+* The process instances of a system are composed in a Nix expression that is\n+  typically called: `processes.nix` in which every constructor function is\n+  called with its required instance parameters.\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    chown ${user}:${group} ${socketDir}\n+    chown ${user}:${group} ${dataDir}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        su ${user} -c '${postgresql}/bin/initdb -D ${dataDir} --no-locale'\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a constructor function for PostgreSQL and has the\n+following properties:\n+\n+* The outer function-header (first line) refer to the build inputs of the\n+  service. For example, to construct a configuration from a high-level\n+  specification for any supported process manager, we propagate the\n+  `createManagedProcess` as a parameter.\n+* The inner-function header refers to the instance parameters of a PostgreSQL\n+  instance. When it is desired to have two instances deployed to the same\n+  machine we must make sure that there is no conflict between the state of\n+  the two instances. Avoiding a conflict is possible by assigning a unique TCP\n+  port number and making sure that the state directories do not conflict. The\n+  `instanceName` parameter is appended to the name of the state directories to\n+  make this possible.\n+* In the body of the inner function, we invoke `createManagedProcess` to\n+  agnostically generate a configuration for a supported process manager.\n+  In the configuration, we provide an initialization script and we call the\n+  `postgres` service with its required command-line parameters and environment\n+  variables.\n+\n+As explained earlier, a constructor function is a nested function that we need to compose\n+twice. The build-time dependencies are composed in a Nix expression typically called\n+`constructors.nix`:\n+\n+```nix\n+{ nix-processmgmt ? ../../nix-processmgmt\n+, pkgs\n+, stateDir\n+, logDir\n+, runtimeDir\n+, cacheDir\n+, spoolDir\n+, libDir\n+, tmpDir\n+, callingUser ? null\n+, callingGroup ? null\n+, processManager\n+, ids ? {}\n+}:\n+\n+let\n+  createManagedProcess = import \"${nix-processmgmt}/nixproc/create-managed-process/universal/create-managed-process-universal.nix\" {\n+    inherit pkgs runtimeDir stateDir logDir tmpDir processManager ids;\n+  };\n+in\n+{\n+  postgresql = import ./postgresql {\n+    inherit createManagedProcess stateDir runtimeDir;\n+    inherit (pkgs) lib postgresql su;\n+  };\n+}\n+```\n+\n+In the above Nix expression, we invoke the `postgresql` constructor function and\n+we propagated its required build-time dependencies, such as the `postgresql`\n+package and the `createManagedProcess` abstraction function.\n+\n+The instance parameters are composed in a Nix expression that is typically called\n+`processes.nix`:\n+\n+```nix\n+{ pkgs ? import <nixpkgs> { inherit system; }\n+, system ? builtins.currentSystem\n+, stateDir ? \"/var\"\n+, runtimeDir ? \"${stateDir}/run\"\n+, logDir ? \"${stateDir}/log\"\n+, spoolDir ? \"${stateDir}/spool\"\n+, cacheDir ? \"${stateDir}/cache\"\n+, libDir ? \"${stateDir}/lib\"\n+, tmpDir ? (if stateDir == \"/var\" then \"/tmp\" else \"${stateDir}/tmp\")\n+, processManager\n+, nix-processmgmt ? ../../../nix-processmgmt\n+}:\n+\n+let\n+  constructors = import ../../services-agnostic/constructors.nix {\n+    inherit pkgs stateDir runtimeDir logDir tmpDir cacheDir spoolDir libDir processManager nix-processmgmt;\n+  };\n+in\n+rec {\n+  postgresql = rec {\n+    port = 5432;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+    };\n+  };\n+\n+  postgresql-secondary = rec {\n+    port = 5433;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+      instanceSuffix = \"-secondary\";\n+    };\n+  };\n+}\n+```\n+\n+In the above Nix expression, we compose two PostgreSQL instances that run on the\n+same machine simultaneously. This is possible due to the fact that they use\n+instance parameters that avoid their states to conflict.\n+\n+The above expression imports the `constructors.nix` expression so that each\n+process instance can be composed from the PostgreSQL constructor function.\n+\n+In NixOS, we use the NixOS module system that makes it possible to define\n+multiple aspects of a system in a module (such as packages, config files,\n+systemd units and environment variables) and combine the aspects of each module\n+into a single configuration from which a complete system is deployed, which is\n+very powerful.\n+\n+Combining the conventions of the nix-processmgmt framework with the NixOS module\n+system is something that still needs to be discussed. There are variety of\n+options possible, such as:\n+\n+* Combining the convention with process instances as described earlier by\n+  referring to a process instance from a NixOS module, similar to how NixOS\n+  modules can also refer to ordinary Nix packages\n+* Using sub modules, in which every sub module can define its own variant of a\n+  service.\n+\n+### Unprivileged user deployments\n+\n+Another interesting feature of Nix is to allow unprivileged users to deploy\n+packages. With services, this is also possible, but there are some things we\n+need to take into account, such as:\n+\n+* The state directories must refer to a directory that has writable permissions\n+  for the user\n+* We cannot bind to a TCP/UDP port below 1024\n+\n+We can also introduce a configuration flag and configure a service in such a way\n+that the above criteria are met:\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir, forceDisableUserChange}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    ${lib.optionalString (!forceDisableUserChange) ''\n+      chown ${user}:${group} ${socketDir}\n+      chown ${user}:${group} ${dataDir}\n+    ''}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        ${lib.optionalString (!forceDisableUserChange) \"su ${user} -c '\"}${postgresql}/bin/initdb -D ${dataDir} --no-locale${lib.optionalString (!forceDisableUserChange) \"'\"}\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a revised PostgreSQL constructor function that\n+accepts a new parameter: `forceDisableUserChange`. When this property is\n+enabled, the service configured in such a way that it does not change users,\n+which an unprivileged user is not allowed to do. With this setting enabled, it\n+becomes possible for an unprivileged user to deploy PostgreSQL.\n+\n+# Examples and Interactions\n+[examples-and-interactions]: #examples-and-interactions\n+\n+The `processes.nix` expression shown earlier can be used with a variety of tools\n+to deploy running process instances.\n+\n+The following command deploys the configurations as `sysvinit` scripts and\n+activates the configuration automatically:\n+\n+```bash\n+$ nixproc-sysvinit-switch processes.nix\n+```\n+\n+We can deploy the same configuration as systemd units with the following command:\n+\n+```bash\n+$ nixproc-systemd-switch processes.nix\n+```\n+\n+Or with `supervisord` with the following command:\n+\n+```bash\n+$ nixproc-supervisord-switch processes.nix\n+```\n+\n+As an unprivileged user, we can use the `--state-dir` parameter to change the\n+prefix of the state directories to reside in a user's home directory\n+(`/home/sander/var`) and disable user changing:\n+\n+```bash\n+$ nixproc-sysvinit-switch --state-dir /home/sander/var --force-disable-user-change processes.nix\n+```\n+\n+# Drawbacks\n+[drawbacks]: #drawbacks\n+\n+The ideas describes in this RFC makes the Nixpkgs eco-system more powerful, but\n+these new features come at at a price:\n+\n+* Using high-level abstractions hide the low-level configuration details of\n+  specific process managers. This makes it sometimes more difficult to reason\n+  about what is happening in problematic cases\n+* More testing is required -- we need to test services in multiple deployment\n+  scenarios, which is more expensive. Fortunately, I have seen that testing for\n+  two kinds of targets often give enough certainty: if you test with a\n+  foreground process (e.g. `systemd`) and a daemon (e.g. `sysvinit`) there is a\n+  big chance that it will work with other process managers as well.\n+* You may target the lowest common denominator. This is not actually true,\n+  because with overrides it is still possible to use any feature of a process\n+  manager. Moreover, not all services need to be portable -- low-level services\n+  such as `cups` and `syslogd` can still be packaged in a process\n+  manager-specific way.\n+\n+# Alternatives\n+[alternatives]: #alternatives\n+\n+This RFC proposes a new strategy to allow multiple process instances of the\n+same service to co-exist. Co-existence is possible by avoiding conflicts --\n+making sure that state directories and port numbers do not conflict through\n+function parameters.\n+\n+It is also possible to isolate services by using concepts such as Linux\n+namespaces. Then it is not required to avoid certain kinds of conflicts.\n+Unfortunately, using these features is restricted to Linux only -- this RFC aims\n+to be operating system agnostic.\n+\n+# Prior art\n+[prior-art]: #prior-art\n+\n+This RFC is not the first approach that combines Nix with a process management\n+solution:\n+\n+* The service deployment chapter (chapter 9) of\n+  [Eelco Dolstra's PhD thesis](https://github.com/edolstra/edolstra.github.io/blob/master/pubs/phd-thesis.pdf)\n+  describes a very early service deployment approach that uses generated\n+  start/stop scripts implemented in `bash`. Because shell scripts are so common,\n+  they can be used on a variety of operating systems -- in addition to Linux, it\n+  can also be used on Darwin and FreeBSD.\n+* [NixOS](https://nixos.org) uses `systemd` as its process management solution.\n+  Services can be deployed by generating systemd unit configuration files in a\n+  Nix expression. NixOS deploys `systemd` units globally and requires super\n+  user privileges to get deployed. Moreover, you cannot use the service layer\n+  independently from the rest of NixOS.\n+* [nix-darwin](https://github.com/LnL7/nix-darwin) provides various NixOS\n+  features on macOS/Darwin, including a service management layer that uses the\n+  process manager of macOS: `launchd`.\n+* [home-manager](https://github.com/nix-community/home-manager) offers a service\n+  layer that works with systemd user services, rather than system services. It\n+  contains its own set of service configurations that can be deployed as user\n+  services.\n+* [system-manager](https://github.com/numtide/system-manager) addresses the\n+  limitation of NixOS to use systemd units independently and can deploy many\n+  services in the NixOS repository on any Ubuntu-based distribution.\n+\n+Aside from the first option, the biggest drawback is that these solutions are\n+not generic and there is only little reuse possible between service\n+configurations.\n+\n+# Unresolved questions\n+[unresolved]: #unresolved-questions\n+\n+The integration with NixOS module system still needs to be worked out in detail.\n+Currently, I can think of two scenarios. For this a discussion in the Nix\n+community is required.",
   "id": 1350051860,
   "in_reply_to_id": 1347861030,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 885,
   "original_position": 885,
   "original_start_line": 883,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1664034495,
   "reactions": {
    "+1": 3,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 3
   },
   "side": "RIGHT",
   "start_line": 883,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-09T09:19:43Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "For completeness, it was a reference to the Nix paper, [Nix: A Safe and Policy-Free System for Software Deployment](https://www.usenix.org/legacy/event/lisa04/tech/full_papers/dolstra/dolstra_html/), which has nothing to do with support policies. (so an inside joke for those aware of the paper)",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-09T09:32:18Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.",
   "id": 1350065207,
   "in_reply_to_id": 1348780418,
   "line": 39,
   "original_commit_id": "dbd38c2cd817cfaa971c677be177bca91f37ee71",
   "original_line": 39,
   "original_position": 39,
   "original_start_line": 37,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 39,
   "pull_request_review_id": 1664055810,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 1,
    "rocket": 0,
    "total_count": 1
   },
   "side": "RIGHT",
   "start_line": 37,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-09T09:32:18Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Then it sounds like this RFC should be a draft instead",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-09T19:06:51Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.\n+\n+For many kinds of services (e.g. PostgreSQL, Apache HTTP server etc), it\n+typically suffices to only have a single instance running on a machine. For\n+microservices, it may actually be quite useful to have the ability to easily\n+create multiple instances. For testing/experimentation purposes it may also be\n+desired to run multiple instances of a system service, such as a web server.\n+\n+It is also not easily possible in NixOS to switch to alternative implementations\n+of a service. For many services, alternative implementations (e.g. `cron`,\n+`ssh`) are not required, but some services accept complex configurations, such\n+as the Apache HTTP server and Apache Tomcat.\n+\n+Finding a universal abstraction in NixOS to facilitate all possible use cases\n+may be impossible. Moreover, it may also be desired for developers to write\n+their own abstractions on top of existing functionality.\n+\n+To support abstractions and multiple instances, the Nix process management\n+framework does not use the NixOS module system, but a convention that is based\n+on the organisation of \"ordinary\" Nix packages in the Nixpkgs repository.\n+\n+In Nixpkgs, most package follow the convention that a separate Nix file defines\n+a function in which the function header refers to the build-time dependencies to\n+construct a package from source. Packages are composed in the top-level Nix\n+expression (`all-packages.nix`) that invokes these functions with the required\n+build-time parameters. For most packages, only a single variant is composed, but\n+it is also possible to build different kinds of package variants by changing the\n+function parameters.\n+\n+The Nix process management framework follows the same principles and extends the\n+Nixpkgs convention with a number of additional concepts:\n+\n+* Every service is instantiated from a *constructor* which is a nested function:\n+  the outer function refers to the build-time dependencies that are required to\n+  build the service from source code.\n+* The inner function refers to the service *instance parameters* -- calling the\n+  constructor with different parameters makes it possible to compose a service\n+  with an alternative configuration and in such a way that they do not conflict\n+  with other instances.\n+* The build-time dependencies of constructor functions are composed in a Nix\n+  expression that is typically called: `constructors.nix` in which every\n+  constructor function is called with its required build-time dependency\n+  parameters.\n+* The process instances of a system are composed in a Nix expression that is\n+  typically called: `processes.nix` in which every constructor function is\n+  called with its required instance parameters.\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    chown ${user}:${group} ${socketDir}\n+    chown ${user}:${group} ${dataDir}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        su ${user} -c '${postgresql}/bin/initdb -D ${dataDir} --no-locale'\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a constructor function for PostgreSQL and has the\n+following properties:\n+\n+* The outer function-header (first line) refer to the build inputs of the\n+  service. For example, to construct a configuration from a high-level\n+  specification for any supported process manager, we propagate the\n+  `createManagedProcess` as a parameter.\n+* The inner-function header refers to the instance parameters of a PostgreSQL\n+  instance. When it is desired to have two instances deployed to the same\n+  machine we must make sure that there is no conflict between the state of\n+  the two instances. Avoiding a conflict is possible by assigning a unique TCP\n+  port number and making sure that the state directories do not conflict. The\n+  `instanceName` parameter is appended to the name of the state directories to\n+  make this possible.\n+* In the body of the inner function, we invoke `createManagedProcess` to\n+  agnostically generate a configuration for a supported process manager.\n+  In the configuration, we provide an initialization script and we call the\n+  `postgres` service with its required command-line parameters and environment\n+  variables.\n+\n+As explained earlier, a constructor function is a nested function that we need to compose\n+twice. The build-time dependencies are composed in a Nix expression typically called\n+`constructors.nix`:\n+\n+```nix\n+{ nix-processmgmt ? ../../nix-processmgmt\n+, pkgs\n+, stateDir\n+, logDir\n+, runtimeDir\n+, cacheDir\n+, spoolDir\n+, libDir\n+, tmpDir\n+, callingUser ? null\n+, callingGroup ? null\n+, processManager\n+, ids ? {}\n+}:\n+\n+let\n+  createManagedProcess = import \"${nix-processmgmt}/nixproc/create-managed-process/universal/create-managed-process-universal.nix\" {\n+    inherit pkgs runtimeDir stateDir logDir tmpDir processManager ids;\n+  };\n+in\n+{\n+  postgresql = import ./postgresql {\n+    inherit createManagedProcess stateDir runtimeDir;\n+    inherit (pkgs) lib postgresql su;\n+  };\n+}\n+```\n+\n+In the above Nix expression, we invoke the `postgresql` constructor function and\n+we propagated its required build-time dependencies, such as the `postgresql`\n+package and the `createManagedProcess` abstraction function.\n+\n+The instance parameters are composed in a Nix expression that is typically called\n+`processes.nix`:\n+\n+```nix\n+{ pkgs ? import <nixpkgs> { inherit system; }\n+, system ? builtins.currentSystem\n+, stateDir ? \"/var\"\n+, runtimeDir ? \"${stateDir}/run\"\n+, logDir ? \"${stateDir}/log\"\n+, spoolDir ? \"${stateDir}/spool\"\n+, cacheDir ? \"${stateDir}/cache\"\n+, libDir ? \"${stateDir}/lib\"\n+, tmpDir ? (if stateDir == \"/var\" then \"/tmp\" else \"${stateDir}/tmp\")\n+, processManager\n+, nix-processmgmt ? ../../../nix-processmgmt\n+}:\n+\n+let\n+  constructors = import ../../services-agnostic/constructors.nix {\n+    inherit pkgs stateDir runtimeDir logDir tmpDir cacheDir spoolDir libDir processManager nix-processmgmt;\n+  };\n+in\n+rec {\n+  postgresql = rec {\n+    port = 5432;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+    };\n+  };\n+\n+  postgresql-secondary = rec {\n+    port = 5433;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+      instanceSuffix = \"-secondary\";\n+    };\n+  };\n+}\n+```\n+\n+In the above Nix expression, we compose two PostgreSQL instances that run on the\n+same machine simultaneously. This is possible due to the fact that they use\n+instance parameters that avoid their states to conflict.\n+\n+The above expression imports the `constructors.nix` expression so that each\n+process instance can be composed from the PostgreSQL constructor function.\n+\n+In NixOS, we use the NixOS module system that makes it possible to define\n+multiple aspects of a system in a module (such as packages, config files,\n+systemd units and environment variables) and combine the aspects of each module\n+into a single configuration from which a complete system is deployed, which is\n+very powerful.\n+\n+Combining the conventions of the nix-processmgmt framework with the NixOS module\n+system is something that still needs to be discussed. There are variety of\n+options possible, such as:\n+\n+* Combining the convention with process instances as described earlier by\n+  referring to a process instance from a NixOS module, similar to how NixOS\n+  modules can also refer to ordinary Nix packages\n+* Using sub modules, in which every sub module can define its own variant of a\n+  service.\n+\n+### Unprivileged user deployments\n+\n+Another interesting feature of Nix is to allow unprivileged users to deploy\n+packages. With services, this is also possible, but there are some things we\n+need to take into account, such as:\n+\n+* The state directories must refer to a directory that has writable permissions\n+  for the user\n+* We cannot bind to a TCP/UDP port below 1024\n+\n+We can also introduce a configuration flag and configure a service in such a way\n+that the above criteria are met:\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir, forceDisableUserChange}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    ${lib.optionalString (!forceDisableUserChange) ''\n+      chown ${user}:${group} ${socketDir}\n+      chown ${user}:${group} ${dataDir}\n+    ''}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        ${lib.optionalString (!forceDisableUserChange) \"su ${user} -c '\"}${postgresql}/bin/initdb -D ${dataDir} --no-locale${lib.optionalString (!forceDisableUserChange) \"'\"}\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a revised PostgreSQL constructor function that\n+accepts a new parameter: `forceDisableUserChange`. When this property is\n+enabled, the service configured in such a way that it does not change users,\n+which an unprivileged user is not allowed to do. With this setting enabled, it\n+becomes possible for an unprivileged user to deploy PostgreSQL.\n+\n+# Examples and Interactions\n+[examples-and-interactions]: #examples-and-interactions\n+\n+The `processes.nix` expression shown earlier can be used with a variety of tools\n+to deploy running process instances.\n+\n+The following command deploys the configurations as `sysvinit` scripts and\n+activates the configuration automatically:\n+\n+```bash\n+$ nixproc-sysvinit-switch processes.nix\n+```\n+\n+We can deploy the same configuration as systemd units with the following command:\n+\n+```bash\n+$ nixproc-systemd-switch processes.nix\n+```\n+\n+Or with `supervisord` with the following command:\n+\n+```bash\n+$ nixproc-supervisord-switch processes.nix\n+```\n+\n+As an unprivileged user, we can use the `--state-dir` parameter to change the\n+prefix of the state directories to reside in a user's home directory\n+(`/home/sander/var`) and disable user changing:\n+\n+```bash\n+$ nixproc-sysvinit-switch --state-dir /home/sander/var --force-disable-user-change processes.nix\n+```\n+\n+# Drawbacks\n+[drawbacks]: #drawbacks\n+\n+The ideas describes in this RFC makes the Nixpkgs eco-system more powerful, but\n+these new features come at at a price:\n+\n+* Using high-level abstractions hide the low-level configuration details of\n+  specific process managers. This makes it sometimes more difficult to reason\n+  about what is happening in problematic cases\n+* More testing is required -- we need to test services in multiple deployment\n+  scenarios, which is more expensive. Fortunately, I have seen that testing for\n+  two kinds of targets often give enough certainty: if you test with a\n+  foreground process (e.g. `systemd`) and a daemon (e.g. `sysvinit`) there is a\n+  big chance that it will work with other process managers as well.\n+* You may target the lowest common denominator. This is not actually true,\n+  because with overrides it is still possible to use any feature of a process\n+  manager. Moreover, not all services need to be portable -- low-level services\n+  such as `cups` and `syslogd` can still be packaged in a process\n+  manager-specific way.\n+\n+# Alternatives\n+[alternatives]: #alternatives\n+\n+This RFC proposes a new strategy to allow multiple process instances of the\n+same service to co-exist. Co-existence is possible by avoiding conflicts --\n+making sure that state directories and port numbers do not conflict through\n+function parameters.\n+\n+It is also possible to isolate services by using concepts such as Linux\n+namespaces. Then it is not required to avoid certain kinds of conflicts.\n+Unfortunately, using these features is restricted to Linux only -- this RFC aims\n+to be operating system agnostic.\n+\n+# Prior art\n+[prior-art]: #prior-art\n+\n+This RFC is not the first approach that combines Nix with a process management\n+solution:\n+\n+* The service deployment chapter (chapter 9) of\n+  [Eelco Dolstra's PhD thesis](https://github.com/edolstra/edolstra.github.io/blob/master/pubs/phd-thesis.pdf)\n+  describes a very early service deployment approach that uses generated\n+  start/stop scripts implemented in `bash`. Because shell scripts are so common,\n+  they can be used on a variety of operating systems -- in addition to Linux, it\n+  can also be used on Darwin and FreeBSD.\n+* [NixOS](https://nixos.org) uses `systemd` as its process management solution.\n+  Services can be deployed by generating systemd unit configuration files in a\n+  Nix expression. NixOS deploys `systemd` units globally and requires super\n+  user privileges to get deployed. Moreover, you cannot use the service layer\n+  independently from the rest of NixOS.\n+* [nix-darwin](https://github.com/LnL7/nix-darwin) provides various NixOS\n+  features on macOS/Darwin, including a service management layer that uses the\n+  process manager of macOS: `launchd`.\n+* [home-manager](https://github.com/nix-community/home-manager) offers a service\n+  layer that works with systemd user services, rather than system services. It\n+  contains its own set of service configurations that can be deployed as user\n+  services.\n+* [system-manager](https://github.com/numtide/system-manager) addresses the\n+  limitation of NixOS to use systemd units independently and can deploy many\n+  services in the NixOS repository on any Ubuntu-based distribution.\n+\n+Aside from the first option, the biggest drawback is that these solutions are\n+not generic and there is only little reuse possible between service\n+configurations.\n+\n+# Unresolved questions\n+[unresolved]: #unresolved-questions\n+\n+The integration with NixOS module system still needs to be worked out in detail.\n+Currently, I can think of two scenarios. For this a discussion in the Nix\n+community is required.",
   "id": 1350708990,
   "in_reply_to_id": 1347859356,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 885,
   "original_position": 885,
   "original_start_line": 883,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1665124676,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 883,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-09T19:06:51Z",
   "user": {
    "id": 20525370,
    "login": "infinisil",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I do not, but perhaps I just explain it. Suppose in some language I declare a type (`struct`, whatever) called `Foo`. I can't express \"give me all the `Foo`s\", I have to find them myself. But in SQL I can. SQL doesn't follow the object-capability discipline that most other programming languages do.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-11T14:19:31Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.",
   "id": 1355088496,
   "in_reply_to_id": 1347854623,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 486,
   "original_position": 486,
   "original_start_line": 482,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1671369484,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 482,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-11T14:20:21Z",
   "user": {
    "id": 1055245,
    "login": "Ericson2314",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "We could call this \"enumerability\" of instances. In the end, for the configuration to be buildable, all services have to be enumerated anyway, so I guess this is a bit about how it \"feels\" more than what is fundamentally possible. \r\nAlthough I think we could make this idea work by talking about the enumerability of service \"subtypes\" where a subtype is for example a postgresql instance (ie not talking about the other dimension, which is different supervisors).\r\n\r\nWhether making enumeration \"hard\" is a good thing, I'm not sure about. For instance, it will be somewhat harder to configure a database management thing for all local databases.\r\nThough arguably that's not a question of configuring services, but rather a constellation of services that interact. How to do this would tend to involve the specifics of the \"medium\" in which these services live, whether that's a NixOS configuration, or a NixOps network, or something more dynamic, etc.\r\nSolving these as part of the (individual) service abstraction may well be a mistake. Instead, we'd have to abstract over larger systems, which seems like a different proposition altogether.\r\n\r\nI think it'd be fine to solve reuse of fairly uninvolved service definitions before talking more about modeling larger systems of services.\r\n\r\n",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-11T15:35:20Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.",
   "id": 1355255294,
   "in_reply_to_id": 1347854623,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 486,
   "original_position": 486,
   "original_start_line": 482,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1671628292,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 482,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-11T15:35:20Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Home Manager has ability to [create a DAG](https://github.com/nix-community/home-manager/blob/master/modules/lib/dag.nix) (also the [Manual](https://nix-community.github.io/home-manager/#sec-option-types)), such concept maybe useful to some extent to specify dependency and reverse-dependency of the services at a high-level.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-15T03:08:05Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.",
   "id": 1359738742,
   "line": 436,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 436,
   "original_position": 436,
   "original_start_line": 433,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 436,
   "pull_request_review_id": 1678600312,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 433,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-15T03:08:06Z",
   "user": {
    "id": 26714676,
    "login": "Animeshz",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Nixpkgs also have [some similar module for strings](https://github.com/NixOS/nixpkgs/blob/master/lib/strings-with-deps.nix) which is not as generic as home-manager has, but it is used for ordering the system activation script.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-15T03:11:32Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.",
   "id": 1359740177,
   "in_reply_to_id": 1359738742,
   "line": 436,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 436,
   "original_position": 436,
   "original_start_line": 433,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 436,
   "pull_request_review_id": 1678602618,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 433,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-15T03:11:32Z",
   "user": {
    "id": 26714676,
    "login": "Animeshz",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> I think it'd be fine to solve reuse of fairly uninvolved service definitions before talking more about modeling larger systems of services.\r\n\r\nI'd say that packages with overrides in Nixpkgs are simpler to reason about than modules in NixOS, so it would be nice to have the lower implementation level to be functions, and the module-based whole function composition sit on top.\r\n\r\n(Would also allow slightly less horribly slow to evaluate implementations of whole-system composition to share more code with NixOS)",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-15T08:38:39Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.",
   "id": 1359833760,
   "in_reply_to_id": 1347854623,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 486,
   "original_position": 486,
   "original_start_line": 482,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1678752824,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 1,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 1
   },
   "side": "RIGHT",
   "start_line": 482,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-15T08:38:39Z",
   "user": {
    "id": 1891350,
    "login": "7c6f434c",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> Using it might look something like\r\n\r\nI understand at a high level what you're saying but I'm not sure it's immediately clear to everyone the _details_ of what this would actually look like. I don't understand, at least.\r\n\r\nMaybe you can lead me in the right direction. For simplicity and so that we're all clear let's pretend we're implementing this system and we happen to be implementing a `systemd` based backend - again, for simplicity and clarity using conventions we're all used to.\r\n\r\nBack to your example. You defined an option `listenPort` in your submodule and I understand what this would look like. What I don't understand is what the implementation of this would look like - what configuration would you merge into `config` based on any `options`, like `listenPort`? In NixOS merging configuration into `config` works so well because of a number of high level abstractions defined in other modules, like `users.users`, `users.groups`, `environment.etc`, `environment.systemPackages`, and `systemd` to have a few of the most common and valuable.\r\n\r\nIn your example I don't understand how we build anything useful without those abstractions, or if we have those abstractions where they come from... unless you say each submodule `import`s everything it needs, like `systemd.nix`, `users.nix`, `environment.nix`, etc... Even if you say each submodule needs to `import` what it needs I could still use some clarification, though. Doesn't this mean a bit of redundant evaluation because multiple modules are `import`ing the same dependencies (like `systemd.nix`) over and over? If so, I guess these `import`ed files would have to be carefully designed to be used in submodules.\r\n\r\nAny clarification you can provide would be greatly appreciated.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-15T20:40:16Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.",
   "id": 1359946626,
   "in_reply_to_id": 1347854623,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 486,
   "original_position": 486,
   "original_start_line": 482,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1678868441,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 482,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-15T20:40:16Z",
   "user": {
    "id": 7755101,
    "login": "aanderse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I also have [a PR](https://github.com/NixOS/nixpkgs/pull/97392) to add the DAG stuff to Nixpkgs",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-16T13:39:28Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.",
   "id": 1360683093,
   "in_reply_to_id": 1359738742,
   "line": 436,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 436,
   "original_position": 436,
   "original_start_line": 433,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 436,
   "pull_request_review_id": 1680016075,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 3,
    "laugh": 0,
    "rocket": 0,
    "total_count": 3
   },
   "side": "RIGHT",
   "start_line": 433,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-16T13:39:29Z",
   "user": {
    "id": 20525370,
    "login": "infinisil",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I know from home-manager that this accumulates layers upon layers of dirty workarounds because every distro is slightly different. I don't want this to take presence in NixOS modules to have 5 workarounds for all kinds of distros in every module. This would create an enormous amount of churn for maintainers and the opposite is what the community needs.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-22T17:10:47Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.",
   "id": 1367939326,
   "line": 17,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 17,
   "original_position": 17,
   "original_start_line": 15,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 17,
   "pull_request_review_id": 1691465165,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 15,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-22T18:10:19Z",
   "user": {
    "id": 7258858,
    "login": "SuperSandro2000",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Up to a point because some things just don't work well like the graphic stack or mixing glibc.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-22T17:11:13Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not",
   "id": 1367939401,
   "line": 24,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 24,
   "original_position": 24,
   "original_start_line": 23,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 24,
   "pull_request_review_id": 1691465165,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 23,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-22T18:10:19Z",
   "user": {
    "id": 7258858,
    "login": "SuperSandro2000",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I doubt that it makes sense moving the files. They should have rather appropriate metadata which can be found on search.nixos.org or similar.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-22T17:13:41Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.",
   "id": 1367939736,
   "line": 111,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 111,
   "original_position": 111,
   "original_start_line": 106,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 111,
   "pull_request_review_id": 1691465165,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 106,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-22T18:10:19Z",
   "user": {
    "id": 7258858,
    "login": "SuperSandro2000",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "What makes a service high or low level? We should find something we can't bikesheed a lot about. Probably something like easy to integrate services.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-22T17:17:58Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.",
   "id": 1367940309,
   "line": 115,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 115,
   "original_position": 115,
   "original_start_line": 113,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 115,
   "pull_request_review_id": 1691465165,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 113,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-22T18:10:19Z",
   "user": {
    "id": 7258858,
    "login": "SuperSandro2000",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "We currently do not support this and I have my extreme doubts if we really want to support older distros.\r\n\r\nNixOS is so fast moving and keeping support with Ubuntu 14.04, 16.04, 18.08 and probably 20.04 will require lots of work mostly because of different major versions of almost everything.\r\n\r\nYou can no longer rely on having a kernel which supports modestly modern features like cgroupsv2 and cgroupsv1 support is being removed from systemd in a few months.\r\nSo this would mean we would need to keep older libraries for the sake of LTS distros.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-22T17:22:02Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).",
   "id": 1367940824,
   "line": 141,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 141,
   "original_position": 141,
   "original_start_line": 139,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 141,
   "pull_request_review_id": 1691465165,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 139,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-22T18:10:19Z",
   "user": {
    "id": 7258858,
    "login": "SuperSandro2000",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "systemd has support for this but I think they also plan to remove it because of the maintenance burden and not feature parity.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-22T17:24:20Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:",
   "id": 1367941146,
   "line": 160,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 160,
   "original_position": 160,
   "original_start_line": 158,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 160,
   "pull_request_review_id": 1691465165,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 158,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-22T18:10:19Z",
   "user": {
    "id": 7258858,
    "login": "SuperSandro2000",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Would this be more ergonomic to use than home-manager options for ssh_config which also use DAG and scale pretty bad when you have 20+ host entries which need to be in a specific order.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-22T17:33:18Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.",
   "id": 1367942436,
   "in_reply_to_id": 1359738742,
   "line": 436,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 436,
   "original_position": 436,
   "original_start_line": 433,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 436,
   "pull_request_review_id": 1691465165,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 433,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-22T18:10:19Z",
   "user": {
    "id": 7258858,
    "login": "SuperSandro2000",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "There is a good example of this in nixos, redis.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-22T17:34:47Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.\n+\n+For many kinds of services (e.g. PostgreSQL, Apache HTTP server etc), it\n+typically suffices to only have a single instance running on a machine. For\n+microservices, it may actually be quite useful to have the ability to easily\n+create multiple instances. For testing/experimentation purposes it may also be\n+desired to run multiple instances of a system service, such as a web server.",
   "id": 1367942611,
   "line": 492,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 492,
   "original_position": 492,
   "original_start_line": 490,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 492,
   "pull_request_review_id": 1691465165,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 490,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-22T18:10:19Z",
   "user": {
    "id": 7258858,
    "login": "SuperSandro2000",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "su is probably a bad example as it won't work very well without it's setuid bit.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-22T17:36:12Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.\n+\n+For many kinds of services (e.g. PostgreSQL, Apache HTTP server etc), it\n+typically suffices to only have a single instance running on a machine. For\n+microservices, it may actually be quite useful to have the ability to easily\n+create multiple instances. For testing/experimentation purposes it may also be\n+desired to run multiple instances of a system service, such as a web server.\n+\n+It is also not easily possible in NixOS to switch to alternative implementations\n+of a service. For many services, alternative implementations (e.g. `cron`,\n+`ssh`) are not required, but some services accept complex configurations, such\n+as the Apache HTTP server and Apache Tomcat.\n+\n+Finding a universal abstraction in NixOS to facilitate all possible use cases\n+may be impossible. Moreover, it may also be desired for developers to write\n+their own abstractions on top of existing functionality.\n+\n+To support abstractions and multiple instances, the Nix process management\n+framework does not use the NixOS module system, but a convention that is based\n+on the organisation of \"ordinary\" Nix packages in the Nixpkgs repository.\n+\n+In Nixpkgs, most package follow the convention that a separate Nix file defines\n+a function in which the function header refers to the build-time dependencies to\n+construct a package from source. Packages are composed in the top-level Nix\n+expression (`all-packages.nix`) that invokes these functions with the required\n+build-time parameters. For most packages, only a single variant is composed, but\n+it is also possible to build different kinds of package variants by changing the\n+function parameters.\n+\n+The Nix process management framework follows the same principles and extends the\n+Nixpkgs convention with a number of additional concepts:\n+\n+* Every service is instantiated from a *constructor* which is a nested function:\n+  the outer function refers to the build-time dependencies that are required to\n+  build the service from source code.\n+* The inner function refers to the service *instance parameters* -- calling the\n+  constructor with different parameters makes it possible to compose a service\n+  with an alternative configuration and in such a way that they do not conflict\n+  with other instances.\n+* The build-time dependencies of constructor functions are composed in a Nix\n+  expression that is typically called: `constructors.nix` in which every\n+  constructor function is called with its required build-time dependency\n+  parameters.\n+* The process instances of a system are composed in a Nix expression that is\n+  typically called: `processes.nix` in which every constructor function is\n+  called with its required instance parameters.\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];",
   "id": 1367942778,
   "line": 554,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 554,
   "original_position": 554,
   "original_start_line": null,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 554,
   "pull_request_review_id": 1691465165,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": null,
   "start_side": null,
   "subject_type": "line",
   "updated_at": "2023-10-22T18:10:19Z",
   "user": {
    "id": 7258858,
    "login": "SuperSandro2000",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "What if people are not interested in testing their changes with old init systems? We already have the same problem when package updates break on other platforms like darwin and it often creates traction and can be very frustrating. ",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-22T17:42:16Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.\n+\n+For many kinds of services (e.g. PostgreSQL, Apache HTTP server etc), it\n+typically suffices to only have a single instance running on a machine. For\n+microservices, it may actually be quite useful to have the ability to easily\n+create multiple instances. For testing/experimentation purposes it may also be\n+desired to run multiple instances of a system service, such as a web server.\n+\n+It is also not easily possible in NixOS to switch to alternative implementations\n+of a service. For many services, alternative implementations (e.g. `cron`,\n+`ssh`) are not required, but some services accept complex configurations, such\n+as the Apache HTTP server and Apache Tomcat.\n+\n+Finding a universal abstraction in NixOS to facilitate all possible use cases\n+may be impossible. Moreover, it may also be desired for developers to write\n+their own abstractions on top of existing functionality.\n+\n+To support abstractions and multiple instances, the Nix process management\n+framework does not use the NixOS module system, but a convention that is based\n+on the organisation of \"ordinary\" Nix packages in the Nixpkgs repository.\n+\n+In Nixpkgs, most package follow the convention that a separate Nix file defines\n+a function in which the function header refers to the build-time dependencies to\n+construct a package from source. Packages are composed in the top-level Nix\n+expression (`all-packages.nix`) that invokes these functions with the required\n+build-time parameters. For most packages, only a single variant is composed, but\n+it is also possible to build different kinds of package variants by changing the\n+function parameters.\n+\n+The Nix process management framework follows the same principles and extends the\n+Nixpkgs convention with a number of additional concepts:\n+\n+* Every service is instantiated from a *constructor* which is a nested function:\n+  the outer function refers to the build-time dependencies that are required to\n+  build the service from source code.\n+* The inner function refers to the service *instance parameters* -- calling the\n+  constructor with different parameters makes it possible to compose a service\n+  with an alternative configuration and in such a way that they do not conflict\n+  with other instances.\n+* The build-time dependencies of constructor functions are composed in a Nix\n+  expression that is typically called: `constructors.nix` in which every\n+  constructor function is called with its required build-time dependency\n+  parameters.\n+* The process instances of a system are composed in a Nix expression that is\n+  typically called: `processes.nix` in which every constructor function is\n+  called with its required instance parameters.\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    chown ${user}:${group} ${socketDir}\n+    chown ${user}:${group} ${dataDir}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        su ${user} -c '${postgresql}/bin/initdb -D ${dataDir} --no-locale'\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a constructor function for PostgreSQL and has the\n+following properties:\n+\n+* The outer function-header (first line) refer to the build inputs of the\n+  service. For example, to construct a configuration from a high-level\n+  specification for any supported process manager, we propagate the\n+  `createManagedProcess` as a parameter.\n+* The inner-function header refers to the instance parameters of a PostgreSQL\n+  instance. When it is desired to have two instances deployed to the same\n+  machine we must make sure that there is no conflict between the state of\n+  the two instances. Avoiding a conflict is possible by assigning a unique TCP\n+  port number and making sure that the state directories do not conflict. The\n+  `instanceName` parameter is appended to the name of the state directories to\n+  make this possible.\n+* In the body of the inner function, we invoke `createManagedProcess` to\n+  agnostically generate a configuration for a supported process manager.\n+  In the configuration, we provide an initialization script and we call the\n+  `postgres` service with its required command-line parameters and environment\n+  variables.\n+\n+As explained earlier, a constructor function is a nested function that we need to compose\n+twice. The build-time dependencies are composed in a Nix expression typically called\n+`constructors.nix`:\n+\n+```nix\n+{ nix-processmgmt ? ../../nix-processmgmt\n+, pkgs\n+, stateDir\n+, logDir\n+, runtimeDir\n+, cacheDir\n+, spoolDir\n+, libDir\n+, tmpDir\n+, callingUser ? null\n+, callingGroup ? null\n+, processManager\n+, ids ? {}\n+}:\n+\n+let\n+  createManagedProcess = import \"${nix-processmgmt}/nixproc/create-managed-process/universal/create-managed-process-universal.nix\" {\n+    inherit pkgs runtimeDir stateDir logDir tmpDir processManager ids;\n+  };\n+in\n+{\n+  postgresql = import ./postgresql {\n+    inherit createManagedProcess stateDir runtimeDir;\n+    inherit (pkgs) lib postgresql su;\n+  };\n+}\n+```\n+\n+In the above Nix expression, we invoke the `postgresql` constructor function and\n+we propagated its required build-time dependencies, such as the `postgresql`\n+package and the `createManagedProcess` abstraction function.\n+\n+The instance parameters are composed in a Nix expression that is typically called\n+`processes.nix`:\n+\n+```nix\n+{ pkgs ? import <nixpkgs> { inherit system; }\n+, system ? builtins.currentSystem\n+, stateDir ? \"/var\"\n+, runtimeDir ? \"${stateDir}/run\"\n+, logDir ? \"${stateDir}/log\"\n+, spoolDir ? \"${stateDir}/spool\"\n+, cacheDir ? \"${stateDir}/cache\"\n+, libDir ? \"${stateDir}/lib\"\n+, tmpDir ? (if stateDir == \"/var\" then \"/tmp\" else \"${stateDir}/tmp\")\n+, processManager\n+, nix-processmgmt ? ../../../nix-processmgmt\n+}:\n+\n+let\n+  constructors = import ../../services-agnostic/constructors.nix {\n+    inherit pkgs stateDir runtimeDir logDir tmpDir cacheDir spoolDir libDir processManager nix-processmgmt;\n+  };\n+in\n+rec {\n+  postgresql = rec {\n+    port = 5432;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+    };\n+  };\n+\n+  postgresql-secondary = rec {\n+    port = 5433;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+      instanceSuffix = \"-secondary\";\n+    };\n+  };\n+}\n+```\n+\n+In the above Nix expression, we compose two PostgreSQL instances that run on the\n+same machine simultaneously. This is possible due to the fact that they use\n+instance parameters that avoid their states to conflict.\n+\n+The above expression imports the `constructors.nix` expression so that each\n+process instance can be composed from the PostgreSQL constructor function.\n+\n+In NixOS, we use the NixOS module system that makes it possible to define\n+multiple aspects of a system in a module (such as packages, config files,\n+systemd units and environment variables) and combine the aspects of each module\n+into a single configuration from which a complete system is deployed, which is\n+very powerful.\n+\n+Combining the conventions of the nix-processmgmt framework with the NixOS module\n+system is something that still needs to be discussed. There are variety of\n+options possible, such as:\n+\n+* Combining the convention with process instances as described earlier by\n+  referring to a process instance from a NixOS module, similar to how NixOS\n+  modules can also refer to ordinary Nix packages\n+* Using sub modules, in which every sub module can define its own variant of a\n+  service.\n+\n+### Unprivileged user deployments\n+\n+Another interesting feature of Nix is to allow unprivileged users to deploy\n+packages. With services, this is also possible, but there are some things we\n+need to take into account, such as:\n+\n+* The state directories must refer to a directory that has writable permissions\n+  for the user\n+* We cannot bind to a TCP/UDP port below 1024\n+\n+We can also introduce a configuration flag and configure a service in such a way\n+that the above criteria are met:\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir, forceDisableUserChange}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    ${lib.optionalString (!forceDisableUserChange) ''\n+      chown ${user}:${group} ${socketDir}\n+      chown ${user}:${group} ${dataDir}\n+    ''}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        ${lib.optionalString (!forceDisableUserChange) \"su ${user} -c '\"}${postgresql}/bin/initdb -D ${dataDir} --no-locale${lib.optionalString (!forceDisableUserChange) \"'\"}\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a revised PostgreSQL constructor function that\n+accepts a new parameter: `forceDisableUserChange`. When this property is\n+enabled, the service configured in such a way that it does not change users,\n+which an unprivileged user is not allowed to do. With this setting enabled, it\n+becomes possible for an unprivileged user to deploy PostgreSQL.\n+\n+# Examples and Interactions\n+[examples-and-interactions]: #examples-and-interactions\n+\n+The `processes.nix` expression shown earlier can be used with a variety of tools\n+to deploy running process instances.\n+\n+The following command deploys the configurations as `sysvinit` scripts and\n+activates the configuration automatically:\n+\n+```bash\n+$ nixproc-sysvinit-switch processes.nix\n+```\n+\n+We can deploy the same configuration as systemd units with the following command:\n+\n+```bash\n+$ nixproc-systemd-switch processes.nix\n+```\n+\n+Or with `supervisord` with the following command:\n+\n+```bash\n+$ nixproc-supervisord-switch processes.nix\n+```\n+\n+As an unprivileged user, we can use the `--state-dir` parameter to change the\n+prefix of the state directories to reside in a user's home directory\n+(`/home/sander/var`) and disable user changing:\n+\n+```bash\n+$ nixproc-sysvinit-switch --state-dir /home/sander/var --force-disable-user-change processes.nix\n+```\n+\n+# Drawbacks\n+[drawbacks]: #drawbacks\n+\n+The ideas describes in this RFC makes the Nixpkgs eco-system more powerful, but\n+these new features come at at a price:\n+\n+* Using high-level abstractions hide the low-level configuration details of\n+  specific process managers. This makes it sometimes more difficult to reason\n+  about what is happening in problematic cases\n+* More testing is required -- we need to test services in multiple deployment\n+  scenarios, which is more expensive. Fortunately, I have seen that testing for\n+  two kinds of targets often give enough certainty: if you test with a\n+  foreground process (e.g. `systemd`) and a daemon (e.g. `sysvinit`) there is a\n+  big chance that it will work with other process managers as well.",
   "id": 1367943643,
   "line": 828,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 828,
   "original_position": 828,
   "original_start_line": 827,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 828,
   "pull_request_review_id": 1691465165,
   "reactions": {
    "+1": 2,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 2
   },
   "side": "RIGHT",
   "start_line": 827,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-22T18:10:19Z",
   "user": {
    "id": 7258858,
    "login": "SuperSandro2000",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "That will likely always be sysvinit which severely will hinder inovation and using new systemd features. Not a fan of this.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-22T17:46:28Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.\n+\n+For many kinds of services (e.g. PostgreSQL, Apache HTTP server etc), it\n+typically suffices to only have a single instance running on a machine. For\n+microservices, it may actually be quite useful to have the ability to easily\n+create multiple instances. For testing/experimentation purposes it may also be\n+desired to run multiple instances of a system service, such as a web server.\n+\n+It is also not easily possible in NixOS to switch to alternative implementations\n+of a service. For many services, alternative implementations (e.g. `cron`,\n+`ssh`) are not required, but some services accept complex configurations, such\n+as the Apache HTTP server and Apache Tomcat.\n+\n+Finding a universal abstraction in NixOS to facilitate all possible use cases\n+may be impossible. Moreover, it may also be desired for developers to write\n+their own abstractions on top of existing functionality.\n+\n+To support abstractions and multiple instances, the Nix process management\n+framework does not use the NixOS module system, but a convention that is based\n+on the organisation of \"ordinary\" Nix packages in the Nixpkgs repository.\n+\n+In Nixpkgs, most package follow the convention that a separate Nix file defines\n+a function in which the function header refers to the build-time dependencies to\n+construct a package from source. Packages are composed in the top-level Nix\n+expression (`all-packages.nix`) that invokes these functions with the required\n+build-time parameters. For most packages, only a single variant is composed, but\n+it is also possible to build different kinds of package variants by changing the\n+function parameters.\n+\n+The Nix process management framework follows the same principles and extends the\n+Nixpkgs convention with a number of additional concepts:\n+\n+* Every service is instantiated from a *constructor* which is a nested function:\n+  the outer function refers to the build-time dependencies that are required to\n+  build the service from source code.\n+* The inner function refers to the service *instance parameters* -- calling the\n+  constructor with different parameters makes it possible to compose a service\n+  with an alternative configuration and in such a way that they do not conflict\n+  with other instances.\n+* The build-time dependencies of constructor functions are composed in a Nix\n+  expression that is typically called: `constructors.nix` in which every\n+  constructor function is called with its required build-time dependency\n+  parameters.\n+* The process instances of a system are composed in a Nix expression that is\n+  typically called: `processes.nix` in which every constructor function is\n+  called with its required instance parameters.\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    chown ${user}:${group} ${socketDir}\n+    chown ${user}:${group} ${dataDir}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        su ${user} -c '${postgresql}/bin/initdb -D ${dataDir} --no-locale'\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a constructor function for PostgreSQL and has the\n+following properties:\n+\n+* The outer function-header (first line) refer to the build inputs of the\n+  service. For example, to construct a configuration from a high-level\n+  specification for any supported process manager, we propagate the\n+  `createManagedProcess` as a parameter.\n+* The inner-function header refers to the instance parameters of a PostgreSQL\n+  instance. When it is desired to have two instances deployed to the same\n+  machine we must make sure that there is no conflict between the state of\n+  the two instances. Avoiding a conflict is possible by assigning a unique TCP\n+  port number and making sure that the state directories do not conflict. The\n+  `instanceName` parameter is appended to the name of the state directories to\n+  make this possible.\n+* In the body of the inner function, we invoke `createManagedProcess` to\n+  agnostically generate a configuration for a supported process manager.\n+  In the configuration, we provide an initialization script and we call the\n+  `postgres` service with its required command-line parameters and environment\n+  variables.\n+\n+As explained earlier, a constructor function is a nested function that we need to compose\n+twice. The build-time dependencies are composed in a Nix expression typically called\n+`constructors.nix`:\n+\n+```nix\n+{ nix-processmgmt ? ../../nix-processmgmt\n+, pkgs\n+, stateDir\n+, logDir\n+, runtimeDir\n+, cacheDir\n+, spoolDir\n+, libDir\n+, tmpDir\n+, callingUser ? null\n+, callingGroup ? null\n+, processManager\n+, ids ? {}\n+}:\n+\n+let\n+  createManagedProcess = import \"${nix-processmgmt}/nixproc/create-managed-process/universal/create-managed-process-universal.nix\" {\n+    inherit pkgs runtimeDir stateDir logDir tmpDir processManager ids;\n+  };\n+in\n+{\n+  postgresql = import ./postgresql {\n+    inherit createManagedProcess stateDir runtimeDir;\n+    inherit (pkgs) lib postgresql su;\n+  };\n+}\n+```\n+\n+In the above Nix expression, we invoke the `postgresql` constructor function and\n+we propagated its required build-time dependencies, such as the `postgresql`\n+package and the `createManagedProcess` abstraction function.\n+\n+The instance parameters are composed in a Nix expression that is typically called\n+`processes.nix`:\n+\n+```nix\n+{ pkgs ? import <nixpkgs> { inherit system; }\n+, system ? builtins.currentSystem\n+, stateDir ? \"/var\"\n+, runtimeDir ? \"${stateDir}/run\"\n+, logDir ? \"${stateDir}/log\"\n+, spoolDir ? \"${stateDir}/spool\"\n+, cacheDir ? \"${stateDir}/cache\"\n+, libDir ? \"${stateDir}/lib\"\n+, tmpDir ? (if stateDir == \"/var\" then \"/tmp\" else \"${stateDir}/tmp\")\n+, processManager\n+, nix-processmgmt ? ../../../nix-processmgmt\n+}:\n+\n+let\n+  constructors = import ../../services-agnostic/constructors.nix {\n+    inherit pkgs stateDir runtimeDir logDir tmpDir cacheDir spoolDir libDir processManager nix-processmgmt;\n+  };\n+in\n+rec {\n+  postgresql = rec {\n+    port = 5432;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+    };\n+  };\n+\n+  postgresql-secondary = rec {\n+    port = 5433;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+      instanceSuffix = \"-secondary\";\n+    };\n+  };\n+}\n+```\n+\n+In the above Nix expression, we compose two PostgreSQL instances that run on the\n+same machine simultaneously. This is possible due to the fact that they use\n+instance parameters that avoid their states to conflict.\n+\n+The above expression imports the `constructors.nix` expression so that each\n+process instance can be composed from the PostgreSQL constructor function.\n+\n+In NixOS, we use the NixOS module system that makes it possible to define\n+multiple aspects of a system in a module (such as packages, config files,\n+systemd units and environment variables) and combine the aspects of each module\n+into a single configuration from which a complete system is deployed, which is\n+very powerful.\n+\n+Combining the conventions of the nix-processmgmt framework with the NixOS module\n+system is something that still needs to be discussed. There are variety of\n+options possible, such as:\n+\n+* Combining the convention with process instances as described earlier by\n+  referring to a process instance from a NixOS module, similar to how NixOS\n+  modules can also refer to ordinary Nix packages\n+* Using sub modules, in which every sub module can define its own variant of a\n+  service.\n+\n+### Unprivileged user deployments\n+\n+Another interesting feature of Nix is to allow unprivileged users to deploy\n+packages. With services, this is also possible, but there are some things we\n+need to take into account, such as:\n+\n+* The state directories must refer to a directory that has writable permissions\n+  for the user\n+* We cannot bind to a TCP/UDP port below 1024\n+\n+We can also introduce a configuration flag and configure a service in such a way\n+that the above criteria are met:\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir, forceDisableUserChange}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    ${lib.optionalString (!forceDisableUserChange) ''\n+      chown ${user}:${group} ${socketDir}\n+      chown ${user}:${group} ${dataDir}\n+    ''}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        ${lib.optionalString (!forceDisableUserChange) \"su ${user} -c '\"}${postgresql}/bin/initdb -D ${dataDir} --no-locale${lib.optionalString (!forceDisableUserChange) \"'\"}\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a revised PostgreSQL constructor function that\n+accepts a new parameter: `forceDisableUserChange`. When this property is\n+enabled, the service configured in such a way that it does not change users,\n+which an unprivileged user is not allowed to do. With this setting enabled, it\n+becomes possible for an unprivileged user to deploy PostgreSQL.\n+\n+# Examples and Interactions\n+[examples-and-interactions]: #examples-and-interactions\n+\n+The `processes.nix` expression shown earlier can be used with a variety of tools\n+to deploy running process instances.\n+\n+The following command deploys the configurations as `sysvinit` scripts and\n+activates the configuration automatically:\n+\n+```bash\n+$ nixproc-sysvinit-switch processes.nix\n+```\n+\n+We can deploy the same configuration as systemd units with the following command:\n+\n+```bash\n+$ nixproc-systemd-switch processes.nix\n+```\n+\n+Or with `supervisord` with the following command:\n+\n+```bash\n+$ nixproc-supervisord-switch processes.nix\n+```\n+\n+As an unprivileged user, we can use the `--state-dir` parameter to change the\n+prefix of the state directories to reside in a user's home directory\n+(`/home/sander/var`) and disable user changing:\n+\n+```bash\n+$ nixproc-sysvinit-switch --state-dir /home/sander/var --force-disable-user-change processes.nix\n+```\n+\n+# Drawbacks\n+[drawbacks]: #drawbacks\n+\n+The ideas describes in this RFC makes the Nixpkgs eco-system more powerful, but\n+these new features come at at a price:\n+\n+* Using high-level abstractions hide the low-level configuration details of\n+  specific process managers. This makes it sometimes more difficult to reason\n+  about what is happening in problematic cases\n+* More testing is required -- we need to test services in multiple deployment\n+  scenarios, which is more expensive. Fortunately, I have seen that testing for\n+  two kinds of targets often give enough certainty: if you test with a\n+  foreground process (e.g. `systemd`) and a daemon (e.g. `sysvinit`) there is a\n+  big chance that it will work with other process managers as well.\n+* You may target the lowest common denominator. This is not actually true,",
   "id": 1367944153,
   "line": 829,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 829,
   "original_position": 829,
   "original_start_line": null,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 829,
   "pull_request_review_id": 1691465165,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": null,
   "start_side": null,
   "subject_type": "line",
   "updated_at": "2023-10-22T18:10:19Z",
   "user": {
    "id": 7258858,
    "login": "SuperSandro2000",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "There is a big reason why people stopped doing this: The problem systemd tries to solve is inheritely complex and services are getting more and more complex. Solving this with shellscript became harder and harder and shelling out to many other programs to solve this complexity is slow and error prone.\r\n\r\nAlso old init systems ran in sequence, not parallel like systemd which makes it a lot faster by default but doing parallel things in shellscripts is extremely hard.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-22T17:51:15Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.\n+\n+For many kinds of services (e.g. PostgreSQL, Apache HTTP server etc), it\n+typically suffices to only have a single instance running on a machine. For\n+microservices, it may actually be quite useful to have the ability to easily\n+create multiple instances. For testing/experimentation purposes it may also be\n+desired to run multiple instances of a system service, such as a web server.\n+\n+It is also not easily possible in NixOS to switch to alternative implementations\n+of a service. For many services, alternative implementations (e.g. `cron`,\n+`ssh`) are not required, but some services accept complex configurations, such\n+as the Apache HTTP server and Apache Tomcat.\n+\n+Finding a universal abstraction in NixOS to facilitate all possible use cases\n+may be impossible. Moreover, it may also be desired for developers to write\n+their own abstractions on top of existing functionality.\n+\n+To support abstractions and multiple instances, the Nix process management\n+framework does not use the NixOS module system, but a convention that is based\n+on the organisation of \"ordinary\" Nix packages in the Nixpkgs repository.\n+\n+In Nixpkgs, most package follow the convention that a separate Nix file defines\n+a function in which the function header refers to the build-time dependencies to\n+construct a package from source. Packages are composed in the top-level Nix\n+expression (`all-packages.nix`) that invokes these functions with the required\n+build-time parameters. For most packages, only a single variant is composed, but\n+it is also possible to build different kinds of package variants by changing the\n+function parameters.\n+\n+The Nix process management framework follows the same principles and extends the\n+Nixpkgs convention with a number of additional concepts:\n+\n+* Every service is instantiated from a *constructor* which is a nested function:\n+  the outer function refers to the build-time dependencies that are required to\n+  build the service from source code.\n+* The inner function refers to the service *instance parameters* -- calling the\n+  constructor with different parameters makes it possible to compose a service\n+  with an alternative configuration and in such a way that they do not conflict\n+  with other instances.\n+* The build-time dependencies of constructor functions are composed in a Nix\n+  expression that is typically called: `constructors.nix` in which every\n+  constructor function is called with its required build-time dependency\n+  parameters.\n+* The process instances of a system are composed in a Nix expression that is\n+  typically called: `processes.nix` in which every constructor function is\n+  called with its required instance parameters.\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    chown ${user}:${group} ${socketDir}\n+    chown ${user}:${group} ${dataDir}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        su ${user} -c '${postgresql}/bin/initdb -D ${dataDir} --no-locale'\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a constructor function for PostgreSQL and has the\n+following properties:\n+\n+* The outer function-header (first line) refer to the build inputs of the\n+  service. For example, to construct a configuration from a high-level\n+  specification for any supported process manager, we propagate the\n+  `createManagedProcess` as a parameter.\n+* The inner-function header refers to the instance parameters of a PostgreSQL\n+  instance. When it is desired to have two instances deployed to the same\n+  machine we must make sure that there is no conflict between the state of\n+  the two instances. Avoiding a conflict is possible by assigning a unique TCP\n+  port number and making sure that the state directories do not conflict. The\n+  `instanceName` parameter is appended to the name of the state directories to\n+  make this possible.\n+* In the body of the inner function, we invoke `createManagedProcess` to\n+  agnostically generate a configuration for a supported process manager.\n+  In the configuration, we provide an initialization script and we call the\n+  `postgres` service with its required command-line parameters and environment\n+  variables.\n+\n+As explained earlier, a constructor function is a nested function that we need to compose\n+twice. The build-time dependencies are composed in a Nix expression typically called\n+`constructors.nix`:\n+\n+```nix\n+{ nix-processmgmt ? ../../nix-processmgmt\n+, pkgs\n+, stateDir\n+, logDir\n+, runtimeDir\n+, cacheDir\n+, spoolDir\n+, libDir\n+, tmpDir\n+, callingUser ? null\n+, callingGroup ? null\n+, processManager\n+, ids ? {}\n+}:\n+\n+let\n+  createManagedProcess = import \"${nix-processmgmt}/nixproc/create-managed-process/universal/create-managed-process-universal.nix\" {\n+    inherit pkgs runtimeDir stateDir logDir tmpDir processManager ids;\n+  };\n+in\n+{\n+  postgresql = import ./postgresql {\n+    inherit createManagedProcess stateDir runtimeDir;\n+    inherit (pkgs) lib postgresql su;\n+  };\n+}\n+```\n+\n+In the above Nix expression, we invoke the `postgresql` constructor function and\n+we propagated its required build-time dependencies, such as the `postgresql`\n+package and the `createManagedProcess` abstraction function.\n+\n+The instance parameters are composed in a Nix expression that is typically called\n+`processes.nix`:\n+\n+```nix\n+{ pkgs ? import <nixpkgs> { inherit system; }\n+, system ? builtins.currentSystem\n+, stateDir ? \"/var\"\n+, runtimeDir ? \"${stateDir}/run\"\n+, logDir ? \"${stateDir}/log\"\n+, spoolDir ? \"${stateDir}/spool\"\n+, cacheDir ? \"${stateDir}/cache\"\n+, libDir ? \"${stateDir}/lib\"\n+, tmpDir ? (if stateDir == \"/var\" then \"/tmp\" else \"${stateDir}/tmp\")\n+, processManager\n+, nix-processmgmt ? ../../../nix-processmgmt\n+}:\n+\n+let\n+  constructors = import ../../services-agnostic/constructors.nix {\n+    inherit pkgs stateDir runtimeDir logDir tmpDir cacheDir spoolDir libDir processManager nix-processmgmt;\n+  };\n+in\n+rec {\n+  postgresql = rec {\n+    port = 5432;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+    };\n+  };\n+\n+  postgresql-secondary = rec {\n+    port = 5433;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+      instanceSuffix = \"-secondary\";\n+    };\n+  };\n+}\n+```\n+\n+In the above Nix expression, we compose two PostgreSQL instances that run on the\n+same machine simultaneously. This is possible due to the fact that they use\n+instance parameters that avoid their states to conflict.\n+\n+The above expression imports the `constructors.nix` expression so that each\n+process instance can be composed from the PostgreSQL constructor function.\n+\n+In NixOS, we use the NixOS module system that makes it possible to define\n+multiple aspects of a system in a module (such as packages, config files,\n+systemd units and environment variables) and combine the aspects of each module\n+into a single configuration from which a complete system is deployed, which is\n+very powerful.\n+\n+Combining the conventions of the nix-processmgmt framework with the NixOS module\n+system is something that still needs to be discussed. There are variety of\n+options possible, such as:\n+\n+* Combining the convention with process instances as described earlier by\n+  referring to a process instance from a NixOS module, similar to how NixOS\n+  modules can also refer to ordinary Nix packages\n+* Using sub modules, in which every sub module can define its own variant of a\n+  service.\n+\n+### Unprivileged user deployments\n+\n+Another interesting feature of Nix is to allow unprivileged users to deploy\n+packages. With services, this is also possible, but there are some things we\n+need to take into account, such as:\n+\n+* The state directories must refer to a directory that has writable permissions\n+  for the user\n+* We cannot bind to a TCP/UDP port below 1024\n+\n+We can also introduce a configuration flag and configure a service in such a way\n+that the above criteria are met:\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir, forceDisableUserChange}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    ${lib.optionalString (!forceDisableUserChange) ''\n+      chown ${user}:${group} ${socketDir}\n+      chown ${user}:${group} ${dataDir}\n+    ''}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        ${lib.optionalString (!forceDisableUserChange) \"su ${user} -c '\"}${postgresql}/bin/initdb -D ${dataDir} --no-locale${lib.optionalString (!forceDisableUserChange) \"'\"}\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a revised PostgreSQL constructor function that\n+accepts a new parameter: `forceDisableUserChange`. When this property is\n+enabled, the service configured in such a way that it does not change users,\n+which an unprivileged user is not allowed to do. With this setting enabled, it\n+becomes possible for an unprivileged user to deploy PostgreSQL.\n+\n+# Examples and Interactions\n+[examples-and-interactions]: #examples-and-interactions\n+\n+The `processes.nix` expression shown earlier can be used with a variety of tools\n+to deploy running process instances.\n+\n+The following command deploys the configurations as `sysvinit` scripts and\n+activates the configuration automatically:\n+\n+```bash\n+$ nixproc-sysvinit-switch processes.nix\n+```\n+\n+We can deploy the same configuration as systemd units with the following command:\n+\n+```bash\n+$ nixproc-systemd-switch processes.nix\n+```\n+\n+Or with `supervisord` with the following command:\n+\n+```bash\n+$ nixproc-supervisord-switch processes.nix\n+```\n+\n+As an unprivileged user, we can use the `--state-dir` parameter to change the\n+prefix of the state directories to reside in a user's home directory\n+(`/home/sander/var`) and disable user changing:\n+\n+```bash\n+$ nixproc-sysvinit-switch --state-dir /home/sander/var --force-disable-user-change processes.nix\n+```\n+\n+# Drawbacks\n+[drawbacks]: #drawbacks\n+\n+The ideas describes in this RFC makes the Nixpkgs eco-system more powerful, but\n+these new features come at at a price:\n+\n+* Using high-level abstractions hide the low-level configuration details of\n+  specific process managers. This makes it sometimes more difficult to reason\n+  about what is happening in problematic cases\n+* More testing is required -- we need to test services in multiple deployment\n+  scenarios, which is more expensive. Fortunately, I have seen that testing for\n+  two kinds of targets often give enough certainty: if you test with a\n+  foreground process (e.g. `systemd`) and a daemon (e.g. `sysvinit`) there is a\n+  big chance that it will work with other process managers as well.\n+* You may target the lowest common denominator. This is not actually true,\n+  because with overrides it is still possible to use any feature of a process\n+  manager. Moreover, not all services need to be portable -- low-level services\n+  such as `cups` and `syslogd` can still be packaged in a process\n+  manager-specific way.\n+\n+# Alternatives\n+[alternatives]: #alternatives\n+\n+This RFC proposes a new strategy to allow multiple process instances of the\n+same service to co-exist. Co-existence is possible by avoiding conflicts --\n+making sure that state directories and port numbers do not conflict through\n+function parameters.\n+\n+It is also possible to isolate services by using concepts such as Linux\n+namespaces. Then it is not required to avoid certain kinds of conflicts.\n+Unfortunately, using these features is restricted to Linux only -- this RFC aims\n+to be operating system agnostic.\n+\n+# Prior art\n+[prior-art]: #prior-art\n+\n+This RFC is not the first approach that combines Nix with a process management\n+solution:\n+\n+* [NixOS](https://nixos.org) uses `systemd` as its process management solution.\n+  Services can be deployed by generating systemd unit configuration files in a\n+  Nix expression. NixOS deploys `systemd` units globally and requires super\n+  user privileges to get deployed. Moreover, you cannot use the service layer\n+  independently from the rest of NixOS.\n+* [nix-darwin](https://github.com/LnL7/nix-darwin) provides various NixOS\n+  features on macOS/Darwin, including a service management layer that uses the\n+  process manager of macOS: `launchd`.\n+* [home-manager](https://github.com/nix-community/home-manager) offers a service\n+  layer that works with systemd user services, rather than system services. It\n+  contains its own set of service configurations that can be deployed as user\n+  services.\n+* [system-manager](https://github.com/numtide/system-manager) addresses the\n+  limitation of NixOS to use systemd units independently and can deploy many\n+  services in the NixOS repository on any Ubuntu-based distribution.\n+* [nixos-init-freedom](https://git.sr.ht/~guido/nixos-init-freedom) puts some\n+  efforts on replacing systemd-as-pid-1 with s6. Instead of implementing a\n+  generic service layer, it translates parts of systemd module configuration\n+  into s6 configuration.\n+\n+The biggest drawback is that these solutions are not generic and there is only\n+little reuse possible between service configurations.\n+\n+There are also solutions that introduce a portable approach for managing\n+processes in combination Nix:\n+\n+* The service deployment chapter (chapter 9) of\n+  [Eelco Dolstra's PhD thesis](https://github.com/edolstra/edolstra.github.io/blob/master/pubs/phd-thesis.pdf)\n+  describes a very early service deployment approach that uses generated\n+  start/stop scripts implemented in `bash`. Because shell scripts are so common,\n+  they can be used on a variety of operating systems -- in addition to Linux, it\n+  can also be used on Darwin and FreeBSD.",
   "id": 1367944884,
   "line": 885,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 885,
   "original_position": 885,
   "original_start_line": 883,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 885,
   "pull_request_review_id": 1691465165,
   "reactions": {
    "+1": 8,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 8
   },
   "side": "RIGHT",
   "start_line": 883,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-22T18:10:19Z",
   "user": {
    "id": 7258858,
    "login": "SuperSandro2000",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This speaks big for modern process managers. With systemd processes cannot that easily become rogue and linger around in your system. For a small embedded system like my router that is not a problem but on one of my servers PIDs are already above the 4M mark and modern software just solved this problem. I do not think it is time efficient to ignore those conveniences.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-22T18:06:24Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.",
   "id": 1367946958,
   "line": 402,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 402,
   "original_position": 402,
   "original_start_line": 400,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 402,
   "pull_request_review_id": 1691465165,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 400,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-22T18:10:19Z",
   "user": {
    "id": 7258858,
    "login": "SuperSandro2000",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "@roberth maybe i'm a bit dense and this doesn't make automatic sense to me... but i was really hoping you could explain your ideas to me - sounds really interesting.\r\n\r\nsorry for the hassle!",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-22T20:13:26Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.",
   "id": 1367963355,
   "in_reply_to_id": 1347854623,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 486,
   "original_position": 486,
   "original_start_line": 482,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1691485641,
   "reactions": {
    "+1": 1,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 1
   },
   "side": "RIGHT",
   "start_line": 482,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-22T20:13:27Z",
   "user": {
    "id": 7755101,
    "login": "aanderse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> I don't want this to take presence in NixOS modules to have 5 workarounds for all kinds of distros in every module. \r\n\r\nThis is more or less similar to a compiler keeping different backends and frontends. The workarounds should be treated as distro-dependent code.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-10-22T21:06:31Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.",
   "id": 1367969656,
   "in_reply_to_id": 1367939326,
   "line": 17,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 17,
   "original_position": 17,
   "original_start_line": 15,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 17,
   "pull_request_review_id": 1691490717,
   "reactions": {
    "+1": 2,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 2
   },
   "side": "RIGHT",
   "start_line": 15,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-10-22T21:06:31Z",
   "user": {
    "id": 5954806,
    "login": "AndersonTorres",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> This proposal might combine very well with the new `pkgs/by-name` directory introduced with [RFC 140](https://github.com/NixOS/rfcs/pull/140), because it could be extended to support a standard `service.nix` file to specify a standardised service definition for a package, without having to be tied to NixOS.\r\n\r\nThere is already at least one project doing exactly this.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-11-05T09:22:20Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.\n+\n+For many kinds of services (e.g. PostgreSQL, Apache HTTP server etc), it\n+typically suffices to only have a single instance running on a machine. For\n+microservices, it may actually be quite useful to have the ability to easily\n+create multiple instances. For testing/experimentation purposes it may also be\n+desired to run multiple instances of a system service, such as a web server.\n+\n+It is also not easily possible in NixOS to switch to alternative implementations\n+of a service. For many services, alternative implementations (e.g. `cron`,\n+`ssh`) are not required, but some services accept complex configurations, such\n+as the Apache HTTP server and Apache Tomcat.\n+\n+Finding a universal abstraction in NixOS to facilitate all possible use cases\n+may be impossible. Moreover, it may also be desired for developers to write\n+their own abstractions on top of existing functionality.\n+\n+To support abstractions and multiple instances, the Nix process management\n+framework does not use the NixOS module system, but a convention that is based\n+on the organisation of \"ordinary\" Nix packages in the Nixpkgs repository.\n+\n+In Nixpkgs, most package follow the convention that a separate Nix file defines\n+a function in which the function header refers to the build-time dependencies to\n+construct a package from source. Packages are composed in the top-level Nix\n+expression (`all-packages.nix`) that invokes these functions with the required\n+build-time parameters. For most packages, only a single variant is composed, but\n+it is also possible to build different kinds of package variants by changing the\n+function parameters.\n+\n+The Nix process management framework follows the same principles and extends the\n+Nixpkgs convention with a number of additional concepts:\n+\n+* Every service is instantiated from a *constructor* which is a nested function:\n+  the outer function refers to the build-time dependencies that are required to\n+  build the service from source code.\n+* The inner function refers to the service *instance parameters* -- calling the\n+  constructor with different parameters makes it possible to compose a service\n+  with an alternative configuration and in such a way that they do not conflict\n+  with other instances.\n+* The build-time dependencies of constructor functions are composed in a Nix\n+  expression that is typically called: `constructors.nix` in which every\n+  constructor function is called with its required build-time dependency\n+  parameters.\n+* The process instances of a system are composed in a Nix expression that is\n+  typically called: `processes.nix` in which every constructor function is\n+  called with its required instance parameters.\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    chown ${user}:${group} ${socketDir}\n+    chown ${user}:${group} ${dataDir}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        su ${user} -c '${postgresql}/bin/initdb -D ${dataDir} --no-locale'\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a constructor function for PostgreSQL and has the\n+following properties:\n+\n+* The outer function-header (first line) refer to the build inputs of the\n+  service. For example, to construct a configuration from a high-level\n+  specification for any supported process manager, we propagate the\n+  `createManagedProcess` as a parameter.\n+* The inner-function header refers to the instance parameters of a PostgreSQL\n+  instance. When it is desired to have two instances deployed to the same\n+  machine we must make sure that there is no conflict between the state of\n+  the two instances. Avoiding a conflict is possible by assigning a unique TCP\n+  port number and making sure that the state directories do not conflict. The\n+  `instanceName` parameter is appended to the name of the state directories to\n+  make this possible.\n+* In the body of the inner function, we invoke `createManagedProcess` to\n+  agnostically generate a configuration for a supported process manager.\n+  In the configuration, we provide an initialization script and we call the\n+  `postgres` service with its required command-line parameters and environment\n+  variables.\n+\n+As explained earlier, a constructor function is a nested function that we need to compose\n+twice. The build-time dependencies are composed in a Nix expression typically called\n+`constructors.nix`:\n+\n+```nix\n+{ nix-processmgmt ? ../../nix-processmgmt\n+, pkgs\n+, stateDir\n+, logDir\n+, runtimeDir\n+, cacheDir\n+, spoolDir\n+, libDir\n+, tmpDir\n+, callingUser ? null\n+, callingGroup ? null\n+, processManager\n+, ids ? {}\n+}:\n+\n+let\n+  createManagedProcess = import \"${nix-processmgmt}/nixproc/create-managed-process/universal/create-managed-process-universal.nix\" {\n+    inherit pkgs runtimeDir stateDir logDir tmpDir processManager ids;\n+  };\n+in\n+{\n+  postgresql = import ./postgresql {\n+    inherit createManagedProcess stateDir runtimeDir;\n+    inherit (pkgs) lib postgresql su;\n+  };\n+}\n+```\n+\n+In the above Nix expression, we invoke the `postgresql` constructor function and\n+we propagated its required build-time dependencies, such as the `postgresql`\n+package and the `createManagedProcess` abstraction function.\n+\n+The instance parameters are composed in a Nix expression that is typically called\n+`processes.nix`:\n+\n+```nix\n+{ pkgs ? import <nixpkgs> { inherit system; }\n+, system ? builtins.currentSystem\n+, stateDir ? \"/var\"\n+, runtimeDir ? \"${stateDir}/run\"\n+, logDir ? \"${stateDir}/log\"\n+, spoolDir ? \"${stateDir}/spool\"\n+, cacheDir ? \"${stateDir}/cache\"\n+, libDir ? \"${stateDir}/lib\"\n+, tmpDir ? (if stateDir == \"/var\" then \"/tmp\" else \"${stateDir}/tmp\")\n+, processManager\n+, nix-processmgmt ? ../../../nix-processmgmt\n+}:\n+\n+let\n+  constructors = import ../../services-agnostic/constructors.nix {\n+    inherit pkgs stateDir runtimeDir logDir tmpDir cacheDir spoolDir libDir processManager nix-processmgmt;\n+  };\n+in\n+rec {\n+  postgresql = rec {\n+    port = 5432;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+    };\n+  };\n+\n+  postgresql-secondary = rec {\n+    port = 5433;\n+\n+    pkg = constructors.simplePostgresql {\n+      inherit port;\n+      instanceSuffix = \"-secondary\";\n+    };\n+  };\n+}\n+```\n+\n+In the above Nix expression, we compose two PostgreSQL instances that run on the\n+same machine simultaneously. This is possible due to the fact that they use\n+instance parameters that avoid their states to conflict.\n+\n+The above expression imports the `constructors.nix` expression so that each\n+process instance can be composed from the PostgreSQL constructor function.\n+\n+In NixOS, we use the NixOS module system that makes it possible to define\n+multiple aspects of a system in a module (such as packages, config files,\n+systemd units and environment variables) and combine the aspects of each module\n+into a single configuration from which a complete system is deployed, which is\n+very powerful.\n+\n+Combining the conventions of the nix-processmgmt framework with the NixOS module\n+system is something that still needs to be discussed. There are variety of\n+options possible, such as:\n+\n+* Combining the convention with process instances as described earlier by\n+  referring to a process instance from a NixOS module, similar to how NixOS\n+  modules can also refer to ordinary Nix packages\n+* Using sub modules, in which every sub module can define its own variant of a\n+  service.\n+\n+### Unprivileged user deployments\n+\n+Another interesting feature of Nix is to allow unprivileged users to deploy\n+packages. With services, this is also possible, but there are some things we\n+need to take into account, such as:\n+\n+* The state directories must refer to a directory that has writable permissions\n+  for the user\n+* We cannot bind to a TCP/UDP port below 1024\n+\n+We can also introduce a configuration flag and configure a service in such a way\n+that the above criteria are met:\n+\n+```nix\n+{createManagedProcess, lib, postgresql, su, stateDir, runtimeDir, forceDisableUserChange}:\n+\n+{ port ? 5432\n+, instanceSuffix ? \"\"\n+, instanceName ? \"postgresql${instanceSuffix}\"\n+, configFile ? null\n+, postInstall ? \"\"\n+}:\n+\n+let\n+  postgresqlStateDir = \"${stateDir}/db/${instanceName}\";\n+  dataDir = \"${postgresqlStateDir}/data\";\n+  socketDir = \"${runtimeDir}/${instanceName}\";\n+\n+  user = instanceName;\n+  group = instanceName;\n+in\n+createManagedProcess rec {\n+  inherit instanceName user postInstall;\n+\n+  path = [ postgresql su ];\n+  initialize = ''\n+    mkdir -m0755 -p ${socketDir}\n+    mkdir -m0700 -p ${dataDir}\n+\n+    ${lib.optionalString (!forceDisableUserChange) ''\n+      chown ${user}:${group} ${socketDir}\n+      chown ${user}:${group} ${dataDir}\n+    ''}\n+\n+    if [ ! -e \"${dataDir}/PG_VERSION\" ]\n+    then\n+        ${lib.optionalString (!forceDisableUserChange) \"su ${user} -c '\"}${postgresql}/bin/initdb -D ${dataDir} --no-locale${lib.optionalString (!forceDisableUserChange) \"'\"}\n+    fi\n+\n+    ${lib.optionalString (configFile != null) ''\n+      ln -sfn ${configFile} ${dataDir}/postgresql.conf\n+    ''}\n+  '';\n+\n+  foregroundProcess = \"${postgresql}/bin/postgres\";\n+  args = [ \"-D\" dataDir \"-p\" port \"-k\" socketDir ];\n+  environment = {\n+    PGDATA = dataDir;\n+  };\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+The above Nix expression shows a revised PostgreSQL constructor function that\n+accepts a new parameter: `forceDisableUserChange`. When this property is\n+enabled, the service configured in such a way that it does not change users,\n+which an unprivileged user is not allowed to do. With this setting enabled, it\n+becomes possible for an unprivileged user to deploy PostgreSQL.\n+\n+# Examples and Interactions\n+[examples-and-interactions]: #examples-and-interactions\n+\n+The `processes.nix` expression shown earlier can be used with a variety of tools\n+to deploy running process instances.\n+\n+The following command deploys the configurations as `sysvinit` scripts and\n+activates the configuration automatically:\n+\n+```bash\n+$ nixproc-sysvinit-switch processes.nix\n+```\n+\n+We can deploy the same configuration as systemd units with the following command:\n+\n+```bash\n+$ nixproc-systemd-switch processes.nix\n+```\n+\n+Or with `supervisord` with the following command:\n+\n+```bash\n+$ nixproc-supervisord-switch processes.nix\n+```\n+\n+As an unprivileged user, we can use the `--state-dir` parameter to change the\n+prefix of the state directories to reside in a user's home directory\n+(`/home/sander/var`) and disable user changing:\n+\n+```bash\n+$ nixproc-sysvinit-switch --state-dir /home/sander/var --force-disable-user-change processes.nix\n+```\n+\n+# Drawbacks\n+[drawbacks]: #drawbacks\n+\n+The ideas describes in this RFC makes the Nixpkgs eco-system more powerful, but\n+these new features come at at a price:\n+\n+* Using high-level abstractions hide the low-level configuration details of\n+  specific process managers. This makes it sometimes more difficult to reason\n+  about what is happening in problematic cases\n+* More testing is required -- we need to test services in multiple deployment\n+  scenarios, which is more expensive. Fortunately, I have seen that testing for\n+  two kinds of targets often give enough certainty: if you test with a\n+  foreground process (e.g. `systemd`) and a daemon (e.g. `sysvinit`) there is a\n+  big chance that it will work with other process managers as well.\n+* You may target the lowest common denominator. This is not actually true,\n+  because with overrides it is still possible to use any feature of a process\n+  manager. Moreover, not all services need to be portable -- low-level services\n+  such as `cups` and `syslogd` can still be packaged in a process\n+  manager-specific way.\n+\n+# Alternatives\n+[alternatives]: #alternatives\n+\n+This RFC proposes a new strategy to allow multiple process instances of the\n+same service to co-exist. Co-existence is possible by avoiding conflicts --\n+making sure that state directories and port numbers do not conflict through\n+function parameters.\n+\n+It is also possible to isolate services by using concepts such as Linux\n+namespaces. Then it is not required to avoid certain kinds of conflicts.\n+Unfortunately, using these features is restricted to Linux only -- this RFC aims\n+to be operating system agnostic.\n+\n+# Prior art\n+[prior-art]: #prior-art\n+\n+This RFC is not the first approach that combines Nix with a process management\n+solution:\n+\n+* The service deployment chapter (chapter 9) of\n+  [Eelco Dolstra's PhD thesis](https://github.com/edolstra/edolstra.github.io/blob/master/pubs/phd-thesis.pdf)\n+  describes a very early service deployment approach that uses generated\n+  start/stop scripts implemented in `bash`. Because shell scripts are so common,\n+  they can be used on a variety of operating systems -- in addition to Linux, it\n+  can also be used on Darwin and FreeBSD.\n+* [NixOS](https://nixos.org) uses `systemd` as its process management solution.\n+  Services can be deployed by generating systemd unit configuration files in a\n+  Nix expression. NixOS deploys `systemd` units globally and requires super\n+  user privileges to get deployed. Moreover, you cannot use the service layer\n+  independently from the rest of NixOS.\n+* [nix-darwin](https://github.com/LnL7/nix-darwin) provides various NixOS\n+  features on macOS/Darwin, including a service management layer that uses the\n+  process manager of macOS: `launchd`.\n+* [home-manager](https://github.com/nix-community/home-manager) offers a service\n+  layer that works with systemd user services, rather than system services. It\n+  contains its own set of service configurations that can be deployed as user\n+  services.\n+* [system-manager](https://github.com/numtide/system-manager) addresses the\n+  limitation of NixOS to use systemd units independently and can deploy many\n+  services in the NixOS repository on any Ubuntu-based distribution.\n+\n+Aside from the first option, the biggest drawback is that these solutions are\n+not generic and there is only little reuse possible between service\n+configurations.\n+\n+# Unresolved questions\n+[unresolved]: #unresolved-questions\n+\n+The integration with NixOS module system still needs to be worked out in detail.\n+Currently, I can think of two scenarios. For this a discussion in the Nix\n+community is required.",
   "id": 1382538261,
   "in_reply_to_id": 1347861030,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 885,
   "original_position": 885,
   "original_start_line": 883,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1713946705,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 883,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-11-05T09:22:20Z",
   "user": null
  },
  {
   "body": "> I even created [a gist](https://gist.github.com/infinisil/e29ee58a8184eeb7a2e139c2b2d3e4ff) for you to demonstrate how the module system can be used to support instances of services.\r\n\r\nThe fact that there are no examples of this technique actually being used is probably meaningful here.\r\n\r\n@svanderburg's point remains: with Nix functions and packages you don't have to do anything special in order to \"multi-instance enable\" them.  It's automatic.  The gist above requires extra work in order to make a service \"multi-instance aware\", and comes at a cost to people who only want one instance.\r\n\r\nWe should strive for a service abstraction that makes multiple instances effortless, the way `/nix/store` makes it effortless to have multiple versions of the same package installed.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-11-05T09:45:12Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.",
   "id": 1382541592,
   "in_reply_to_id": 1347854623,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 486,
   "original_position": 486,
   "original_start_line": 482,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1713949915,
   "reactions": {
    "+1": 2,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 2
   },
   "side": "RIGHT",
   "start_line": 482,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-11-05T09:45:12Z",
   "user": null
  },
  {
   "body": "> Technically, there is RFC #46 for Nixpkgs. But I understood the point.\r\n\r\nThat RFC is about documentation.  Unfortunately the title it chose does not mention that.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-11-05T09:52:46Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.",
   "id": 1382542631,
   "in_reply_to_id": 1348780418,
   "line": 39,
   "original_commit_id": "dbd38c2cd817cfaa971c677be177bca91f37ee71",
   "original_line": 39,
   "original_position": 39,
   "original_start_line": 37,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 39,
   "pull_request_review_id": 1713950992,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 37,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-11-05T09:52:47Z",
   "user": null
  },
  {
   "body": "Yeah, let's not turn file hierarchy into an API. We just made a commitment to get rid of it in #140 , and #145 and #146 gives us ability to get rid of more file hierarchy-based APIs.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-11-05T17:27:53Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.",
   "id": 1382614428,
   "in_reply_to_id": 1367939736,
   "line": 111,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 111,
   "original_position": 111,
   "original_start_line": 106,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 111,
   "pull_request_review_id": 1714024844,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 106,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-11-05T19:41:01Z",
   "user": {
    "id": 66781795,
    "login": "KFearsoff",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I think integrating services should be on a best-effort basis anyway. It's unlikely that we have the capacity to have high-tier support for a lot of service management systems anyway.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-11-05T17:30:58Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.",
   "id": 1382615237,
   "in_reply_to_id": 1367940309,
   "line": 115,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 115,
   "original_position": 115,
   "original_start_line": 113,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 115,
   "pull_request_review_id": 1714024844,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 113,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-11-05T19:41:01Z",
   "user": {
    "id": 66781795,
    "login": "KFearsoff",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I assume this is more of a \"potential use-case\" rather than a \"good reason\". You can probably use the service management proposed by this RFC to run some service on Ubuntu in a Docker container, I guess. ",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-11-05T17:33:53Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).",
   "id": 1382615678,
   "in_reply_to_id": 1367940824,
   "line": 141,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 141,
   "original_position": 141,
   "original_start_line": 139,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 141,
   "pull_request_review_id": 1714024844,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 139,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-11-05T19:41:01Z",
   "user": {
    "id": 66781795,
    "login": "KFearsoff",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This sounds like a stretch. You have an ability to control systemd if you are an unprivileged user - you just don't get access to the system-level stuff, and control only the user-level stuff. Exactly like Home Manager does it.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-11-05T17:35:54Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.",
   "id": 1382615953,
   "line": 145,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 145,
   "original_position": 145,
   "original_start_line": null,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 145,
   "pull_request_review_id": 1714024844,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": null,
   "start_side": null,
   "subject_type": "line",
   "updated_at": "2023-11-05T19:41:01Z",
   "user": {
    "id": 66781795,
    "login": "KFearsoff",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This really is walking backwards, especially when you consider that doing module-based Nixpkgs was investigated fairly recently: https://github.com/nixpkgs-architecture/pkgs-modules",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-11-05T17:48:24Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.\n+\n+For many kinds of services (e.g. PostgreSQL, Apache HTTP server etc), it\n+typically suffices to only have a single instance running on a machine. For\n+microservices, it may actually be quite useful to have the ability to easily\n+create multiple instances. For testing/experimentation purposes it may also be\n+desired to run multiple instances of a system service, such as a web server.\n+\n+It is also not easily possible in NixOS to switch to alternative implementations\n+of a service. For many services, alternative implementations (e.g. `cron`,\n+`ssh`) are not required, but some services accept complex configurations, such\n+as the Apache HTTP server and Apache Tomcat.\n+\n+Finding a universal abstraction in NixOS to facilitate all possible use cases\n+may be impossible. Moreover, it may also be desired for developers to write\n+their own abstractions on top of existing functionality.\n+\n+To support abstractions and multiple instances, the Nix process management\n+framework does not use the NixOS module system, but a convention that is based\n+on the organisation of \"ordinary\" Nix packages in the Nixpkgs repository.",
   "id": 1382617742,
   "line": 505,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 505,
   "original_position": 505,
   "original_start_line": null,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 505,
   "pull_request_review_id": 1714024844,
   "reactions": {
    "+1": 1,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 1
   },
   "side": "RIGHT",
   "start_line": null,
   "start_side": null,
   "subject_type": "line",
   "updated_at": "2023-11-05T19:41:01Z",
   "user": {
    "id": 66781795,
    "login": "KFearsoff",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "We might end up using both something like the gist and my example because they solve different problems.\r\n\r\n - @infinisil's [gist](https://gist.github.com/infinisil/e29ee58a8184eeb7a2e139c2b2d3e4ff) operates at the NixOS level, allowing a \"constellation\" of services to be configured in one go. For example you could have a module that sets up an entire web application with local reverse proxy, database, etc, which would be counterproductive to do in a single systemd service.\r\n - [my example](https://github.com/NixOS/rfcs/pull/163#discussion_r1348841973) operates within a single service. This solves one of the gripes people have, that a NixOS module can affect *any* aspect of the system - no principle of least authority. In my example, the service module can only affect the system in ways that are explicitly facilitated by the system. For instance it can not add a file to `/etc` or create a user unless a proper NixOS module (ie the service abstraction layer implementation) inspects the service submodules and acts on them as it sees fit.\r\n\r\nI imagine a crossover is also possible, where such \"constellations\" are defined in isolation just like individual services, but are only allotted a certain prefix of unit names for example.\r\n\r\nBtw surely someone has come up with a better name than constellation for this.\r\n\r\nI'll try to demonstrate the principle with a working example. (no constellations probably. that's mostly just the same pattern applied twice. interesting though. we'll see)",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-11-12T19:48:00Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.",
   "id": 1390470473,
   "in_reply_to_id": 1347854623,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 486,
   "original_position": 486,
   "original_start_line": 482,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1726313217,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 3,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 3
   },
   "side": "RIGHT",
   "start_line": 482,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-11-12T19:48:00Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "> In my example, the service module can only affect the system in ways that are explicitly facilitated by the system. For instance it can not add a file to /etc or create a user unless a proper NixOS module (ie the service abstraction layer implementation) inspects the service submodules and acts on them as it sees fit.\r\n\r\nthanks for the explanation! looking forward to seeing your example.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-11-12T19:51:42Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.",
   "id": 1390471023,
   "in_reply_to_id": 1347854623,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 486,
   "original_position": 486,
   "original_start_line": 482,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1726313728,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 482,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-11-12T19:51:42Z",
   "user": {
    "id": 7755101,
    "login": "aanderse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Ok, did the thing. A bit rushed maybe, but a lot more complete than anything I wrote before this.\r\n\r\n- https://github.com/NixOS/nixpkgs/pull/267111",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-11-12T21:47:55Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes a number of feature additions to the Nixpkgs ecosystem. These\n+features correspond to the concepts implemented in the experimental Nix process\n+management framework.\n+\n+There are variety of ways to implement these concepts. Moreover, the idea is that\n+these concepts can be implemented one-by-one and in an iterative fashion.\n+\n+The following sections explain its concepts one by one and provide possible\n+implementation strategies.\n+\n+## A service layer that can be used independently of NixOS\n+\n+Currently, the Nixpkgs repository only contains systemd service configurations.\n+These systemd configurations cannot be used separately from NixOS (as a\n+sidenote: there is project called\n+[system-manager](https://github.com/numtide/system-manager)\n+that makes it possible to only generate systemd units that can be used outside\n+NixOS, but it can only be used on Ubuntu and is still under heavy development).\n+\n+Another project that makes services management available in combination with\n+Nix is [nix-darwin](https://github.com/LnL7/nix-darwin). This project uses\n+launchd (on macOS) in combination with Nix to support process management.\n+\n+To make service deployment accessible to a broader audience, it would be nice to\n+create a separation in the Nixpkgs tree for portable systemd services (e.g. a\n+`services/` directory in the root folder) and to develop a derivation and\n+command-line tool (e.g. `nix-services-rebuild`) that allows you to deploy (at\n+least a reasonable sub set) of systemd units separately on a conventional Linux\n+distribution.\n+\n+Low-level system services, such as `cups`) do not need to be separated from\n+NixOS, but high-level services should, such as `Apache`, `Nginx`, `MariaDB`,\n+`PostgreSQL` etc.\n+\n+NixOS will be a consumer of this separated service layer.\n+\n+## Generator functions for other kinds of process managers\n+\n+With a separated services tree and command-line tool to manage instances of\n+services, it should also become more convenient to target different kinds of\n+process managers.\n+\n+The purpose of this RFC is not to have a debate about systemd's usefulness.\n+systemd has IMO many good features, and I believe it should remain the default\n+option in NixOS.\n+\n+However, there are also good reasons to sometimes consider a different process\n+manager:\n+\n+* systemd is a Linux-specific tool. If we want to deploy a service on another\n+  operating system, we need to pick a different solution that is compatible with\n+  that operating system.\n+* systemd is glibc-specific. Sometimes, it may also be desired to manage\n+  services on a Linux system that uses a different libc, such as musl. Then\n+  you need a process manager that is compatible with that different flavour of\n+  libc.\n+* We may want to deploy services on older Linux distros or distros with LTS\n+  support. These distros may still work with sysvinit scripts (also known as\n+  [LSB Init compliant scripts](https://wiki.debian.org/LSBInitScripts)).\n+* Sometimes we may want to experiment with services as an unprivileged user\n+  that does not have the ability to control systemd. Then it can be very\n+  convenient to use a process manager that can be used as an unprivileged user,\n+  such as `supervisord`.\n+* Although it is possible to run systemd in a Docker container to manage\n+  multiple processes, it is typically more preferable to use a more Docker\n+  friendly alternative\n+\n+With a separated service layer in Nixpkgs, it should also become more convenient\n+to use a different generator module for a different kind of process manager.\n+\n+Similar to the systemd generator module, we can provide NixOS generator modules\n+for other process managers as well, that provide a one-on-one translation\n+between properties in the Nix expression language and generated config file\n+properties.\n+\n+For example, we can also develop an abstraction that generates `sysvinit` scripts\n+(also known as LSB Init compliant scripts). The nix-processmgmt framework allows\n+you to generate a `sysvinit` script as follows:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  activities = {\n+    start = ''\n+      mkdir -p ${stateDir}/logs\n+      log_info_msg \"Starting Nginx...\"\n+      loadproc ${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\n+      evaluate_retval\n+    '';\n+    stop = ''\n+      log_info_msg \"Stopping Nginx...\"\n+      killproc ${nginx}/bin/nginx\n+      evaluate_retval\n+    '';\n+    reload = ''\n+      log_info_msg \"Reloading Nginx...\"\n+      killproc ${nginx}/bin/nginx -HUP\n+      evaluate_retval\n+    '';\n+    restart = ''\n+      $0 stop\n+      sleep 1\n+      $0 start\n+    '';\n+    status = \"statusproc ${nginx}/bin/nginx\";\n+  };\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+(As a sidenote: the nix-processmgmt framework uses different conventions than\n+NixOS, which I will explain later)\n+\n+The above code example invokes the `createSystemVInitScript` function to generate\n+a `sysvinit` script to manage Nginx:\n+\n+* The `name` of the script is: `nginx`\n+* The `description` in the comment is: Nginx\n+* The `activities` parameter specifies the implementation of all the activities\n+  (process lifecycle operations) that the init script exposes (as bash shell\n+  instructions), such as starting, stopping, restarting and reloading the\n+  service.\n+\n+Since the kind of activities that you need to implement are so common, it is\n+also possible to add a little bit of abstraction on top of the generator\n+function:\n+\n+```nix\n+{createSystemVInitScript, nginx}:\n+\n+let\n+  configFile = ./nginx.conf;\n+  stateDir = \"/var\";\n+in\n+createSystemVInitScript {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${stateDir}/logs\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+}\n+```\n+\n+In the above example, we have simplified the previous example -- we no longer\n+specify the implementation of any of the activities, but we specify which\n+process we want to be managed and how it can be initialized. From this\n+high-level specification the activities are automatically derived.\n+\n+Currently, the Nix process management framework provides generator functions for\n+the following backends:\n+\n+* `sysvinit` scripts (also known as LSB Init compliant scripts). Because it is\n+  standardize by the LSB, widely used and the most primitive, I have decided to\n+  experiment with this first.\n+* `systemd` services\n+* `supervisord` services\n+* `launchd` services\n+* `bsdrc` scripts\n+* `s6-rc` services\n+* Windows services (deployed by calling: `cygrunsrv`)\n+\n+In addition to the above process managers, it also possible to directly generate\n+Docker containers for each process instance. Although the framework was not\n+originally designed for this purpose, I have noticed that the abstractions are\n+generic enough to make such deployments possible.\n+\n+In NixOS, for each backend, we can add a generator module. Currently, a NixOS\n+module for systemd already exists, but we can also add generator modules for\n+different kinds of process managers.\n+\n+## A high-level, target-agnostic specification format for running processes\n+\n+During the development of the Nix process management framework I have written\n+configurations for various kinds of services for various kinds of process\n+managers.\n+\n+If you look closely at their configurations, then I have noticed that they have\n+much in common. For example, to generate a `sysvinit` script for managing Nginx,\n+I can use the following Nix expression:\n+\n+```nix\n+{createSystemVInitScript, nginx,  stateDir}:\n+{configFile, dependencies ? [], instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemVInitScript {\n+  name = instanceName;\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile \"-p\" stateDir ];\n+  runlevels = [ 3 4 5 ];\n+\n+  inherit dependencies instanceName;\n+}\n+```\n+\n+For `supervisord` I could write:\n+\n+```nix\n+{createSupervisordProgram, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSupervisordProgram {\n+  name = instanceName;\n+  command = \"mkdir -p ${nginxLogDir}; \"+\n+    \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+}\n+```\n+\n+And for `systemd` I could write:\n+\n+```nix\n+{createSystemdService, nginx, stateDir}:\n+{configFile, instanceSuffix ? \"\"}:\n+\n+let\n+  instanceName = \"nginx${instanceSuffix}\";\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createSystemdService {\n+  name = instanceName;\n+  Unit = {\n+    Description = \"Nginx\";\n+  };\n+  Service = {\n+    ExecStartPre = \"+mkdir -p ${nginxLogDir}\";\n+    ExecStart = \"${nginx}/bin/nginx -c ${configFile} -p ${stateDir}\";\n+    Type = \"simple\";\n+  };\n+}\n+```\n+\n+As you may probably have already seen, in all the examples I specify the same\n+kinds of configuration properties:\n+* The initialization steps\n+* The executable to run with the appropriate command-line arguments\n+* Meta information such as the name and description\n+\n+We could also create a high-level abstraction that captures these properties:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+}\n+```\n+\n+The above code example uses a high-level function: `createManagedProcess` that\n+specifies an `initialize` script and a process that we need to launch (`process`)\n+with a number of command-line parameters (`args`). This high-level specification\n+can be translated to a configuration file for various kinds of process managers.\n+\n+Currently, the `createManagedProcess` function in the Nix process management\n+framework supports the following properties:\n+\n+* `name`. Name of the service\n+* `description`. Description of the service\n+* `initialize`. Initialization script that initializes state on first startup.\n+* `foregroundProcess`. Path to an executable that launches a process in\n+  foreground mode\n+* `foregroundProcessArgs`. Command-line parameters propagated to the foreground\n+  process\n+* `daemon`. Path to an executable that launches a process in daemon mode\n+* `daemonArgs`. Command-line parameters propagated to the daemon\n+* `process`. When this property is specified, it translates both to:\n+  `foregroundProcess` and `daemon`.\n+* `args`. When this property is specified, it translates both to:\n+  `foregroundProcessArgs`, `daemonArgs`.\n+* `foregroundProcessExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in foreground mode\n+* `daemonExtraArgs`. Extra command-line parameters appended to the\n+  list of command-line parameters when the process runs in daemon mode\n+* `environment`. An attribute set with environment variables\n+* `user`. Name of the user we should change to\n+* `directory`. The current working directory we should change to before\n+  executing the process\n+* `nice`. Nice level\n+* `umask`. Umask\n+* `dependencies`. Specifies the process dependencies: dependencies on other\n+  running processes that should be deployed first.\n+\n+### About foreground processes and daemons\n+\n+The function abstraction makes a distinction between *foreground processes* and\n+*daemons*. Most modern process managers\n+(e.g. `systemd`, `launchd`, `supervisord`) have a preference for the former,\n+because they can be more reliably managed (i.e. no PID files). However,\n+`sysvinit` (which is an LSB standard) and `bsdrc` scripts require processes to\n+daemonize by themselves, otherwise the scripts will block.\n+\n+Most services have the option to support both deployment scenarios. For an\n+optimal user experience, it is also best to support both. However, this is not a\n+strict requirement -- if a service is only offered as a foreground process then \n+it can still be daemonized by calling an external tool, such as:\n+[libslack's daemon](https://libslack.org/daemon).\n+\n+The inverse scenario is also possible -- if a service always daemonizes, you can\n+add a substitute proxy that runs in foreground mode that watches the lifecycle\n+of the daemon.\n+\n+In the Nix process management framework, the `createManagedProcess` abstraction\n+will automatically use a simulation strategy when a deployment scenario is\n+unspecified. For example, if you only specify a `foregroundProcess` then it will\n+transparently call the `daemon` tool if a process manager requires a process to\n+daemonize by itself.\n+\n+### Process dependencies\n+\n+Another interesting concept is the notion process dependencies (`dependencies`\n+parameter). Sometimes, a service requires the presence of another service to\n+work reliably. For example, if you have a PHP application deployed to Apache\n+that uses a MariaDB database for data storage, then MariaDB needs to be\n+activated before Apache.\n+\n+The `dependencies` parameter can be used to automatically generate a\n+configuration in which the order is preserved. For example, when generating\n+a `systemd` configuration, this property can be translated into `Wants=` and\n+`Requires=` directives. For `sysvinit` we can derived sequence numbers.\n+\n+In NixOS, there is no high-level concept of process dependencies. It is still\n+possible to directly control the activation order by modifying systemd's `Wants=`\n+and `Requires=` directives through overrides, but this is not really\n+user-friendly.\n+\n+### Process manager-specific overrides\n+\n+Obviously, it is impossible to standardize all properties of all process\n+managers. Sometimes, it may still be desired to specify properties that are\n+unique to certain process managers.\n+\n+For example, `sysvinit` has the notion of run levels that are not used by other\n+process managers. Specifying runlevels for `sysvinit` scripts can be done by\n+defining a process-manager specific override:\n+\n+```nix\n+{createManagedProcess, nginx, stateDir}:\n+{configFile}:\n+\n+let\n+  nginxLogDir = \"${stateDir}/${instanceName}/logs\";\n+in\n+createManagedProcess {\n+  name = \"nginx\";\n+  description = \"Nginx\";\n+  initialize = ''\n+    mkdir -p ${nginxLogDir}\n+  '';\n+  process = \"${nginx}/bin/nginx\";\n+  args = [ \"-c\" configFile\" -p\" \"${stateDir}/${instanceName}\" ];\n+\n+  overrides = {\n+    sysvinit = {\n+      runlevels = [ 3 4 5 ];\n+    };\n+  };\n+}\n+```\n+\n+In the above example, the `overrides.sysvinit` property specifies\n+`sysvinit`-only options. We use the override to define in which runlevels the\n+script should be started.\n+\n+We can also use overrides for other process managers. For example, if we want\n+to use the readiness-check property of systemd (which is not standardized) or\n+Linux namespaces and cgroups, we can define a `systemd`-specific override.\n+\n+## Writing services as units of instantiation rather than singleton objects\n+\n+Compared to NixOS and other solutions, such as nix-darwin and system-manager,\n+another feature of the Nix process management framework is that services are\n+units of *instantiation* rather than *singleton* objects -- in NixOS, services\n+are bound to modules. As a result, services are singleton objects that only have\n+a single implementation and a single configuration.",
   "id": 1390489048,
   "in_reply_to_id": 1347854623,
   "line": null,
   "original_commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "original_line": 486,
   "original_position": 486,
   "original_start_line": 482,
   "path": "rfcs/0160-portable-service-layer.md",
   "position": 1,
   "pull_request_review_id": 1726334784,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 482,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-11-12T21:47:55Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "```suggestion\r\nNix does not solve all application deployment problems -- Nix's\r\n```\r\n\r\nThere's merit to that.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-12-07T09:00:46Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's",
   "id": 1418619962,
   "line": 41,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 41,
   "original_position": 41,
   "original_start_line": null,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 41,
   "pull_request_review_id": 1769574670,
   "reactions": {
    "+1": 4,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 4
   },
   "side": "RIGHT",
   "start_line": null,
   "start_side": null,
   "subject_type": "line",
   "updated_at": "2023-12-07T09:00:46Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "While good, this raises the question, why an RFC?\r\nI have an inkling, but let's make explicit the intended effect.\r\nWhat we're going for, I think is:\r\n\r\n- Propose this mechanism to be available in NixOS, so that it is available to NixOS service definitions.\r\n- No proposal to turn the mechanism into policy\r\n\r\nAs for [why](https://github.com/NixOS/rfcs#when-this-process-is-followed) an RFC:\r\n\r\n> Introduction of new interfaces or functions\r\n> Expansions to the scope of Nixpkgs (new arch, major subprojects, ...)\r\n\r\nMajor subproject of _NixOS_. Even if not policy, contributors, and at least to a degree, users will have to work with it. \r\n\r\nHence it deserves broad attention and a robust design improvement process.\r\n\r\nAlso, a proposed implementation in the form of a NixOS (draft) PR.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-12-07T09:11:16Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.\n+\n+Unfortunately, Nix does not solve all application deployment problems -- Nix's\n+purpose is to deploy packages (e.g. delivering the required artifacts, such as\n+executables and configuration files) but not to manage the life-cycle of\n+applications, such as long running services (the only exception is the `nix run`\n+command that allows you to conveniently start an executable).\n+\n+For these kinds of applications, Nix is typically combined with a *process\n+manager*, such as `systemd`, that is responsible for managing the life-cycle of\n+a service. Configuration files for these process managers are also deployed as\n+Nix packages.\n+\n+There are a variety of Nix-based solutions that combine Nix with a process\n+management solution (see: [prior art](#prior-art)). These solutions are quite\n+useful, but have a number of drawbacks. The most notable drawback is that there\n+is almost no reuse between the configurations of the services that are managed by\n+these solutions. In practice, I have noticed that quite a few properties remain\n+the same in spite of the process management solution that was selected.\n+\n+Moreover, there are also a number of interesting Nix properties for which we also\n+want to provide a process-management equivalent:\n+\n+* Nix makes it possible to have *multiple versions* and variants of the same\n+  package to safely co-exist on the same system. We also want to bring this\n+  property to process management -- this becomes possible if we make sure that\n+  multiple instances do not share the same state (e.g. state directories, port\n+  numbers, UIDs, GIDs).\n+* Nix supports *unprivileged* user package installations. It is also possible for\n+  many kinds services to run as an unprivileged user as long as we make sure\n+  that they use state directories have write permissions (e.g. using a state\n+  directory in the user's home directory) and that these services use TCP/UDP\n+  port numbers that are greater than 1024\n+\n+The ideas described in this RFC have been explored in the experimental\n+[Nix process management framework](https://github.com/svanderburg/nix-processmgmt).\n+There is also an\n+[example services repository](https://github.com/svanderburg/nix-processmgmt-services)\n+repository that demonstrates how it can be used with a number of commonly used\n+services.",
   "id": 1418638007,
   "line": 78,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 78,
   "original_position": 78,
   "original_start_line": null,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 78,
   "pull_request_review_id": 1769603289,
   "reactions": {
    "+1": 5,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 5
   },
   "side": "RIGHT",
   "start_line": null,
   "start_side": null,
   "subject_type": "line",
   "updated_at": "2023-12-07T09:11:17Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "```suggestion\r\nshepherd-team:  roberth, AndersonTorres, ehmry, Animeshz, IreneKnapp\r\nshepherd-leader: AndersonTorres\r\n```\r\nThanks for all your nominations!",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2023-12-14T11:40:59Z",
   "diff_hunk": "@@ -0,0 +1,925 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)",
   "id": 1426607982,
   "line": 7,
   "original_commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "original_line": 7,
   "original_position": 7,
   "original_start_line": 6,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 7,
   "pull_request_review_id": 1781630982,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 6,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2023-12-14T11:41:00Z",
   "user": {
    "id": 341954,
    "login": "lheckemann",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Now that we have https://github.com/nixos-bsd, we know a lot more about this!!",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "created_at": "2024-05-28T16:48:58Z",
   "diff_hunk": "@@ -0,0 +1,893 @@\n+---\n+feature: portable_service_layer\n+start-date: 2023-09-10\n+author: Sander van der Burg\n+co-authors: (find a buddy later to help out with the RFC)\n+shepherd-team: (names, to be nominated and accepted by RFC steering committee)\n+shepherd-leader: (name to be appointed by RFC steering committee)\n+related-issues: (will contain links to implementation PRs)\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+This proposal introduces a generic, portable, high-level process management\n+framework that makes it possible to manage a variety of commonly used\n+application services on preferably all operating systems/environments that the\n+Nix package manager supports.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+The Nix package manager is a deployment solution that offers many powerful\n+features. For example, it is a very convenient solution to deploy\n+*multiple versions* and *variants* of packages in isolation so that they do not\n+conflict with each other and packages of an existing system installation.\n+\n+The `nix-shell` command is particularly useful to safely experiment with\n+packages -- you can conveniently spawn a shell session in which you can use\n+different versions of packages than the host system without changing the host\n+system's package configuration.\n+\n+For Nix, it is *not* even required to have *super-user privileges* to deploy\n+packages -- thanks to its isolation properties this can be done without\n+interfering with the packages of another user or the system-wide set of packages.\n+\n+Moreover, the Nix package manager is also a *portable* solution. In addition\n+to Linux, Nix also has first-class support for macOS/Darwin. With some effort,\n+it can also be used on other UNIX-like operating systems, such as FreeBSD and\n+Cygwin.",
   "id": 1617618267,
   "in_reply_to_id": 1348780418,
   "line": 39,
   "original_commit_id": "dbd38c2cd817cfaa971c677be177bca91f37ee71",
   "original_line": 39,
   "original_position": 39,
   "original_start_line": 37,
   "path": "rfcs/0163-portable-service-layer.md",
   "position": 39,
   "pull_request_review_id": 2083400929,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 37,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2024-05-28T16:48:58Z",
   "user": {
    "id": 1055245,
    "login": "Ericson2314",
    "site_admin": false,
    "type": "User"
   }
  }
 ],
 "reviews": [
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1660524954,
   "state": "CHANGES_REQUESTED",
   "submitted_at": "2023-10-05T19:10:27Z",
   "user": {
    "id": 20525370,
    "login": "infinisil",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1661072637,
   "state": "COMMENTED",
   "submitted_at": "2023-10-06T03:56:09Z",
   "user": {
    "id": 7755101,
    "login": "aanderse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "831405257e5c25126d6d3437d5075f62842baa42",
   "id": 1661115188,
   "state": "COMMENTED",
   "submitted_at": "2023-10-06T04:58:04Z",
   "user": {
    "id": 628387,
    "login": "arianvp",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "831405257e5c25126d6d3437d5075f62842baa42",
   "id": 1661115869,
   "state": "COMMENTED",
   "submitted_at": "2023-10-06T04:59:09Z",
   "user": {
    "id": 628387,
    "login": "arianvp",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1661542340,
   "state": "COMMENTED",
   "submitted_at": "2023-10-06T10:12:06Z",
   "user": {
    "id": 72429,
    "login": "alper",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1661546392,
   "state": "COMMENTED",
   "submitted_at": "2023-10-06T10:14:19Z",
   "user": {
    "id": 1153271,
    "login": "svanderburg",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1661557727,
   "state": "COMMENTED",
   "submitted_at": "2023-10-06T10:21:28Z",
   "user": {
    "id": 1153271,
    "login": "svanderburg",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I am absolutely in favour of achieving this RFCs goal but there are two non-negotiable aspects about this topic that the RFC must address IMO:\r\n\r\n### It must stay compatible with NixOS module-style configuration.\r\n\r\nIf NixOS has some service defined, the user must be able to control its behaviour in every detail via their own module(s) using standard module functions such as `mkMerge`, `mkForce` and the like. In other words: It must stay composable.\r\n\r\n### It cannot come at the cost of unreasonable complexity or loss of functionality to NixOS maintainers.\r\n\r\nIf I, as a maintainer for some NixOS module, do not care for other init systems but systemd, I must be able to write the module in such a way that it is only compatible with systemd. Additionally, if another person who does care for other inits comes along and makes it compatible, I (and, by extension, the module's users) must still be able to configure every aspect of the systemd service without limitations.  \r\nAbstractions that make certain common things such as the service's exectuable portable are welcome and (IMO) do not constitute unreasonable complexity but I must still be able to use e.g. systemd's ExecStart with its exact semantics without having to think about other inits' equivalents should there not be an abstract interface for this.\r\n\r\nRequiring NixOS maintainers to muck with bash scripts or whatever in order to implement mechanisms for which systemd has a standardised interface would be an absolute no-go for me.\r\n\r\nRelated to this: If we want alternative inits to have a chance, I think it'd also be a good idea to be able to explicitly declare a service as incompatible. If my module makes use of an init-specific feature and the service's execution is dependant on it, I need to be able to tell the user that my module won't work for them at eval time.  \r\nThis also goes the other way: If at some point service modules are implemented by non-systemd users making use of non-systemd init features, they must be able to declare their modules incompatible with systemd. (This would also be useful for modules that only make sense on a specific platform.)\r\n\r\nIf you can achieve that, I'm all for it.",
   "commit_id": "dbd38c2cd817cfaa971c677be177bca91f37ee71",
   "id": 1661986826,
   "state": "CHANGES_REQUESTED",
   "submitted_at": "2023-10-06T14:27:21Z",
   "user": {
    "id": 18599032,
    "login": "Atemu",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Since I am very interested in getting rid of systemd dependency, I am very excited about this RFC!",
   "commit_id": "dbd38c2cd817cfaa971c677be177bca91f37ee71",
   "id": 1661959534,
   "state": "COMMENTED",
   "submitted_at": "2023-10-06T14:44:54Z",
   "user": {
    "id": 5954806,
    "login": "AndersonTorres",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1662073150,
   "state": "COMMENTED",
   "submitted_at": "2023-10-06T15:01:26Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1662092682,
   "state": "COMMENTED",
   "submitted_at": "2023-10-06T15:09:12Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "dbd38c2cd817cfaa971c677be177bca91f37ee71",
   "id": 1662119007,
   "state": "COMMENTED",
   "submitted_at": "2023-10-06T15:20:41Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1662498738,
   "state": "COMMENTED",
   "submitted_at": "2023-10-06T17:27:57Z",
   "user": {
    "id": 1153271,
    "login": "svanderburg",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "dbd38c2cd817cfaa971c677be177bca91f37ee71",
   "id": 1662531554,
   "state": "COMMENTED",
   "submitted_at": "2023-10-06T17:46:00Z",
   "user": {
    "id": 1153271,
    "login": "svanderburg",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1662669660,
   "state": "COMMENTED",
   "submitted_at": "2023-10-06T19:25:39Z",
   "user": {
    "id": 1055245,
    "login": "Ericson2314",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "1776c5854323ab0f2d5d384a096753f7540fccf3",
   "id": 1663085271,
   "state": "COMMENTED",
   "submitted_at": "2023-10-07T17:42:17Z",
   "user": {
    "id": 5954806,
    "login": "AndersonTorres",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "1776c5854323ab0f2d5d384a096753f7540fccf3",
   "id": 1663150918,
   "state": "COMMENTED",
   "submitted_at": "2023-10-08T03:38:26Z",
   "user": {
    "id": 42209822,
    "login": "Aleksanaa",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1663238052,
   "state": "COMMENTED",
   "submitted_at": "2023-10-08T13:40:29Z",
   "user": {
    "id": 3662816,
    "login": "8573",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1663603265,
   "state": "COMMENTED",
   "submitted_at": "2023-10-08T21:27:56Z",
   "user": {
    "id": 20525370,
    "login": "infinisil",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "dbd38c2cd817cfaa971c677be177bca91f37ee71",
   "id": 1663604224,
   "state": "COMMENTED",
   "submitted_at": "2023-10-08T21:37:32Z",
   "user": {
    "id": 5954806,
    "login": "AndersonTorres",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1663605637,
   "state": "COMMENTED",
   "submitted_at": "2023-10-08T21:50:15Z",
   "user": {
    "id": 7755101,
    "login": "aanderse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1663671614,
   "state": "COMMENTED",
   "submitted_at": "2023-10-09T02:41:21Z",
   "user": {
    "id": 5954806,
    "login": "AndersonTorres",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1664034495,
   "state": "COMMENTED",
   "submitted_at": "2023-10-09T09:19:43Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "dbd38c2cd817cfaa971c677be177bca91f37ee71",
   "id": 1664055810,
   "state": "COMMENTED",
   "submitted_at": "2023-10-09T09:32:18Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1665124676,
   "state": "COMMENTED",
   "submitted_at": "2023-10-09T19:06:51Z",
   "user": {
    "id": 20525370,
    "login": "infinisil",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1671369484,
   "state": "COMMENTED",
   "submitted_at": "2023-10-11T14:19:31Z",
   "user": {
    "id": 1055245,
    "login": "Ericson2314",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1671628292,
   "state": "COMMENTED",
   "submitted_at": "2023-10-11T15:35:20Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "id": 1678600312,
   "state": "COMMENTED",
   "submitted_at": "2023-10-15T03:08:06Z",
   "user": {
    "id": 26714676,
    "login": "Animeshz",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "id": 1678602618,
   "state": "COMMENTED",
   "submitted_at": "2023-10-15T03:11:32Z",
   "user": {
    "id": 26714676,
    "login": "Animeshz",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1678752824,
   "state": "COMMENTED",
   "submitted_at": "2023-10-15T08:38:39Z",
   "user": {
    "id": 1891350,
    "login": "7c6f434c",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1678868441,
   "state": "COMMENTED",
   "submitted_at": "2023-10-15T20:40:16Z",
   "user": {
    "id": 7755101,
    "login": "aanderse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "id": 1680016075,
   "state": "COMMENTED",
   "submitted_at": "2023-10-16T13:39:28Z",
   "user": {
    "id": 20525370,
    "login": "infinisil",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I appreciate and like the idea but adding extra layers of abstractions even for NixOS is not a good looking sign. Also that all alternatives to systemd lack major features of it is really worrying me.\r\nAlso you completely ignored some of systemd's features like service hardening, sandboxing, tmpfiles.d, journald, networkd.\r\nReimplementing that all in shell script is probably possible but at what cost?\r\n\r\nPS: I would appreciate a more accessible writing style like one sentence per line.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "id": 1691465165,
   "state": "COMMENTED",
   "submitted_at": "2023-10-22T18:10:19Z",
   "user": {
    "id": 7258858,
    "login": "SuperSandro2000",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1691485641,
   "state": "COMMENTED",
   "submitted_at": "2023-10-22T20:13:26Z",
   "user": {
    "id": 7755101,
    "login": "aanderse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "id": 1691490717,
   "state": "COMMENTED",
   "submitted_at": "2023-10-22T21:06:31Z",
   "user": {
    "id": 5954806,
    "login": "AndersonTorres",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1713946705,
   "state": "COMMENTED",
   "submitted_at": "2023-11-05T09:22:20Z",
   "user": null
  },
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1713949915,
   "state": "COMMENTED",
   "submitted_at": "2023-11-05T09:45:12Z",
   "user": null
  },
  {
   "body": "",
   "commit_id": "dbd38c2cd817cfaa971c677be177bca91f37ee71",
   "id": 1713950992,
   "state": "COMMENTED",
   "submitted_at": "2023-11-05T09:52:47Z",
   "user": null
  },
  {
   "body": "I like the RFC, but I fear it is already ambitious, and will only have more feature creep on top.\r\n\r\nI don't think discussing whether support for additional service managers is valuable. I think the discussion we should be having is **what** service managers we want to support, and how.\r\n\r\nFor me, an easy one that comes to mind is Tier 1 support for systemd, userland systemd and containers. We could have Tier 2 or 3 support for launchd. Everything else sounds fun, but we really can't make any guarantees, I think. If someone wants to write glue to port services to other init systems - they are welcome, but I don't think we have the capacity to provide full-fledged support for those.\r\n\r\nI imagine you would specify the service manager in the top-level attributes (something like `service-manager.type = \"systemd\";`, and then everything would inherit from that, but you can override it case by case basis (something like `services.postgres.runs-on = \"container\";`.\r\n\r\nThen, we'd be able to build NixOS services, NixOS containers, OCI-containers-ran-on-NixOS, Home Manager and Nix Darwin on the same abstract base, getting rid of a lot of inconsistencies and glue throughout them. I feel like that's extremely relevant to the success of this RFC, and what we should spend more time discussing.\r\n\r\nStakeholders in all of the mentioned subsystems welcome to share their input.",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "id": 1714024844,
   "state": "CHANGES_REQUESTED",
   "submitted_at": "2023-11-05T19:41:01Z",
   "user": {
    "id": 66781795,
    "login": "KFearsoff",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1726313217,
   "state": "COMMENTED",
   "submitted_at": "2023-11-12T19:48:00Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1726313728,
   "state": "COMMENTED",
   "submitted_at": "2023-11-12T19:51:42Z",
   "user": {
    "id": 7755101,
    "login": "aanderse",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "8f068de0791c88ea3e0b62949b5c9e5b0a157a4b",
   "id": 1726334784,
   "state": "COMMENTED",
   "submitted_at": "2023-11-12T21:47:55Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "id": 1769574670,
   "state": "COMMENTED",
   "submitted_at": "2023-12-07T09:00:46Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "id": 1769603289,
   "state": "COMMENTED",
   "submitted_at": "2023-12-07T09:11:17Z",
   "user": {
    "id": 496447,
    "login": "roberth",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "3798ca1a0946f3fd745b6765c133838b65ae1960",
   "id": 1781630982,
   "state": "COMMENTED",
   "submitted_at": "2023-12-14T11:41:00Z",
   "user": {
    "id": 341954,
    "login": "lheckemann",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "dbd38c2cd817cfaa971c677be177bca91f37ee71",
   "id": 2083400929,
   "state": "COMMENTED",
   "submitted_at": "2024-05-28T16:48:58Z",
   "user": {
    "id": 1055245,
    "login": "Ericson2314",
    "site_admin": false,
    "type": "User"
   }
  }
 ],
 "sources": [
  "rfc-163-portable-service-layer",
  "rfcs-modular-service-prs",
  "rfcs-portable-service-prs"
 ],
 "timeline": [
  {
   "actor": {
    "id": 20525370,
    "login": "infinisil",
    "site_admin": false,
    "type": "User"
   },
   "commit_id": null,
   "commit_url": null,
   "created_at": "2023-10-05T19:24:37Z",
   "event": "renamed",
   "id": 10567691305,
   "rename": {
    "from": "[RFC 160] Initial commit",
    "to": "[RFC 163] Initial commit"
   }
  },
  {
   "actor": {
    "id": 1153271,
    "login": "svanderburg",
    "site_admin": false,
    "type": "User"
   },
   "commit_id": null,
   "commit_url": null,
   "created_at": "2023-10-06T09:58:53Z",
   "event": "renamed",
   "id": 10573116893,
   "rename": {
    "from": "[RFC 163] Initial commit",
    "to": "[RFC 163] Portable Service Layer"
   }
  },
  {
   "actor": {
    "id": 20525370,
    "login": "infinisil",
    "site_admin": false,
    "type": "User"
   },
   "commit_id": null,
   "commit_url": null,
   "created_at": "2023-11-16T00:39:23Z",
   "event": "renamed",
   "id": 10973927529,
   "rename": {
    "from": "[RFC 163] Portable Service Layer",
    "to": "[RFC 0163] Portable Service Layer"
   }
  },
  {
   "actor": {
    "id": 20525370,
    "login": "infinisil",
    "site_admin": false,
    "type": "User"
   },
   "commit_id": null,
   "commit_url": null,
   "created_at": "2025-01-20T16:14:38Z",
   "event": "closed",
   "id": 16001547970,
   "intent": null,
   "state_reason": null
  }
 ]
}
