{
 "comments": [
  {
   "body": "PR and file are named wrong. The number should be the same as the PR number, padded to four digits: 0201. Not sure why that isn't written anywhere tbh.",
   "created_at": "2026-05-13T03:40:07Z",
   "id": 4436958216,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2026-05-13T03:40:07Z",
   "user": {
    "id": 46138807,
    "login": "Lord-Valen",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Fixed: Renamed RFC file to \\\rfcs/0201-security-artifacts.md\\ and updated the PR title to match the PR number as requested. Thanks for the catch!",
   "created_at": "2026-05-13T03:59:15Z",
   "id": 4437098479,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2026-05-13T03:59:15Z",
   "user": {
    "id": 140179052,
    "login": "Mutasem-mk4",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "1) It's not immediately clear from the RFC text, how exactly are `services.*` supposed to \"natively consume\" `security.artifacts`.\r\n\r\n    In the postgres example, you use `\"postgres-pw\"` as the name of the secret. Am I understanding correctly, that this name is just implicitly picked up by `services.postgresql` with no extra configuration? That seems a bit too \"spooky action at a distance\" to me.\r\n\r\n2) IIUC, in the current design it's not possible to use different secret providers for different secrets. Even right now with only 3 providers, I can imagine situations when one might want to mix different providers (ie use `systemd-creds`  for machine-specific secrets and `sops-nix` / `agenix` for shared secrets). It might be worth it to not hardcode the \"single provider\" assumption into the system.\r\n\r\n3) IIUC, the `dummy` provider creates secret files from plain text data. If we are including \"built in\" providers, it might make sense to include some kind of `externally-provided-path` provider (or extend the `dummy` provider to support this).\r\n\r\n4) Speaking of providers - right now the RFC describes them as \"pluggable backends\", but it's not clear how would one create and \"plug in\" their own provider.\r\n\r\n    Looking at the reference implementation it looks like all of the current providers are just hard coded in nixpkgs and in fact the `security.artifacts` module has bespoke logic for each provider (for example, the `security.artifacts.provider` option is a fixed enum, the `security.artifacts.secrets.\"*\".dummy` option is globally defined by the `security.artifacts` module, not by the `dummy` provider, etc).\r\n\r\nP.S. Is this LLM slop?",
   "created_at": "2026-05-13T11:05:16Z",
   "id": 4440262531,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2026-05-13T16:55:04Z",
   "user": {
    "id": 3747318,
    "login": "ruro",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I've updated the RFC and the reference implementation to address the feedback:\n\n1. **Per-Secret Providers**: Now supports mixed configurations (e.g., combining \\systemd-creds\\ for hardware-bound secrets and \\sops-nix\\ for shared ones).\n2. **External Provider**: Added a provider for secrets provisioned outside of NixOS (cloud-init, etc.).\n3. **Implicit vs Explicit**: Added examples clarifying that modules should explicitly reference \\config.security.artifacts.secrets.<name>.path\\ to avoid 'spooky action'.\n4. **Naming**: Renamed the RFC file to \\",
   "created_at": "2026-05-13T13:47:50Z",
   "id": 4441651018,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2026-05-13T13:47:50Z",
   "user": {
    "id": 140179052,
    "login": "Mutasem-mk4",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "this should probably mention clan's vars effort for comparison.\r\n\r\ni also see partial overlap with contracts (#189) and its implementations (e.g. #506343), which isn't concerned with deploying secrets directly, but does help decouple logic more broadly (not just for secrets providers), and may also be reusable across nix-based systems (nixos, home-manager, ...), across modular services and across (eval-time) node boundaries.",
   "created_at": "2026-05-13T18:13:00Z",
   "id": 4443989885,
   "minimized": null,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "updated_at": "2026-05-13T18:13:00Z",
   "user": {
    "id": 3059397,
    "login": "KiaraGrouwstra",
    "site_admin": false,
    "type": "User"
   }
  }
 ],
 "html_url": "https://github.com/NixOS/rfcs/pull/201",
 "issue": {
  "active_lock_reason": null,
  "assignee": null,
  "assignees": [],
  "body": "This RFC proposes \\security.artifacts\\, a backend-agnostic abstraction layer for managing secrets in NixOS. It decouples NixOS service modules from specific secret providers (like \\sops-nix\\, \\^Ggenix\\, or \\systemd-creds\\), allowing users to uniformly declare and inject secrets without vendor lock-in.\n\n*This RFC accompanies the implementation PR at NixOS/nixpkgs#519619*.",
  "closed_at": null,
  "closed_by": null,
  "comments": 5,
  "created_at": "2026-05-13T02:40:01Z",
  "draft": false,
  "id": 4434247418,
  "issue_field_values": [],
  "labels": [],
  "locked": false,
  "milestone": null,
  "number": 201,
  "pull_request": {
   "diff_url": "https://github.com/NixOS/rfcs/pull/201.diff",
   "merged_at": null,
   "patch_url": "https://github.com/NixOS/rfcs/pull/201.patch"
  },
  "reactions": {
   "+1": 0,
   "-1": 0,
   "confused": 0,
   "eyes": 0,
   "heart": 0,
   "hooray": 0,
   "laugh": 0,
   "rocket": 0,
   "total_count": 0
  },
  "repository_url": "https://api.github.com/repos/NixOS/rfcs",
  "state": "open",
  "state_reason": null,
  "title": "[RFC 0201] security.artifacts: backend-agnostic secret management",
  "type": null,
  "updated_at": "2026-05-13T18:13:00Z",
  "user": {
   "id": 140179052,
   "login": "Mutasem-mk4",
   "site_admin": false,
   "type": "User"
  }
 },
 "kind": "pull_request",
 "pull_request": {
  "active_lock_reason": null,
  "additions": 120,
  "assignee": null,
  "assignees": [],
  "auto_merge": null,
  "base": {
   "label": "NixOS:master",
   "ref": "master",
   "repo": {
    "archived": false,
    "default_branch": "master",
    "fork": false,
    "full_name": "NixOS/rfcs",
    "id": 81662501,
    "private": false
   },
   "sha": "c655bdaab40f7a467f75dbb5af4325d991874e44",
   "user": {
    "id": 487568,
    "login": "NixOS",
    "site_admin": false,
    "type": "Organization"
   }
  },
  "body": "This RFC proposes \\security.artifacts\\, a backend-agnostic abstraction layer for managing secrets in NixOS. It decouples NixOS service modules from specific secret providers (like \\sops-nix\\, \\^Ggenix\\, or \\systemd-creds\\), allowing users to uniformly declare and inject secrets without vendor lock-in.\n\n*This RFC accompanies the implementation PR at NixOS/nixpkgs#519619*.",
  "changed_files": 1,
  "closed_at": null,
  "comments": 5,
  "commits": 3,
  "created_at": "2026-05-13T02:40:01Z",
  "deletions": 0,
  "diff_url": "https://github.com/NixOS/rfcs/pull/201.diff",
  "draft": false,
  "head": {
   "label": "Mutasem-mk4:rfc/security-artifacts",
   "ref": "rfc/security-artifacts",
   "repo": {
    "archived": false,
    "default_branch": "master",
    "fork": true,
    "full_name": "Mutasem-mk4/rfcs",
    "id": 1237253269,
    "private": false
   },
   "sha": "d299fda695a33e8c9253e35797cded9e09126dd7",
   "user": {
    "id": 140179052,
    "login": "Mutasem-mk4",
    "site_admin": false,
    "type": "User"
   }
  },
  "id": 3673045384,
  "labels": [],
  "locked": false,
  "maintainer_can_modify": true,
  "merge_commit_sha": "9f1926d6f5cb334717520ea317d01fd7b410ae50",
  "mergeable": true,
  "mergeable_state": "clean",
  "merged": false,
  "merged_at": null,
  "merged_by": null,
  "milestone": null,
  "number": 201,
  "patch_url": "https://github.com/NixOS/rfcs/pull/201.patch",
  "rebaseable": false,
  "requested_reviewers": [],
  "requested_teams": [],
  "review_comments": 9,
  "state": "open",
  "title": "[RFC 0201] security.artifacts: backend-agnostic secret management",
  "updated_at": "2026-05-13T18:13:00Z",
  "user": {
   "id": 140179052,
   "login": "Mutasem-mk4",
   "site_admin": false,
   "type": "User"
  }
 },
 "ref": "NixOS/rfcs#201",
 "review_comments": [
  {
   "body": "NixOS Modules accessing security artifact secrets directly seems more difficult to explain to users than a passwordFile option.\n\nAdding a string based API (the secret name) to NixOS modules doesn't seem ideal.",
   "commit_id": "94b126ea9d4e054f87de5d1b27190c5e616ee917",
   "created_at": "2026-05-13T10:44:14Z",
   "diff_hunk": "@@ -0,0 +1,98 @@\n+---\n+feature: security-artifacts-backend-agnostic\n+start-date: 2026-05-13\n+author: Mutasem Kharma\n+co-authors:\n+shepherd-team:\n+shepherd-leader:\n+related-issues: https://github.com/NixOS/nixpkgs/pull/519619\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+Introduce `security.artifacts`, a backend-agnostic abstraction layer for managing secrets in NixOS. This feature decouples NixOS service modules from specific secret providers (like `sops-nix`, `agenix`, or `systemd-creds`), allowing users to uniformly declare and inject secrets without vendor lock-in.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+Currently, NixOS modules handle secrets inconsistently. Some modules hardcode support for specific secret managers, while most simply provide options like `passwordFile` or `secretFile` and leave it to the user to manually wire up their chosen secret manager. This results in fragile configurations, tight coupling, and difficult migrations when switching between secret backends (e.g., from `agenix` to `sops-nix`).\n+\n+A unified interface will:\n+1. Provide a standard way for NixOS services to consume secrets natively.\n+2. Standardize filesystem permissions, systemd ordering, and deployment paths.\n+3. Allow users to easily switch secret backends without rewriting their entire service configuration.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes the `security.artifacts` module tree:\n+\n+1. **Option Declaration**:\n+   `security.artifacts.secrets.<name>` accepts a submodule configuring the owner, group, mode, and target path of the secret.\n+\n+2. **Providers**:\n+   The abstraction supports pluggable backends (`security.artifacts.provider`):\n+   - `sops-nix`\n+   - `agenix`\n+   - `systemd-creds`\n+   - `dummy` (for CI/CD environments where secrets are mocked plaintext)\n+\n+3. **Systemd Synchronization**:\n+   A global systemd target `nixos-artifacts-secrets.target` is introduced. All secrets must be provisioned before this target is reached. Downstream services that consume secrets simply need `Wants = [ \"nixos-artifacts-secrets.target\" ]` and `After = [ \"nixos-artifacts-secrets.target\" ]`.\n+\n+4. **Security Assertions**:\n+   Evaluation-time assertions guarantee that the resolved paths of the secrets do not leak into `/nix/store`.\n+\n+# Examples and Interactions\n+[examples-and-interactions]: #examples-and-interactions\n+\n+A user deploying a Postgres database password using `sops-nix`:\n+\n+```nix\n+security.artifacts.enable = true;\n+security.artifacts.provider = \"sops-nix\";\n+\n+security.artifacts.secrets.\"postgres-pw\" = {\n+  owner = \"postgres\";\n+  group = \"postgres\";\n+  mode = \"0400\";\n+};\n+\n+services.postgresql = {\n+  enable = true;\n+  # ...\n+  # The module can directly read the provisioned artifact or accept the path.",
   "id": 3233495447,
   "line": null,
   "original_commit_id": "94b126ea9d4e054f87de5d1b27190c5e616ee917",
   "original_line": 65,
   "original_position": 65,
   "original_start_line": null,
   "path": "rfcs/0201-security-artifacts.md",
   "position": 1,
   "pull_request_review_id": 4280733658,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": null,
   "start_side": null,
   "subject_type": "line",
   "updated_at": "2026-05-13T10:51:06Z",
   "user": {
    "id": 1183303,
    "login": "truh",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Do you have an article/blog posts etc that explains the use of systemd-creds with NixOS you have in mind? I haven't encountered it yet and wasn't able to find anything with a quick web search.",
   "commit_id": "d299fda695a33e8c9253e35797cded9e09126dd7",
   "created_at": "2026-05-13T10:46:47Z",
   "diff_hunk": "@@ -0,0 +1,98 @@\n+---\n+feature: security-artifacts-backend-agnostic\n+start-date: 2026-05-13\n+author: Mutasem Kharma\n+co-authors:\n+shepherd-team:\n+shepherd-leader:\n+related-issues: https://github.com/NixOS/nixpkgs/pull/519619\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+Introduce `security.artifacts`, a backend-agnostic abstraction layer for managing secrets in NixOS. This feature decouples NixOS service modules from specific secret providers (like `sops-nix`, `agenix`, or `systemd-creds`), allowing users to uniformly declare and inject secrets without vendor lock-in.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+Currently, NixOS modules handle secrets inconsistently. Some modules hardcode support for specific secret managers, while most simply provide options like `passwordFile` or `secretFile` and leave it to the user to manually wire up their chosen secret manager. This results in fragile configurations, tight coupling, and difficult migrations when switching between secret backends (e.g., from `agenix` to `sops-nix`).\n+\n+A unified interface will:\n+1. Provide a standard way for NixOS services to consume secrets natively.\n+2. Standardize filesystem permissions, systemd ordering, and deployment paths.\n+3. Allow users to easily switch secret backends without rewriting their entire service configuration.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes the `security.artifacts` module tree:\n+\n+1. **Option Declaration**:\n+   `security.artifacts.secrets.<name>` accepts a submodule configuring the owner, group, mode, and target path of the secret.\n+\n+2. **Providers**:\n+   The abstraction supports pluggable backends (`security.artifacts.provider`):\n+   - `sops-nix`\n+   - `agenix`\n+   - `systemd-creds`",
   "id": 3233513639,
   "line": 38,
   "original_commit_id": "94b126ea9d4e054f87de5d1b27190c5e616ee917",
   "original_line": 38,
   "original_position": 38,
   "original_start_line": null,
   "path": "rfcs/0201-security-artifacts.md",
   "position": 38,
   "pull_request_review_id": 4280733658,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": null,
   "start_side": null,
   "subject_type": "line",
   "updated_at": "2026-05-13T10:51:06Z",
   "user": {
    "id": 1183303,
    "login": "truh",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "How do modules consume secrets managed with security.artifacts? I think that's worth including in the RFC.",
   "commit_id": "94b126ea9d4e054f87de5d1b27190c5e616ee917",
   "created_at": "2026-05-13T10:48:05Z",
   "diff_hunk": "@@ -0,0 +1,98 @@\n+---\n+feature: security-artifacts-backend-agnostic\n+start-date: 2026-05-13\n+author: Mutasem Kharma\n+co-authors:\n+shepherd-team:\n+shepherd-leader:\n+related-issues: https://github.com/NixOS/nixpkgs/pull/519619\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+Introduce `security.artifacts`, a backend-agnostic abstraction layer for managing secrets in NixOS. This feature decouples NixOS service modules from specific secret providers (like `sops-nix`, `agenix`, or `systemd-creds`), allowing users to uniformly declare and inject secrets without vendor lock-in.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+Currently, NixOS modules handle secrets inconsistently. Some modules hardcode support for specific secret managers, while most simply provide options like `passwordFile` or `secretFile` and leave it to the user to manually wire up their chosen secret manager. This results in fragile configurations, tight coupling, and difficult migrations when switching between secret backends (e.g., from `agenix` to `sops-nix`).\n+\n+A unified interface will:\n+1. Provide a standard way for NixOS services to consume secrets natively.\n+2. Standardize filesystem permissions, systemd ordering, and deployment paths.\n+3. Allow users to easily switch secret backends without rewriting their entire service configuration.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes the `security.artifacts` module tree:\n+\n+1. **Option Declaration**:\n+   `security.artifacts.secrets.<name>` accepts a submodule configuring the owner, group, mode, and target path of the secret.\n+\n+2. **Providers**:\n+   The abstraction supports pluggable backends (`security.artifacts.provider`):\n+   - `sops-nix`\n+   - `agenix`\n+   - `systemd-creds`\n+   - `dummy` (for CI/CD environments where secrets are mocked plaintext)\n+\n+3. **Systemd Synchronization**:\n+   A global systemd target `nixos-artifacts-secrets.target` is introduced. All secrets must be provisioned before this target is reached. Downstream services that consume secrets simply need `Wants = [ \"nixos-artifacts-secrets.target\" ]` and `After = [ \"nixos-artifacts-secrets.target\" ]`.\n+\n+4. **Security Assertions**:\n+   Evaluation-time assertions guarantee that the resolved paths of the secrets do not leak into `/nix/store`.\n+\n+# Examples and Interactions\n+[examples-and-interactions]: #examples-and-interactions\n+\n+A user deploying a Postgres database password using `sops-nix`:\n+\n+```nix\n+security.artifacts.enable = true;\n+security.artifacts.provider = \"sops-nix\";\n+\n+security.artifacts.secrets.\"postgres-pw\" = {\n+  owner = \"postgres\";\n+  group = \"postgres\";\n+  mode = \"0400\";\n+};\n+\n+services.postgresql = {\n+  enable = true;\n+  # ...\n+  # The module can directly read the provisioned artifact or accept the path.",
   "id": 3233521929,
   "line": null,
   "original_commit_id": "94b126ea9d4e054f87de5d1b27190c5e616ee917",
   "original_line": 65,
   "original_position": 65,
   "original_start_line": null,
   "path": "rfcs/0201-security-artifacts.md",
   "position": 1,
   "pull_request_review_id": 4280733658,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": null,
   "start_side": null,
   "subject_type": "line",
   "updated_at": "2026-05-13T10:51:06Z",
   "user": {
    "id": 1183303,
    "login": "truh",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "```suggestion\n   The abstraction supports pluggable backends (`security.artifacts.defaultProvider`):\n```\n\nNit:",
   "commit_id": "d299fda695a33e8c9253e35797cded9e09126dd7",
   "created_at": "2026-05-13T15:43:26Z",
   "diff_hunk": "@@ -0,0 +1,120 @@\n+---\n+feature: security-artifacts-backend-agnostic\n+start-date: 2026-05-13\n+author: Mutasem Kharma\n+co-authors:\n+shepherd-team:\n+shepherd-leader:\n+related-issues: https://github.com/NixOS/nixpkgs/pull/519619\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+Introduce `security.artifacts`, a backend-agnostic abstraction layer for managing secrets in NixOS. This feature decouples NixOS service modules from specific secret providers (like `sops-nix`, `agenix`, or `systemd-creds`), allowing users to uniformly declare and inject secrets without vendor lock-in.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+Currently, NixOS modules handle secrets inconsistently. Some modules hardcode support for specific secret managers, while most simply provide options like `passwordFile` or `secretFile` and leave it to the user to manually wire up their chosen secret manager. This results in fragile configurations, tight coupling, and difficult migrations when switching between secret backends (e.g., from `agenix` to `sops-nix`).\n+\n+A unified interface will:\n+1. Provide a standard way for NixOS services to consume secrets natively.\n+2. Standardize filesystem permissions, systemd ordering, and deployment paths.\n+3. Allow users to easily switch secret backends without rewriting their entire service configuration.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes the `security.artifacts` module tree:\n+\n+1. **Option Declaration**:\n+   `security.artifacts.secrets.<name>` accepts a submodule configuring the owner, group, mode, target path, and **optional per-secret provider**.\n+\n+2. **Providers**:\n+   The abstraction supports pluggable backends (`security.artifacts.provider`):",
   "id": 3235575741,
   "line": 35,
   "original_commit_id": "d299fda695a33e8c9253e35797cded9e09126dd7",
   "original_line": 35,
   "original_position": 35,
   "original_start_line": null,
   "path": "rfcs/0201-security-artifacts.md",
   "position": 35,
   "pull_request_review_id": 4283190428,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": null,
   "start_side": null,
   "subject_type": "line",
   "updated_at": "2026-05-13T15:53:27Z",
   "user": {
    "id": 494012,
    "login": "kevincox",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I suspect this code be an issue for some providers like sops that do decryption on-host and can have any number of failure modes such as \"New key `x` is not decryptable\". In this case it is probably better to have a distinct target per-key so that most services can still boot up. Providers that do just copy secrets in one batch or otherwise can't have partial failures can just have these all be the same but providers that do have this issue can create a target per key.",
   "commit_id": "d299fda695a33e8c9253e35797cded9e09126dd7",
   "created_at": "2026-05-13T15:45:58Z",
   "diff_hunk": "@@ -0,0 +1,120 @@\n+---\n+feature: security-artifacts-backend-agnostic\n+start-date: 2026-05-13\n+author: Mutasem Kharma\n+co-authors:\n+shepherd-team:\n+shepherd-leader:\n+related-issues: https://github.com/NixOS/nixpkgs/pull/519619\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+Introduce `security.artifacts`, a backend-agnostic abstraction layer for managing secrets in NixOS. This feature decouples NixOS service modules from specific secret providers (like `sops-nix`, `agenix`, or `systemd-creds`), allowing users to uniformly declare and inject secrets without vendor lock-in.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+Currently, NixOS modules handle secrets inconsistently. Some modules hardcode support for specific secret managers, while most simply provide options like `passwordFile` or `secretFile` and leave it to the user to manually wire up their chosen secret manager. This results in fragile configurations, tight coupling, and difficult migrations when switching between secret backends (e.g., from `agenix` to `sops-nix`).\n+\n+A unified interface will:\n+1. Provide a standard way for NixOS services to consume secrets natively.\n+2. Standardize filesystem permissions, systemd ordering, and deployment paths.\n+3. Allow users to easily switch secret backends without rewriting their entire service configuration.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes the `security.artifacts` module tree:\n+\n+1. **Option Declaration**:\n+   `security.artifacts.secrets.<name>` accepts a submodule configuring the owner, group, mode, target path, and **optional per-secret provider**.\n+\n+2. **Providers**:\n+   The abstraction supports pluggable backends (`security.artifacts.provider`):\n+   - `sops-nix`\n+   - `agenix`\n+   - `systemd-creds`\n+   - `external` (Assumes secret is provisioned outside of NixOS, e.g. cloud-init)\n+   - `dummy` (for CI/CD environments where secrets are mocked plaintext)\n+\n+   Users can set a global provider or override it per-secret, allowing for mixed configurations (e.g. TPM2-bound local secrets via `systemd-creds` alongside shared GitOps secrets via `sops-nix`).\n+\n+3. **Systemd Synchronization**:\n+   A global systemd target `nixos-artifacts-secrets.target` is introduced. All secrets must be provisioned before this target is reached. Downstream services that consume secrets simply need `Wants = [ \"nixos-artifacts-secrets.target\" ]` and `After = [ \"nixos-artifacts-secrets.target\" ]`.",
   "id": 3235591496,
   "line": 45,
   "original_commit_id": "d299fda695a33e8c9253e35797cded9e09126dd7",
   "original_line": 45,
   "original_position": 45,
   "original_start_line": 44,
   "path": "rfcs/0201-security-artifacts.md",
   "position": 45,
   "pull_request_review_id": 4283190428,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 44,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2026-05-13T15:53:27Z",
   "user": {
    "id": 494012,
    "login": "kevincox",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "I worry that figuring out how to ensure that the provider can write to this directory will be complicated. Is it assumed that this directory exists? Do we assume that all providers run as root and can write to any existing directory?\n\nMaybe it would be better to make the `path` an \"out parameter\" which is set by the provider and the provider picks what directory to put files in (possibly with some provider-specific configuration).",
   "commit_id": "d299fda695a33e8c9253e35797cded9e09126dd7",
   "created_at": "2026-05-13T15:48:08Z",
   "diff_hunk": "@@ -0,0 +1,120 @@\n+---\n+feature: security-artifacts-backend-agnostic\n+start-date: 2026-05-13\n+author: Mutasem Kharma\n+co-authors:\n+shepherd-team:\n+shepherd-leader:\n+related-issues: https://github.com/NixOS/nixpkgs/pull/519619\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+Introduce `security.artifacts`, a backend-agnostic abstraction layer for managing secrets in NixOS. This feature decouples NixOS service modules from specific secret providers (like `sops-nix`, `agenix`, or `systemd-creds`), allowing users to uniformly declare and inject secrets without vendor lock-in.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+Currently, NixOS modules handle secrets inconsistently. Some modules hardcode support for specific secret managers, while most simply provide options like `passwordFile` or `secretFile` and leave it to the user to manually wire up their chosen secret manager. This results in fragile configurations, tight coupling, and difficult migrations when switching between secret backends (e.g., from `agenix` to `sops-nix`).\n+\n+A unified interface will:\n+1. Provide a standard way for NixOS services to consume secrets natively.\n+2. Standardize filesystem permissions, systemd ordering, and deployment paths.\n+3. Allow users to easily switch secret backends without rewriting their entire service configuration.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes the `security.artifacts` module tree:\n+\n+1. **Option Declaration**:\n+   `security.artifacts.secrets.<name>` accepts a submodule configuring the owner, group, mode, target path, and **optional per-secret provider**.\n+\n+2. **Providers**:\n+   The abstraction supports pluggable backends (`security.artifacts.provider`):\n+   - `sops-nix`\n+   - `agenix`\n+   - `systemd-creds`\n+   - `external` (Assumes secret is provisioned outside of NixOS, e.g. cloud-init)\n+   - `dummy` (for CI/CD environments where secrets are mocked plaintext)\n+\n+   Users can set a global provider or override it per-secret, allowing for mixed configurations (e.g. TPM2-bound local secrets via `systemd-creds` alongside shared GitOps secrets via `sops-nix`).\n+\n+3. **Systemd Synchronization**:\n+   A global systemd target `nixos-artifacts-secrets.target` is introduced. All secrets must be provisioned before this target is reached. Downstream services that consume secrets simply need `Wants = [ \"nixos-artifacts-secrets.target\" ]` and `After = [ \"nixos-artifacts-secrets.target\" ]`.\n+\n+4. **Security Assertions**:\n+   Evaluation-time assertions guarantee that the resolved paths of the secrets do not leak into `/nix/store` and that required source files are specified for providers that need them.\n+\n+# Examples and Interactions\n+[examples-and-interactions]: #examples-and-interactions\n+\n+### Basic Usage with `sops-nix`:\n+\n+```nix\n+security.artifacts.enable = true;\n+security.artifacts.provider = \"sops-nix\";\n+\n+security.artifacts.secrets.\"postgres-pw\" = {\n+  owner = \"postgres\";\n+  group = \"postgres\";\n+  mode = \"0400\";\n+  source = ./secrets/postgres.yaml;\n+};\n+\n+services.postgresql = {\n+  enable = true;\n+  # Explicitly reference the artifact path\n+  passwordFile = config.security.artifacts.secrets.\"postgres-pw\".path;\n+};\n+```\n+\n+### Mixed Providers (TPM2 + GitOps):\n+\n+```nix\n+security.artifacts.enable = true;\n+security.artifacts.provider = \"sops-nix\"; # Global default\n+\n+security.artifacts.secrets = {\n+  \"shared-app-key\" = {\n+    source = ./secrets/app.yaml;\n+  };\n+  \n+  \"local-tpm-secret\" = {\n+    provider = \"systemd-creds\"; # Override global default\n+    path = \"/run/secrets/tpm-key\";",
   "id": 3235606185,
   "line": 86,
   "original_commit_id": "d299fda695a33e8c9253e35797cded9e09126dd7",
   "original_line": 86,
   "original_position": 86,
   "original_start_line": null,
   "path": "rfcs/0201-security-artifacts.md",
   "position": 86,
   "pull_request_review_id": 4283190428,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": null,
   "start_side": null,
   "subject_type": "line",
   "updated_at": "2026-05-13T15:53:27Z",
   "user": {
    "id": 494012,
    "login": "kevincox",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "\n```suggestion\n- **colmena**: Similar pattern using `deployment.keys.<name>` (inherited from nixops).\n\n```\n\nNot critical but relevant IMHO. It also shows a different style where the secrets are just copied from the deployment host without any on-host decryption step.",
   "commit_id": "d299fda695a33e8c9253e35797cded9e09126dd7",
   "created_at": "2026-05-13T15:50:45Z",
   "diff_hunk": "@@ -0,0 +1,120 @@\n+---\n+feature: security-artifacts-backend-agnostic\n+start-date: 2026-05-13\n+author: Mutasem Kharma\n+co-authors:\n+shepherd-team:\n+shepherd-leader:\n+related-issues: https://github.com/NixOS/nixpkgs/pull/519619\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+Introduce `security.artifacts`, a backend-agnostic abstraction layer for managing secrets in NixOS. This feature decouples NixOS service modules from specific secret providers (like `sops-nix`, `agenix`, or `systemd-creds`), allowing users to uniformly declare and inject secrets without vendor lock-in.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+Currently, NixOS modules handle secrets inconsistently. Some modules hardcode support for specific secret managers, while most simply provide options like `passwordFile` or `secretFile` and leave it to the user to manually wire up their chosen secret manager. This results in fragile configurations, tight coupling, and difficult migrations when switching between secret backends (e.g., from `agenix` to `sops-nix`).\n+\n+A unified interface will:\n+1. Provide a standard way for NixOS services to consume secrets natively.\n+2. Standardize filesystem permissions, systemd ordering, and deployment paths.\n+3. Allow users to easily switch secret backends without rewriting their entire service configuration.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes the `security.artifacts` module tree:\n+\n+1. **Option Declaration**:\n+   `security.artifacts.secrets.<name>` accepts a submodule configuring the owner, group, mode, target path, and **optional per-secret provider**.\n+\n+2. **Providers**:\n+   The abstraction supports pluggable backends (`security.artifacts.provider`):\n+   - `sops-nix`\n+   - `agenix`\n+   - `systemd-creds`\n+   - `external` (Assumes secret is provisioned outside of NixOS, e.g. cloud-init)\n+   - `dummy` (for CI/CD environments where secrets are mocked plaintext)\n+\n+   Users can set a global provider or override it per-secret, allowing for mixed configurations (e.g. TPM2-bound local secrets via `systemd-creds` alongside shared GitOps secrets via `sops-nix`).\n+\n+3. **Systemd Synchronization**:\n+   A global systemd target `nixos-artifacts-secrets.target` is introduced. All secrets must be provisioned before this target is reached. Downstream services that consume secrets simply need `Wants = [ \"nixos-artifacts-secrets.target\" ]` and `After = [ \"nixos-artifacts-secrets.target\" ]`.\n+\n+4. **Security Assertions**:\n+   Evaluation-time assertions guarantee that the resolved paths of the secrets do not leak into `/nix/store` and that required source files are specified for providers that need them.\n+\n+# Examples and Interactions\n+[examples-and-interactions]: #examples-and-interactions\n+\n+### Basic Usage with `sops-nix`:\n+\n+```nix\n+security.artifacts.enable = true;\n+security.artifacts.provider = \"sops-nix\";\n+\n+security.artifacts.secrets.\"postgres-pw\" = {\n+  owner = \"postgres\";\n+  group = \"postgres\";\n+  mode = \"0400\";\n+  source = ./secrets/postgres.yaml;\n+};\n+\n+services.postgresql = {\n+  enable = true;\n+  # Explicitly reference the artifact path\n+  passwordFile = config.security.artifacts.secrets.\"postgres-pw\".path;\n+};\n+```\n+\n+### Mixed Providers (TPM2 + GitOps):\n+\n+```nix\n+security.artifacts.enable = true;\n+security.artifacts.provider = \"sops-nix\"; # Global default\n+\n+security.artifacts.secrets = {\n+  \"shared-app-key\" = {\n+    source = ./secrets/app.yaml;\n+  };\n+  \n+  \"local-tpm-secret\" = {\n+    provider = \"systemd-creds\"; # Override global default\n+    path = \"/run/secrets/tpm-key\";\n+  };\n+};\n+```\n+\n+If the user switches to `agenix`, they simply change the `provider` string, and the system seamlessly uses the new backend without requiring any changes to the `postgresql` configuration or the file paths.\n+\n+# Drawbacks\n+[drawbacks]: #drawbacks\n+\n+- **Ecosystem Fragmentation during Transition**: Until all major NixOS services adopt this standard, some services will use `security.artifacts` while others will continue using `LoadCredential` or raw paths.\n+- **Maintenance of Translation Layers**: The translation layers for out-of-tree backends (`agenix`, `sops-nix`) will need to be kept up to date with upstream changes.\n+\n+# Alternatives\n+[alternatives]: #alternatives\n+\n+- **Status Quo**: Continue using `passwordFile` options and leave integration up to the user. This keeps NixOS core simpler but pushes complexity to the end-user.\n+- **systemd-creds Only**: Force all secrets to use `systemd-creds`. While powerful, `systemd-creds` does not natively support GitOps workflows (e.g. encrypting with age/PGP and committing to a repo) as easily as `sops` or `agenix` without significant wrapper tooling.\n+\n+# Prior art\n+[prior-art]: #prior-art\n+\n+- **sops-nix**: Pioneered the `sops.secrets.<name>` pattern, which heavily inspired this RFC's interface.\n+- **agenix**: Similar pattern using `age.secrets.<name>`.\n+",
   "id": 3235623561,
   "line": 110,
   "original_commit_id": "d299fda695a33e8c9253e35797cded9e09126dd7",
   "original_line": 110,
   "original_position": 110,
   "original_start_line": null,
   "path": "rfcs/0201-security-artifacts.md",
   "position": 110,
   "pull_request_review_id": 4283190428,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": null,
   "start_side": null,
   "subject_type": "line",
   "updated_at": "2026-05-13T15:53:27Z",
   "user": {
    "id": 494012,
    "login": "kevincox",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "This would be very cool. For example sometimes services need secrets for auth between themselves but the secret doesn't matter as long as both services know it. I see a few use cases:\n\n1. Generated per-deploy.\n2. Generated one for the lifetime of the host.\n3. Generated deterministically but just to use logic (decrypt, base64, ...)\n\nFor example colmena supports 1 and 3 via a [`keyCommand` option](https://colmena.cli.rs/unstable/features/keys.html).",
   "commit_id": "d299fda695a33e8c9253e35797cded9e09126dd7",
   "created_at": "2026-05-13T15:52:57Z",
   "diff_hunk": "@@ -0,0 +1,120 @@\n+---\n+feature: security-artifacts-backend-agnostic\n+start-date: 2026-05-13\n+author: Mutasem Kharma\n+co-authors:\n+shepherd-team:\n+shepherd-leader:\n+related-issues: https://github.com/NixOS/nixpkgs/pull/519619\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+Introduce `security.artifacts`, a backend-agnostic abstraction layer for managing secrets in NixOS. This feature decouples NixOS service modules from specific secret providers (like `sops-nix`, `agenix`, or `systemd-creds`), allowing users to uniformly declare and inject secrets without vendor lock-in.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+Currently, NixOS modules handle secrets inconsistently. Some modules hardcode support for specific secret managers, while most simply provide options like `passwordFile` or `secretFile` and leave it to the user to manually wire up their chosen secret manager. This results in fragile configurations, tight coupling, and difficult migrations when switching between secret backends (e.g., from `agenix` to `sops-nix`).\n+\n+A unified interface will:\n+1. Provide a standard way for NixOS services to consume secrets natively.\n+2. Standardize filesystem permissions, systemd ordering, and deployment paths.\n+3. Allow users to easily switch secret backends without rewriting their entire service configuration.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes the `security.artifacts` module tree:\n+\n+1. **Option Declaration**:\n+   `security.artifacts.secrets.<name>` accepts a submodule configuring the owner, group, mode, target path, and **optional per-secret provider**.\n+\n+2. **Providers**:\n+   The abstraction supports pluggable backends (`security.artifacts.provider`):\n+   - `sops-nix`\n+   - `agenix`\n+   - `systemd-creds`\n+   - `external` (Assumes secret is provisioned outside of NixOS, e.g. cloud-init)\n+   - `dummy` (for CI/CD environments where secrets are mocked plaintext)\n+\n+   Users can set a global provider or override it per-secret, allowing for mixed configurations (e.g. TPM2-bound local secrets via `systemd-creds` alongside shared GitOps secrets via `sops-nix`).\n+\n+3. **Systemd Synchronization**:\n+   A global systemd target `nixos-artifacts-secrets.target` is introduced. All secrets must be provisioned before this target is reached. Downstream services that consume secrets simply need `Wants = [ \"nixos-artifacts-secrets.target\" ]` and `After = [ \"nixos-artifacts-secrets.target\" ]`.\n+\n+4. **Security Assertions**:\n+   Evaluation-time assertions guarantee that the resolved paths of the secrets do not leak into `/nix/store` and that required source files are specified for providers that need them.\n+\n+# Examples and Interactions\n+[examples-and-interactions]: #examples-and-interactions\n+\n+### Basic Usage with `sops-nix`:\n+\n+```nix\n+security.artifacts.enable = true;\n+security.artifacts.provider = \"sops-nix\";\n+\n+security.artifacts.secrets.\"postgres-pw\" = {\n+  owner = \"postgres\";\n+  group = \"postgres\";\n+  mode = \"0400\";\n+  source = ./secrets/postgres.yaml;\n+};\n+\n+services.postgresql = {\n+  enable = true;\n+  # Explicitly reference the artifact path\n+  passwordFile = config.security.artifacts.secrets.\"postgres-pw\".path;\n+};\n+```\n+\n+### Mixed Providers (TPM2 + GitOps):\n+\n+```nix\n+security.artifacts.enable = true;\n+security.artifacts.provider = \"sops-nix\"; # Global default\n+\n+security.artifacts.secrets = {\n+  \"shared-app-key\" = {\n+    source = ./secrets/app.yaml;\n+  };\n+  \n+  \"local-tpm-secret\" = {\n+    provider = \"systemd-creds\"; # Override global default\n+    path = \"/run/secrets/tpm-key\";\n+  };\n+};\n+```\n+\n+If the user switches to `agenix`, they simply change the `provider` string, and the system seamlessly uses the new backend without requiring any changes to the `postgresql` configuration or the file paths.\n+\n+# Drawbacks\n+[drawbacks]: #drawbacks\n+\n+- **Ecosystem Fragmentation during Transition**: Until all major NixOS services adopt this standard, some services will use `security.artifacts` while others will continue using `LoadCredential` or raw paths.\n+- **Maintenance of Translation Layers**: The translation layers for out-of-tree backends (`agenix`, `sops-nix`) will need to be kept up to date with upstream changes.\n+\n+# Alternatives\n+[alternatives]: #alternatives\n+\n+- **Status Quo**: Continue using `passwordFile` options and leave integration up to the user. This keeps NixOS core simpler but pushes complexity to the end-user.\n+- **systemd-creds Only**: Force all secrets to use `systemd-creds`. While powerful, `systemd-creds` does not natively support GitOps workflows (e.g. encrypting with age/PGP and committing to a repo) as easily as `sops` or `agenix` without significant wrapper tooling.\n+\n+# Prior art\n+[prior-art]: #prior-art\n+\n+- **sops-nix**: Pioneered the `sops.secrets.<name>` pattern, which heavily inspired this RFC's interface.\n+- **agenix**: Similar pattern using `age.secrets.<name>`.\n+\n+# Unresolved questions\n+[unresolved]: #unresolved-questions\n+\n+- Should `security.artifacts` also handle dynamically generated secrets (e.g. `nixos-generate-config` generating an SSH key) rather than just static deployed secrets?",
   "id": 3235639106,
   "line": 114,
   "original_commit_id": "d299fda695a33e8c9253e35797cded9e09126dd7",
   "original_line": 114,
   "original_position": 114,
   "original_start_line": null,
   "path": "rfcs/0201-security-artifacts.md",
   "position": 114,
   "pull_request_review_id": 4283190428,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": null,
   "start_side": null,
   "subject_type": "line",
   "updated_at": "2026-05-13T15:53:27Z",
   "user": {
    "id": 494012,
    "login": "kevincox",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "A lot of the prior art allows controlling the mode, user, groups of the secrets but this proposal doesn't mention any of this. This need is somewhat mitigated by systemd's LoadCredential but IMHO there are still many use cases (that for better or worse) still need to have files lying around owned by a particular user or group. What is the plan for supporting these use cases?",
   "commit_id": "d299fda695a33e8c9253e35797cded9e09126dd7",
   "created_at": "2026-05-13T15:55:45Z",
   "diff_hunk": "@@ -0,0 +1,120 @@\n+---\n+feature: security-artifacts-backend-agnostic\n+start-date: 2026-05-13\n+author: Mutasem Kharma\n+co-authors:\n+shepherd-team:\n+shepherd-leader:\n+related-issues: https://github.com/NixOS/nixpkgs/pull/519619\n+---\n+\n+# Summary\n+[summary]: #summary\n+\n+Introduce `security.artifacts`, a backend-agnostic abstraction layer for managing secrets in NixOS. This feature decouples NixOS service modules from specific secret providers (like `sops-nix`, `agenix`, or `systemd-creds`), allowing users to uniformly declare and inject secrets without vendor lock-in.\n+\n+# Motivation\n+[motivation]: #motivation\n+\n+Currently, NixOS modules handle secrets inconsistently. Some modules hardcode support for specific secret managers, while most simply provide options like `passwordFile` or `secretFile` and leave it to the user to manually wire up their chosen secret manager. This results in fragile configurations, tight coupling, and difficult migrations when switching between secret backends (e.g., from `agenix` to `sops-nix`).\n+\n+A unified interface will:\n+1. Provide a standard way for NixOS services to consume secrets natively.\n+2. Standardize filesystem permissions, systemd ordering, and deployment paths.\n+3. Allow users to easily switch secret backends without rewriting their entire service configuration.\n+\n+# Detailed design\n+[design]: #detailed-design\n+\n+This RFC proposes the `security.artifacts` module tree:\n+\n+1. **Option Declaration**:\n+   `security.artifacts.secrets.<name>` accepts a submodule configuring the owner, group, mode, target path, and **optional per-secret provider**.\n+\n+2. **Providers**:\n+   The abstraction supports pluggable backends (`security.artifacts.provider`):\n+   - `sops-nix`\n+   - `agenix`\n+   - `systemd-creds`\n+   - `external` (Assumes secret is provisioned outside of NixOS, e.g. cloud-init)\n+   - `dummy` (for CI/CD environments where secrets are mocked plaintext)\n+\n+   Users can set a global provider or override it per-secret, allowing for mixed configurations (e.g. TPM2-bound local secrets via `systemd-creds` alongside shared GitOps secrets via `sops-nix`).\n+\n+3. **Systemd Synchronization**:\n+   A global systemd target `nixos-artifacts-secrets.target` is introduced. All secrets must be provisioned before this target is reached. Downstream services that consume secrets simply need `Wants = [ \"nixos-artifacts-secrets.target\" ]` and `After = [ \"nixos-artifacts-secrets.target\" ]`.\n+\n+4. **Security Assertions**:\n+   Evaluation-time assertions guarantee that the resolved paths of the secrets do not leak into `/nix/store` and that required source files are specified for providers that need them.\n+\n+# Examples and Interactions\n+[examples-and-interactions]: #examples-and-interactions\n+\n+### Basic Usage with `sops-nix`:\n+\n+```nix\n+security.artifacts.enable = true;\n+security.artifacts.provider = \"sops-nix\";\n+\n+security.artifacts.secrets.\"postgres-pw\" = {\n+  owner = \"postgres\";\n+  group = \"postgres\";\n+  mode = \"0400\";\n+  source = ./secrets/postgres.yaml;\n+};\n+\n+services.postgresql = {\n+  enable = true;\n+  # Explicitly reference the artifact path\n+  passwordFile = config.security.artifacts.secrets.\"postgres-pw\".path;\n+};\n+```\n+\n+### Mixed Providers (TPM2 + GitOps):\n+\n+```nix\n+security.artifacts.enable = true;\n+security.artifacts.provider = \"sops-nix\"; # Global default\n+\n+security.artifacts.secrets = {\n+  \"shared-app-key\" = {\n+    source = ./secrets/app.yaml;\n+  };\n+  \n+  \"local-tpm-secret\" = {\n+    provider = \"systemd-creds\"; # Override global default\n+    path = \"/run/secrets/tpm-key\";\n+  };\n+};\n+```\n+\n+If the user switches to `agenix`, they simply change the `provider` string, and the system seamlessly uses the new backend without requiring any changes to the `postgresql` configuration or the file paths.\n+\n+# Drawbacks\n+[drawbacks]: #drawbacks\n+\n+- **Ecosystem Fragmentation during Transition**: Until all major NixOS services adopt this standard, some services will use `security.artifacts` while others will continue using `LoadCredential` or raw paths.\n+- **Maintenance of Translation Layers**: The translation layers for out-of-tree backends (`agenix`, `sops-nix`) will need to be kept up to date with upstream changes.\n+\n+# Alternatives\n+[alternatives]: #alternatives\n+\n+- **Status Quo**: Continue using `passwordFile` options and leave integration up to the user. This keeps NixOS core simpler but pushes complexity to the end-user.\n+- **systemd-creds Only**: Force all secrets to use `systemd-creds`. While powerful, `systemd-creds` does not natively support GitOps workflows (e.g. encrypting with age/PGP and committing to a repo) as easily as `sops` or `agenix` without significant wrapper tooling.\n+\n+# Prior art\n+[prior-art]: #prior-art",
   "id": 3235658245,
   "line": 106,
   "original_commit_id": "d299fda695a33e8c9253e35797cded9e09126dd7",
   "original_line": 106,
   "original_position": 106,
   "original_start_line": 105,
   "path": "rfcs/0201-security-artifacts.md",
   "position": 106,
   "pull_request_review_id": 4283284441,
   "reactions": {
    "+1": 0,
    "-1": 0,
    "confused": 0,
    "eyes": 0,
    "heart": 0,
    "hooray": 0,
    "laugh": 0,
    "rocket": 0,
    "total_count": 0
   },
   "side": "RIGHT",
   "start_line": 105,
   "start_side": "RIGHT",
   "subject_type": "line",
   "updated_at": "2026-05-13T15:55:45Z",
   "user": {
    "id": 494012,
    "login": "kevincox",
    "site_admin": false,
    "type": "User"
   }
  }
 ],
 "reviews": [
  {
   "body": "",
   "commit_id": "94b126ea9d4e054f87de5d1b27190c5e616ee917",
   "id": 4280733658,
   "state": "COMMENTED",
   "submitted_at": "2026-05-13T10:51:06Z",
   "user": {
    "id": 1183303,
    "login": "truh",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "Thanks for taking this on. Being able to create a common interface would make using NixOS modules far more pleasant by removing those pesky required fields.",
   "commit_id": "d299fda695a33e8c9253e35797cded9e09126dd7",
   "id": 4283190428,
   "state": "COMMENTED",
   "submitted_at": "2026-05-13T15:53:27Z",
   "user": {
    "id": 494012,
    "login": "kevincox",
    "site_admin": false,
    "type": "User"
   }
  },
  {
   "body": "",
   "commit_id": "d299fda695a33e8c9253e35797cded9e09126dd7",
   "id": 4283284441,
   "state": "COMMENTED",
   "submitted_at": "2026-05-13T15:55:45Z",
   "user": {
    "id": 494012,
    "login": "kevincox",
    "site_admin": false,
    "type": "User"
   }
  }
 ],
 "sources": [
  "rfc-201-security-artifacts",
  "rfcs-modular-service-prs"
 ],
 "timeline": [
  {
   "actor": {
    "id": 140179052,
    "login": "Mutasem-mk4",
    "site_admin": false,
    "type": "User"
   },
   "commit_id": null,
   "commit_url": null,
   "created_at": "2026-05-13T03:59:09Z",
   "event": "renamed",
   "id": 25463036522,
   "rename": {
    "from": "[RFC 0182] security.artifacts: backend-agnostic secret management",
    "to": "[RFC 0201] security.artifacts: backend-agnostic secret management"
   }
  }
 ]
}
